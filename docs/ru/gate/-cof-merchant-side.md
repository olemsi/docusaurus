# Проведение оплаты со списаниями по запросу

## Общая информация

*Повторяемая оплата со списаниями по запросу*— это тип платежа, в котором на основании одного исходного запроса выполняется один (повторяемый) перевод денежных средств от пользователя мерчанту с использованием сохраненных платежных данных и без процедуры подтверждения подлинности платежного инструмента пользователя (например, без ввода кода проверки подлинности карты).

ru_gate_recurring_model.ditaВ платежной платформе оплата со списаниями по запросу проводится, как описано в разделе.

## Схема оплаты

Для проведения повторяемой оплаты со списаниями по запросу необходимо:

Для каждого последующего списания необходимо повторно передавать запросы на проведение повторяемой оплаты и принимать оповещения о результате.

ru_gate_interaction_organisation.dita; общая информация о работе с API — в разделеИнформация о форматах запросов и оповещений приведена далее.

1. Зарегистрироватьповторяемую оплату.
2. запрос на проведение повторяемой оплатыПередатьс идентификатором записи о серии списаний.
3. оповещение о результате списанияПринять от платежной платформы.

## Формат запроса

*платежных карт*Формат запросов в этом разделе представлен для инициирования повторяемых оплат со списаниями по запросу с использованием.При формировании запросов необходимо учитывать следующее:

Таким образом, корректный запрос должен содержать идентификаторы проекта и платежа, подпись, IP-адрес пользователя, валюту и сумму проведения платежа, а также идентификатор записи о серии списаний или данные платежной карты и указатель категории повторяемой оплаты.

В зависимости от особенностей провайдеров, участвующих в проведении платежа, набор обязательных параметров может варьироваться. Подробную информацию о требованиях провайдеров можно уточнить у курирующего менеджера Some Platform.

1. Должен использоваться POST-запрос к конечной точке request-payment-card-recurring или к конечной точке request-payment-card-sale. Во втором случае платежные данные пользователя должны храниться на стороне веб-сервиса и категория оплаты должна соответствовать автооплате или регулярной оплате.
2. `general``project_id`— идентификатор проекта, полученный от Some Platform при интеграции;`payment_id`— идентификатор платежа, уникальный в рамках проекта;`signature`ru_pp_authentication.ditaru_gate_authentication.ditaru_signature_intro.dita— подпись запроса, составленная после указания целевых параметров (подробнее — в разделе).— объект, содержащий основные идентификационные сведения запроса:`customer``ip_address`— используемый IP-адрес;`id`— идентификатор пользователя в рамках проекта мерчанта;— объект, содержащий сведения о пользователе:`payment``amount`*за исключениемосновных единицах валюты*ru_currency_codes.dita— сумма платежа в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.;`currency`— валюта платежа в формате ISO-4217 alpha-3.— объект, содержащий сведения о платеже:В запросе должны использоваться следующие объекты и параметры:
3. `idrecurring`В запросах к конечной точке request-payment-card-recurring — идентификатор записи о серии списаний, полученный в оповещении с данными о регистрации, в параметреобъекта.`card``pan`— номер карты;`year`— год окончания срока действия карты;`month`— месяц окончания срока действия карты;`card_holder`— имя держателя карты (в соответствии с указанным на карте);`stored_card_type46`— указатель категории, к которой относится повторяемая оплата (для автооплаты идля регулярной оплаты).В запросах к конечной точке request-payment-card-sale — объектсо следующими параметрами:В зависимости от конечной точки, к которой направляется запрос, в запросе также должны использоваться следующие объекты и параметры:
4. Дополнительно могут использоваться любые другие параметры, указанные в спецификации.

## Формат оповещений

ru_gate_callbacks.ditaПлатежная платформа передает в веб-сервис оповещение с данными о регистрации повторяемой оплаты. Для этого оповещения используется стандартный формат, описание которого представлено в разделе.

`424,00 USD№ 424242******4243`В данном случае оповещение свидетельствует о том, что в рамках проектабыло выполнено списание в размерес платежной карты.
