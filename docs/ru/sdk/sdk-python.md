# sdk_python

sdk_python — это набор средств разработки для взаимодействия веб-сервисов, разработанных на Python, с платежной платформой Some Platform при проведении оплат через Payment Widget. В этом разделе представлена информация о работе с sdk_python с примерами кода на языке программирования Python.

sdk_python совместим с Python версии 3.5 или выше и доступен для загрузки по следующим ссылкам:

- GitHub:,
- PyPI:.

## Возможности

подписывать набор параметров платежа и формировать запрос для вызова Payment Widget,проверять подлинность оповещений от Some Platform и получать из них информацию о платежах.sdk_python позволяет:

## Состав

sdk_python содержит библиотеку для разработки и автоматизированного тестирования, а также служебные файлы.

## Порядок работы

Для использования sdk_python необходимо:

При возникновении вопросов о работе с sdk_python следует обращаться в службу технической поддержки Some Platform по адресу support@example.com

1. Если у компании нет идентификатора проекта и ключа для взаимодействия с Some Platform — отправить заявку на подключение.Если у компании идентификатор и ключ для взаимодействия с Some Platform есть — сообщить специалистам технической поддержки о намерении интеграции с использованием sdk_python и согласовать с ними порядок тестирования.Решить организационные вопросы, касающиеся взаимодействия с Some Platform:
2. Установить библиотеки, входящие в состав sdk_python, и подключить их в коде, а также доработать код для использования необходимой функциональности.
3. Для тестирования следует использовать тестовый идентификатор проекта, тестовые значения параметров платежа.project_idДля перевода в рабочий режим необходимо заменить тестовое значениена рабочее, полученное от Some Platform.Протестировать и запустить в работу обновленный исходный код веб-сервиса.

## Установка и подключение библиотек

Устанавливать библиотеки, входящие в состав sdk_python, в проект с исходным кодом веб-сервиса можно вручную или автоматически с помощью системы управления пакетами pip, который обеспечивает загрузку необходимых библиотек из репозитория.

Чтобы установить библиотеки с помощью pip и подключить их в исходном коде веб-сервиса, необходимо:

1. [https://pip.pypa.io/en/stable/](https://pip.pypa.io/en/stable/)Если не установлен pip — загрузить, установить и настроить (подробнее).
2. pip install sdk_python_libВ командной строке операционной системы в каталоге с исходным кодом веб-сервиса и выполнить следующую команду:
3. from payment_page_sdk.gate import Gate
from payment_page_sdk.payment import PaymentПодключить модули в исходном коде веб-сервиса:

## Вызов платежной формы

Запрос для вызова Payment Widget формируется из набора параметров, подписываемых для обеспечения защиты данных при передаче запроса в платежную платформу Some Platform. sdk_python позволяет автоматически подписывать параметры и формировать из них запрос. Для вызова Payment Widget с использованием sdk_python следует:

Далее приведен пример формирования запроса для вызова платежной формы Payment Widget с открытием на английском языке. На странице с выбором платежных методов обеспечивается отображение информации о платеже: идентификатора, валюты, суммы и описания платежа.

1. `Payment`payment = Payment('186', '1555943554067')
    // Идентификатор проекта и идентификатор платежа, уникальный в рамках проекта
payment.payment_amount = 1001
    // Сумма в дробных единицах валюты
payment.payment_currency = 'RUB'  
    // Код валюты в формате ISO-4217 alpha-3
payment.payment_description = 'Тестовый платеж'
    // Описание платежа. Необязательный параметрru_pp_parameters.ditaВсе параметры в данном примере, за исключением описания платежа, являются необходимыми для любой оплаты. Дополнительно можно использовать любые другие параметры из числа доступных для работы с Payment Widget. Подробнее о доступных параметрах — в разделе.Создать объект классаи указать значения параметров платежа.
2. `Gate`*secret_key*gate = Gate('<>')
    // Секретный ключ проекта, полученный при интеграции от Some PlatformСоздать объект классаи указать значение секретного ключа, полученное от Some Platform. Секретный ключ необходим для автоматического подписывания параметров.
3. *pp_host*payment_url = gate.get_purchase_payment_page_url('<>', payment)domen_PP/payment?signature=OEKRlLXKStyo
...%3D%3D&amp;payment_id=1555943554067...123*pp_host*Здесь вместо <> надо указать доменное имя Payment Widget, которое следует узнавать у своего курирующего менеджера или в службе поддержки.Корректный запрос для вызова платежной формы содержит подпись и параметры платежа:Сформировать запрос для вызова платежной формы.
4. Использовать сформированный запрос для вызова платежной формы.

## Обработка оповещений

**HTTPPOSTJSONJSON**Информацию о результатах проведения платежей можно получать в оповещениях, отправляемых со стороны Some Platform на URL, который необходимо сообщить службе технической поддержки Some Platform. Оповещение представляет собойзапрос с данными в формате-строки. Чтобы извлечь информацию о результате проведения платежа из-строки, необходимо:

Далее приведен пример данных из оповещения, которое включает в себя подпись и информацию о результатах проведения платежа. При использовании sdk_python проверка подписи в оповещении выполняется автоматически.

1. `Gate`*secret_key*gate = Gate('<>')Если ранее, при формировании запроса для вызова Payment Widget, не был создан объект класса, создать объект и указать значение секретного ключа, полученного от Some Platform.
2. `Callback`**JSON**callback = gate.handle_callback(data)Создать объект класса, используя-строку с информацией о платеже из оповещения от Some Platform:
3. callback.get_payment_id()   // Получение идентификатора платежа
callback.get_payment_status()         // Получение текущего статуса платежа
callback.get_payment()                // Получение всей информации о платежеИспользовать методы, доступные для работы с оповещениями. Можно получить всю информацию о платеже или информацию только об отдельных параметрах платежа:

## Дополнительные материалы

ru_pp_callbacks.ditaru_pp_payment_model.ditaДля организации работы с оповещениями также могут быть полезны следующие материалы:
