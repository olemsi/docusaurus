import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Multiple styles setup","description":"","frontmatter":{},"headers":[],"relativePath":"payment-page/pp-design-multiple-setup.md","filePath":"payment-page/pp-design-multiple-setup.md"}');
const _sfc_main = { name: "payment-page/pp-design-multiple-setup.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="multiple-styles-setup" tabindex="-1">Multiple styles setup <a class="header-anchor" href="#multiple-styles-setup" aria-label="Permalink to &quot;Multiple styles setup&quot;">​</a></h1><p><em>terminalsinterface modelsstyles</em>When you need to set up and use different design styles of the payment form for different scenarios within one or several projects, you can use the capabilities of configuring,, and. To do so effectively, you need to understand what the Payment Widget design involves in case of different generations and what you can do with its separate elements.</p><p>Each version of the Payment Widget design is based on the combination of an interface model and a style. The interface model specifies the composition, size, placement, and colours of various elements while the style can additionally determine the look of certain images and colours used on the form. Only one interface model and one style can be applied to the opening of the payment form at a time.</p><p><code>terminal_idstyle_id</code>If simultaneous support of different design styles is needed for different situations, you can configure each version and specify it when invoking the payment form. If you are working with the 4th generation Payment Widget, you can use terminals (and their identifiers—) for this purpose, and with the 5th generation—styles (and their identifiers—). Note that one terminal can be used for different projects while each style can be used for only one project in which it was created.</p><p>Overall, to configure multiple styles of the Payment Widget design, the merchant can do the following:</p><ul><li>*Arrange and use the combination of projects and terminals that the merchant needs.*All actions to set up this combination in the payment platform are carried out by the Platform specialists in accordance with the merchant&#39;s requirements.</li><li>*Coordinate the use of the necessary interface models for specific projects and, if relevant, terminals.*As a rule, the Payment Widget design is based on the most recent version of the basic interface model developed by Platform (which is used as a standard design style). At the same time, it is possible to coordinate with the Platform account manager the use of other interface models for specific cases.</li><li>*Create, set up, and apply required design styles to specific projects and, if relevant, terminals.*These capabilities are fully available when the merchant works with the most recent version of the basic interface model developed by Platform and can be partially or fully unavailable when the merchant works with other interface models. Working with design styles is described in the section that follows.</li></ul><p><em>terminalsinterface modelsstyles</em>When you need to set up and use different design styles of the payment form for different scenarios within one or several projects, you can use the capabilities of configuring,, and. To do so effectively, you need to understand what the Payment Widget design involves and what you can do with its separate elements.</p><p>Each version of the Payment Widget design is based on the combination of an interface model and a style. The interface model specifies the composition, size, placement, and colours of various elements while the style can additionally determine the look of certain images and colours used on the form. Only one interface model and one style can be applied to the opening of the payment form at a time. If simultaneous support of different design styles is needed for different situations, the corresponding interface models and styles should be applied to the required number of projects and terminals within separate projects.</p><p>Overall, to configure multiple styles of the Payment Widget design, the merchant can do the following:</p><ul><li><em>Arrange and use the combination of projects and terminals that the merchant needs.</em><code>project_idterminal_id</code>All actions to set up this combination in the payment platform are carried out by the Platform specialists in accordance with the merchant&#39;s requirements. To use the project and the terminal that the merchant needs in specific situations, the merchant has to provide the IDs of those in the requests to open Payment Widget (in theandparameters respectively).</li><li>*Coordinate the use of the necessary interface models for specific projects and terminals.*As a rule, the Payment Widget design is based on the most recent version of the basic interface model developed by Platform. At the same time, it is possible to coordinate with the Platform account manager the use of other interface models for specific projects and terminals.</li><li>*Create, set up, and apply required design styles to specific projects and terminals.*These capabilities are fully available when the merchant works with the most recent version of the basic interface model developed by Platform and can be partially or fully unavailable when the merchant works with other interface models. Working with design styles is described in the following section.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("payment-page/pp-design-multiple-setup.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ppDesignMultipleSetup = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ppDesignMultipleSetup as default
};
