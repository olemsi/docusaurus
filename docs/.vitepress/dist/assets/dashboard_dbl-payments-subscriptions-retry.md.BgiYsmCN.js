import { _ as _export_sfc, o as openBlock, c as createElementBlock, a2 as createStaticVNode } from "./chunks/framework.BFJzPV4p.js";
const __pageData = JSON.parse('{"title":"Managing debiting retry attempts","description":"","frontmatter":{},"headers":[],"relativePath":"dashboard/dbl-payments-subscriptions-retry.md","filePath":"dashboard/dbl-payments-subscriptions-retry.md"}');
const _sfc_main = { name: "dashboard/dbl-payments-subscriptions-retry.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="managing-debiting-retry-attempts" tabindex="-1">Managing debiting retry attempts <a class="header-anchor" href="#managing-debiting-retry-attempts" aria-label="Permalink to &quot;Managing debiting retry attempts&quot;">​</a></h1><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>One attempt is usually enough to perform a scheduled debit operation executed as part of a COF purchase. However, in some cases, for example, when the customer&#39;s account has insufficient balance, it can be useful to retry debiting after a certain amount of time. The Platform payment platform has the capability to retry COF purchase debits automatically (learn more).</p><p>configure a custom schedule for debiting retries for each project (which will be applied to every regular COF purchase within this project) as well asThis capability is set up upon agreement with your Platform account manager, and when it is supported for the project, you canmanage and cancel debiting retries in the Dashboard interface.</p><p><strong>Show retriesBack to main operation</strong>If the capability of debiting retries is set up for the project, the information about retry attempts is shown in subscription information tabs of Dashboard. These tabs allow you to switch between two viewing modes. You can view the information about retries by clicking thebutton. To return to viewing the information about the initial debit operation, click.</p><p>Depending on the viewing mode, the panels contain the following information.</p><p>The following statuses are assigned to the retries attempted as part of a scheduled debit operation:</p><ul><li><code>retries active</code>—the debiting retry has been scheduled in the payment platform (when the scheduled debit operation was declined by the payment system or the issuer).</li><li><code>retry in progress</code>—the retry attempt is being performed.</li><li><code>retries successful</code>—one of the attempted retries resulted in debiting the funds from the customer to the merchant.</li><li><code>retries unsuccessful</code>—all available retries have been declined.</li><li><code>retries cancelled</code>—retry attempts have been cancelled (either by request from the merchant or automatically, due to modification of the COF purchase debiting terms or other reasons).</li></ul><table tabindex="0"><thead><tr><th>Panel</th><th>Working with debit operations</th><th>Working with debiting retries</th></tr></thead><tbody><tr><td>1. Panel with the list of all actions</td><td>contains the list of debit operations and refunds for a specific regular COF purchase (each list item corresponds to an individual debit operation, shows its status and can show the status of debiting retries)</td><td><code>retry operation</code>contains the list of retry attempts for a specific debit operation (each list item corresponds to an individual retry attempt, shows its status, and is marked with theattribute)</td></tr><tr><td>2. Panel for performing a specific action.</td><td>, to customise the retry attempts settings, and to cancel the retry attemptscontains the information about a specific debit operation (including an explanatory note to the status of the debiting retries) and buttons to switch to the list of debiting retries</td><td><code>retry operation</code>contains the information about a specific retry (including theattribute) and the button to return to the list of all debit operations executed as part of a COF purchase</td></tr><tr><td>3. Panel with information about a specific action</td><td>contains detailed information about a specific retry</td><td>contains detailed information about a specific retry</td></tr></tbody></table><h2 id="configuring-the-retry-schedule" tabindex="-1">Configuring the retry schedule <a class="header-anchor" href="#configuring-the-retry-schedule" aria-label="Permalink to &quot;Configuring the retry schedule&quot;">​</a></h2><p>The payment platform allows you to implement one schedule of debiting retries which will apply to all regular COF purchases within one project. It can be a default schedule from Platform or a custom schedule that merchants can set up on their own depending on the specific project.</p><p>The schedule implemented for a project can be customised by modifying its specific parameters or by resetting them to default. Keep in mind that a retry attempt for this project that was scheduled before the retry schedule was customised is performed according to the initially set time and date (according to the previous schedule). However, if any of these attempts is declined after the schedule was modified and it is possible to perform the next attempt of this debiting retry in the payment platform, then the new attempt is scheduled and performed according to the updated schedule. In either case, the information about each subsequent retry attempt is sent to the web service in the callback notifying that a debiting attempt was declined (details).</p><p>To modify the retry attempts schedule for the specific project:</p><ol><li><strong>ProjectsYour projectsSubscription retry settingsSubscriptions settingsConfigure retries settings</strong>To do so, navigate to thesection, select in thedropdown menu the project you need and open thetab. (You can also get to this tab directly from theby clicking. Make sure to check that the project selected in the dropdown menu is the one you need.)Open in the tab of the project you need the tab that allows configuring the subscription retry settings.</li><li><strong>Default subscription retry schedule</strong>To use the default schedule, select.<strong>Custom subscription retry schedule</strong>To use the custom schedule, selectand specify the days on which retries will be performed for each declined debit operation by clicking the buttons with the ordinal numerals of the days you need. You can schedule one to ten retry attempts with the minimum delay of 24 hours between them.Select the retry schedule you need.</li><li><strong>Save****Save</strong>Keep in mind that the changes will be applied when you click thebutton. You will not be asked to confirm the action additionally.Save changes by clicking.</li><li>To do so, open the tab with the settings of the project you need and check the schedule implemented for this project.Make sure that the new schedule has been saved.</li></ol><h2 id="monitoring-retry-attempts" tabindex="-1">Monitoring retry attempts <a class="header-anchor" href="#monitoring-retry-attempts" aria-label="Permalink to &quot;Monitoring retry attempts&quot;">​</a></h2><p>detailsDashboard allows users to monitor processing of debiting retries. This information can be accessed via subscription tabs that contain information about all debit operations performed as part of a regular COF purchase and retries of individual debitings ().</p><p>To view information about retry attempts for a specific debit operation:</p><ol><li><strong>SubscriptionsPayments</strong>You can use the Search function (search by payment identifier, for example) or go toorand use the registry and filters.Open the payment information tab of the subscription you need.</li><li>Select the item on the panel with the list of debit operations on the left of the subscription tab.Go to the detailed information about the specific debit operation.</li><li><strong>Show retries</strong>Clickon the panel for managing the debit operation.Open the list of the debiting retries.</li><li>Select the item on the panel with the list of debiting retries on the left of the subscription tab.Go to the detailed information about the specific retry.</li><li><strong>Back to main operation</strong>Clickon the panel for managing the debiting retry or clickin the top right corner of the panel with the list of retries.If necessary, return to the list of all debit operations.</li></ol><h2 id="cancelling-subsequent-retry-attempts" tabindex="-1">Cancelling subsequent retry attempts <a class="header-anchor" href="#cancelling-subsequent-retry-attempts" aria-label="Permalink to &quot;Cancelling subsequent retry attempts&quot;">​</a></h2><p>To cancel retrying a specific debit operation:</p><ol><li><strong>SubscriptionsPayments</strong>You can use the Search function (search by payment identifier for example) or go toorand use the registry and filters.Open the payment information tab of the subscription you need.</li><li>Select the item on the panel with the list of debit operations on the left of the subscription tab.Go to the detailed information about the specific debit operation.</li><li><strong>Cancel retries</strong>Clickon the panel for managing the debit operation and confirm cancellation.Cancel debiting retries.</li><li><code>activeretries cancelled</code>Check the status of the retries in the row of the corresponding debit operation on the panel with the list of all debit operations: it should change fromto.Make sure that the subsequent retries have been cancelled.</li></ol>', 21)
  ])]);
}
const dblPaymentsSubscriptionsRetry = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  dblPaymentsSubscriptionsRetry as default
};
