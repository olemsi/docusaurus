# Single styleStylesetup

## Working with the 4th generation payment form

singleTo configure astyle on the basis of the standard Payment Widget design for a specific project, you should:

Once the changes have been applied, the new design style will be used for all Payment Widget sessions of the target project. You can further modify or switch it to a different one using these instructions.

1. **Projects**In Dashboard, open thesection, select the project you need, and switch to thetab.Open the designer.
2. **Default StyleContinue**To add a style, clickon the right of the field that says, name the new style in the dialogue box that opens, and click.Create a design style.
3. **Payment Step**Use the tools located under thedrop-down list on the tools panel of the designer.Designer interface: 1—tools panel; 2—form preview areaConfigure design settings.
4. **Payment methodPayment StepRedirectPopupiFrameMobile**For this, emulate the Payment Widget behaviour in different situations: use theanddrop-down lists to select various payment methods and pages of the payment form and switch between different Payment Widget display modes with the corresponding buttons (,,, and).Preview the design of the payment form.
5. To achieve this:**Continue**Clickat the bottom of the tools panel.**Save OnlySave &amp; Set Active**Click(if you only need to save new style settings) or(if you also need to apply the new style settings) in the dialogue box that opens.Make sure you can see the notification that informs you that the design style has been saved (and applied to the selected project, if relevant) for this project.Apply design style settings.

## Working with the 5th generation payment form

To configure a single style on the basis of the standard Payment Widget design for a specific project, you should:

Once the changes have been applied, the new design style will be used for all Payment Widget sessions of the target project. You can further modify or switch it to a different one using these instructions.

1. **Projects**In Dashboard, open thesection, select the project you need, and switch to thetab.Open the designer.
2. **Create**To add a style, clickin the top left corner, name the new style in the dialogue box that opens, and click.Create a design style.
3. Use the tools located on the tools panel on the left side of the designer.Designer interface: 1—tools panel; 2—form preview areaConfigure design settings.
4. **Preview layoutRedirectPopupiFrameMobile**For this, emulate the Payment Widget behaviour in different situations: use thedrop-down list to select pages of the payment form and switch between different Payment Widget display modes with the corresponding buttons (,,, and).Preview the design of the payment form.
5. To achieve this:**Save styleSave and apply**Clickat the top of the tools panel (if you only need to save new style settings) orat the bottom of the tools panel (if you also need to apply the new style settings).Make sure you can see the notification that informs you that the design style has been saved (and applied, if relevant) for this project.Save or apply design style settings.

singleTo configure astyle of the Payment Widget design for one project, you should:

Once the changes have been applied, the new design style will be used for all Payment Widget sessions of the target project. You can further modify or switch it to a different one using these instructions.

1. **Projects**In Dashboard, open thesection, select the project you need, and switch to thetab.Open the designer.
2. **Default StyleContinue**To add a style, clickon the right of the field that says, name the new style in the dialogue box that opens, and click.Create a design style.
3. **Payment Step**Keep in mind that if you customise colours for the payment button, the customisation will apply when the button is enabled (which is the case when all required fields in the payment form are filled correctly).Use the tools located under thedrop-down list on the tools panel of the designer.Designer interface: 1—tools panel; 2—form preview areaConfigure design settings.
4. **Payment method**andlistlistspayment methods and**Payment StepRedirectPopupiFrameMobile**For this, emulate the Payment Widget behaviour in different situations: use thedrop-downto select variouspages of the payment form and switch between different Payment Widget display modes with the corresponding buttons (,,, and).Preview the design of the payment form.
5. To achieve this:**Continue**Clickat the bottom of the tools panel.**Save &amp; Set Active**Clickin the dialogue box that opens.**Save OnlySave &amp; Set Active**Click(if you only need to save new style settings) or(if you also need to apply the new style settings) in the dialogue box that opens.Make sure you can see the notification that informs you that the design style has been saved and applied to the selected project.Apply design style settings.
