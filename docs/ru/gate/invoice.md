# Оплата по платежной ссылке

ru_gate_invoice_model.ditaВ этом разделе представлена информация о проведении оплаты по платежным ссылкам, дополняющая сведения в разделе.

## Схема и статусы оплаты по платежной ссылке

### Общая информация

*Оплата по платежной ссылке*— это тип платежа, в рамках которого на основании одного исходного запроса осуществляются создание и отправка платежной ссылки, а затем один (разовый) перевод денежных средств от пользователя к мерчанту, с предварительной блокировкой средств или без таковой.

Этот вариант может быть актуален, когда необходимо предоставлять пользователям возможность оплаты заказов без привязки к определенным месту и времени. Платежные ссылки можно отправлять любым удобным способом: средствами Some Platform на электронную почту пользователя или самостоятельно другими способами, например в социальных сетях.

### Схема проведения

`invoice/createinvoice`Стандартные вид и содержание письма, отправляемого на электронную почту, представлены в примере далее.Чтобы инициировать создание платежной ссылки, следует отправить в платформу запрос. Для выполнения такого запроса формируется операция, результатом которой является создание платежной ссылки и ее отправка в оповещении к веб-сервису, а также, при необходимости, в письме на электронную почту пользователя.

`invoice/cancel`При необходимости после отправки платежной ссылки, но до подтверждения пользователем проведения платежа, действие платежной ссылки можно отменить. Для этого необходимо отправить в платежную платформу запрос категории.

`card_operation_type`Пользователю после перехода по платежной ссылке отображается платежная форма Payment Widget, в которой он указывает свои платежные данные и подтверждает проведение оплаты. Далее, в зависимости от значения параметра, переданного в запросе, оплата проводится в соответствии с одним из следущих вариантов:

вспомогательных процедурДля любого из вариантов оплаты по платежной ссылке в процессе проведения платежа могут выполняться одна или несколько, однако дополнительных действий со стороны веб-сервиса при этом не требуется — все действия выполняются на стороне Payment Widget.

*возврат средств*`refund`После проведения оплаты по платежной ссылке можно выполнитьпользователю, если для использованного платежного метода поддерживается проведение возвратов. Чтобы инициировать возврат, следует отправить в платформу запрос категории. Для выполнения такого запроса в платформе используется одна из следующих операций:

ru_gate_invoice.ditaДалее в рамках данного раздела представлена информация о возможных статусах оплаты по платежной ссылке и связанных с ней операций. Более подробную информацию о проведении оплаты по платежной ссылке можно найти в разделе.

### Статусы платежа

При проведении оплаты по платежной ссылке могут использоваться следующие статусы.

| `error` | Проведение платежа не инициировано из-за ошибки, возникшей при проверке принятого запроса | *Конечное состояние. Допускается повторная отправка запроса с тем же идентификатором платежа и повторная попытка проведения этого платежа* |
| --- | --- | --- |
| `awaiting payment` | Проведение платежа инициировано, ожидается отправка платежной ссылки | *Промежуточное состояние* |
| `expired` | Платеж не проведен из-за истечения срока действия платежной ссылки | *Конечное состояние* |
| `invoice canceled` | Проведение платежа отменено по инициативе мерчанта | *Конечное состояние* |
| `invoice sent` | Проведение платежа инициировано, платежная ссылка отправлена | *Промежуточное состояние* |
| `processing` | Платеж проводится | *Промежуточное состояние* |
| `awaiting 3ds result` | `3ds_resultdecline`Проведение платежа приостановлено до получения запроса на продолжение с учетом результата аутентификации 3ds —. Если такой запрос не получен в течение 30 минут, то платеж переводится в статус | *Промежуточное состояние* |
| `awaiting redirect result` | `successdecline`Проведение платежа приостановлено до получения уведомления с результатом со стороны платежной системы. В зависимости от результата на стороне платежной системы платеж переводится в статусили | *Промежуточное состояние* |
| `awaiting clarification` | `clarificationdecline`Проведение платежа приостановлено до получения запроса на продолжение с учетом требуемой дополнительной информации —. Если такой запрос не получен в течение 30 минут, платеж переводится в статус | *Промежуточное состояние* |
| `awaiting customer` | `successdecline`ru_pp_try_again.ditaПроведение платежа приостановлено до получения результата повторных попыток со стороны пользователя. При успешной повторной попытке платеж переводится в статус, а при истечении числа безуспешных попыток — в статус(подробнее — в разделе) | *Промежуточное состояние* |
| `awaiting capture` | `capturecancel`Проведение платежа приостановлено до получения запроса на списание () или на отмену предварительной блокировки средств () | *Промежуточное состояние* |
| `canceled` | `auth`Предварительная блокировка средств, выполненная по запросу, отменена | *Конечное состояние* |
| `decline` | Платеж отклонен | *Конечное состояние* |
| `success` | Платеж проведен | *Конечное состояние. Дополнительно допускается проведение возврата* |
| `partially reversed` | Сумма платежа частично возвращена до закрытия бизнес-дня, в котором он был проведен | *Конечное состояние* |
| `reversed` | Сумма платежа полностью возвращена до закрытия бизнес-дня, в котором он был проведен | *Конечное состояние. Дополнительно допускается отмена возврата* |
| `partially refunded` | Сумма платежа частично возвращена | *Конечное состояние. Дополнительно допускается отмена возврата* |
| `refunded` | Сумма платежа полностью возвращена после закрытия бизнес-дня, в котором он был проведен. Осуществлен один полный возврат суммы платежа или несколько частичных, в совокупности составляющих исходную сумму | *Конечное состояние. Дополнительно допускается отмена возврата* |

### Статусы операции invoice

`invoice`При выполнении операциимогут использоваться следующие статусы.

| `awaiting payment` | Выполнение операции инициировано, ожидается отправка платежной ссылки | *Промежуточное состояние* |
| --- | --- | --- |
| `expired` | Операция выполнена, срок действия платежной ссылки истек | *Конечное состояние* |
| `invoice canceled` | Выполнение операции отменено по инициативе мерчанта | *Конечное состояние* |
| `invoice sent` | Операция выполнена, платежная ссылка отправлена | *Конечное состояние* |

### Статусы операций sale и auth

`saleauth`При выполнении одной из операций,или, могут использоваться следующие статусы.

| `processing` | Операция выполняется | *Промежуточное состояние* |
| --- | --- | --- |
| `awaiting 3ds result` | `3ds_resultdecline`Выполнение операции приостановлено до получения запроса на продолжение с учетом результата аутентификации 3ds —. Если такой запрос не получен в течение 30 минут, операция переводится в статус | *Промежуточное состояние* |
| `awaiting redirect result` | `successdecline`Выполнение операции приостановлено до получения уведомления с результатом от платежной системы. В зависимости от результата операция переводится в статусили статус | *Промежуточное состояние* |
| `awaiting clarification` | `clarificationdecline`Выполнение операции приостановлено до получения запроса на продолжение с учетом требуемой дополнительной информации —. Если такой запрос не получен в течение 30 минут, операция переводится в статус | *Промежуточное состояние* |
| `decline` | Операция отклонена | *Конечное состояние* |
| `success` | Операция выполнена | *Конечное состояние* |

### Статусы операций capture и cancel

`capturecancel`При выполненииимогут использоваться следующие статусы.

| `processing` | Операция выполняется | *Промежуточное состояние* |
| --- | --- | --- |
| `awaiting clarification` | `clarificationdecline`Выполнение операции приостановлено до получения запроса на продолжение с учетом требуемой дополнительной информации —. Если такой запрос не получен в течение 30 минут, операция переводится в статус | *Промежуточное состояние* |
| `decline` | Операция отклонена | *Конечное состояние* |
| `success` | Операция выполнена | *Конечное состояние* |

### Статусы операций reversal и refund

`reversalrefundcapturecancel`Статусы операцийисовпадают со статусами операцийи.

### Дополнительные материалы

При работе с оплатами по счету могут быть полезны следующие материалы:

- ru_gate_interaction_organisation.dita— раздел с общей информацией о взаимодействии с платежной платформой через Gate.
- ru_gate_invoice.dita— раздел с подробной информацией о проведении оплат по платежной ссылке.
- ru_gate_callbacks.dita— раздел с информацией об оповещениях и работе с ними.
- ru_gate_statuses_and_response_codes.dita— раздел с информацией о кодах ошибок, используемых в платежной платформе.

---

## Общая информация

*варианты*Оплата по платежной ссылке — это тип платежа, в рамках которого на основании одного исходного запроса осуществляется создание и отправка платежной ссылки, а затем один (разовый) перевод денежных средств от пользователя к мерчанту. Платежная платформа Some Platform поддерживает следующиетаких оплат:

Также в параметрах такого запроса можно указать платежный метод, с использованием которого необходимо провести платеж, или предоставить пользователю возможность выбора из всех методов, подключенных для проекта мерчанта.Оба варианта проводятся с использованием платежной формы Payment Widget, ссылка на которую отправляется на электронную почту пользователя средствами Some Platform или любым другим способом средствами мерчанта. Срок действия такой ссылки и вариант проведения платежа определяются на стороне мерчанта и указываются в параметрах запроса на его инициирование.

Так как в оплате по платежной ссылке используется платежная форма Payment Widget, в параметрах запросов на инициирование такой оплаты реквизиты платежного инструмента либо вообще не указываются, либо указываются в форме токена карты. В последнем случае пользователю понадобится только подтвердить подлинность платежного инструмента, без указания его реквизитов в платежной форме.

Как и при проведении других типов платежей, в процессе проведения оплаты по платежной ссылке может потребоваться выполнить вспомогательные процедуры, такие как аутентификация с использованием технологии 3ds, дополнение информации о платеже и конвертация валют. Эти процедуры полностью выполняются на стороне Payment Widget, дополнительных действий со стороны веб-сервиса мерчанта при этом не требуется.

- оплата по платежной ссылке в одну стадию,
- оплата по платежной ссылке в две стадии.

---

## Структура запроса

### Формат запроса на проведение платежа

При формировании запросов необходимо учитывать следующее:

Таким образом, корректный запрос на оплату по платежной ссылке должен содержать идентификаторы проекта и платежа, подпись, идентификатор пользователя, валюту и сумму платежа, срок действия платежной ссылки и, при необходимости, токен платежной карты. Дополнительно, если платежную ссылку необходимо отправить средствами Some Platform, запрос должен содержать адрес электронной почты пользователя и индикатор автоматической отправки платежной ссылки.

1. Должен использоваться POST-запрос к одной из следующих конечных точек: request-payment-invoice-create или request-payment-invoice-card-token-create.
2. `general``project_id`— идентификатор проекта, полученный от Some Platform при интеграции;`payment_id`— идентификатор платежа, уникальный в рамках проекта;`signature`ru_gate_authentication.dita— подпись запроса, составленная после указания целевых параметров (подробнее — в разделе);— объект, содержащий основные идентификационные сведения запроса:`customer``id`— идентификатор пользователя в веб-сервисе мерчанта;— объект, содержащий сведения о пользователе:`payment``amount`*за исключениемосновных единицах валюты*ru_currency_codes.dita— сумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.;`currency`— валюта платежа в формате ISO-4217 alpha-3;`best_beforeYYYY-MM-DDThh:mm:ss±hh:mm`— срок действия платежной ссылки в формате.— объект, содержащий сведения о платеже:В запросе должны использоваться следующие объекты и параметры:
3. `token`При передаче токена — токен карты в параметре.
4. `emailcustomer`— адрес электронной почты пользователя, содержится в объекте;`send_emailtrue`— индикатор автоматической отправки платежной ссылки, для отправки необходимо передать значение;`languagecustomer`— код языка, содержится в объекте; необходимо передавать в случае, если язык пользователя отличается от английского и для этого языка настроена верстка письма.Для отправки платежной ссылки средствами Some Platform:
5. `force_method`ru_pp_force_payment_method_codes.ditaДля предварительного выбора платежного метода — код этого метода в параметре(список таких кодов — в разделе).
6. Дополнительно могут использоваться любые другие параметры, указанные в спецификации.

```json
{
  "general": {
      "project_id": 1901,
      "payment_id": "456789",
      "signature": "v7KNMpfogAxwRIL9tVftZ1ZZ5D/aZAeb0VMdeR+CqGrNxYyilUwSm=="
  },
    "customer": {
    "id": "JOHN DOE",
    "email": "johndoe@example.com"
  }
    "payment": {
      "amount": 1500,
      "currency": "GBP",
      "best_before": "2020-10-11T11:50:00+00:00"
  },
    "send_email": true,
// при передаче ранее созданного токена платежной карты:
    "token": "f365bb1729f9b72fd9c097becc679f29c3e35c91d18070d15654"  
}
```

### Формат запроса на отмену платежа

При формировании запросов необходимо учитывать следующее:

Таким образом, корректный запрос на отмену платежа должен содержать идентификаторы проекта и платежа, а также подпись.

1. Должен использоваться POST-запрос к конечной точке request-payment-invoice-cancel.
2. `general``project_id`— идентификатор проекта, полученный от Some Platform при интеграции;`payment_id`— идентификатор того платежа, который необходимо отменить;`signature`ru_gate_authentication.dita— подпись запроса, составленная после указания целевых параметров (подробнее — в разделе);В запросе должен использоваться объект, содержащий основные идентификационные сведения запроса:
3. Дополнительно могут использоваться любые другие параметры, указанные в спецификации.

```json
{
  "general": {
      "project_id": 1901,
      "payment_id": "456789",
      "signature": "v7KNMpfogAxwRIL9tVftZ1ZZ5D/aZAeb0VMdeR+CqGrNxYyilUwSm=="
  }
}
```

---

## Структура оповещения

### Формат оповещения о создании платежной ссылки

ru_gate_callbacks.ditaДля оповещения об отправке платежной ссылки пользователю используется стандартный формат, описание которого представлено в разделе.

`johndoejohndoe@example.com12,99 GBP`В следующем примере содержится информация о том, что пользователюна адрес электронной почтыотправлена платежная ссылка для оплаты заказа в размере. Срок действия ссылки истекает 11 октября 2020 года, в 11:50.

### Формат оповещения об отмене платежа

ru_gate_callbacks.ditaДля оповещения об отмене платежа по инициативе мерчанта используется стандартный формат, описание которого представлено в разделе.

`456789invoice canceled`В следующем примере содержится информация о том, что платежотменен, статус платежа —.

### Формат оповещения об истечении срока действия платежной ссылки

ru_gate_callbacks.ditaДля оповещения об истечении срока действия платежной ссылки используется стандартный формат, описание которого представлено в разделе.

`johndoejohndoe@example.com`В следующем примере содержится информация о том, что срок действия платежной ссылки, отправленной пользователюна адрес электронной почты, истек 11 февраля 2020 года, в 11:50.

### Формат оповещения о результате платежа

ru_gate_callbacks.ditaДля оповещения о результате оплаты по платежной ссылке используется стандартный формат, описание которого представлено в разделе.

`johndoe12,99 GBP`В следующем примере содержится информация о том, что для пользователяпроведена оплата в размере.
