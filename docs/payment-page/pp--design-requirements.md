# Requirements

## I. General requirements

As embedded into a web pageIn a modal windowIn a separate browser tabWay to open Payment Widget (one or more):In the currentIn the separate tabWay to open ACS page of the issuing bank:Redirecting to a project web pageOn Payment WidgetWay to display payment result pages:AdaptiveFixedType of layout:Browsers and preferably their versionOperating systems and preferably their versionsDevices typesEnvironments:Languages supported by defaultOther languagesRequired interface languages:You should provide the following information on the template layout of Payment Widget.

## II. Requirements of layout scheme

1. [Zeplin](https://zeplin.io/)You should provide the template layout within aproject with imported graphic elements either PSD or sketch files.
2. You must use the sRGB colour space in PSD files.
3. In PSD files each element must have either a single layer or layers organised into a layer group and combined into the group fcprer. Names of all elements must uniquely identify them, for example, "Cardhcprer field".
4. You can use only the Normal blending mode without opacity for layered PSD files.
5. You should provide all graphic elements of the sketch files and Zeplin projects in SVG (preferably) or PNG format.
6. You should develop all required page layouts preferably in all required languages.
7. You should develop page layouts with all error messages.

## III. Requirements of interactive element

Idle – by default (how they appear when not hovered over or selected)Hovered – hovered overClick – selected (often referred to as being clicked)You must customise buttons, links and other interactive elements in three states:

## IV. Requirements of text elements

1. You should use integer font size values.
2. If you use custom fonts that are not included in the set of operating system fonts, font files must be attached to the files of the template layout.
3. If you edit default text elements, you must provide translations in required languages in a separate file or add text strings to the layout pages. For example, change "cardhcprer" to "card hcprer".
4. If you edit default error messages, you must provide error translations in required languages in a separate file or add text strings to the layout pages.
