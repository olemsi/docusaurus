# Обзор

## Схема работы

В проведении отдельного платежа с использованием title задействуются веб-сервис мерчанта, один из интерфейсов и платежная платформа Some Platform, а также технические средства title.

## Основные операции

* Проведение оплаты возможно с использованием ваучеров следущих номиналов: 10, 15, 25, 50, 75, 100, 150, 250 в любой из поддерживаемых валют. В случае если сумма платежа превышает максимально допустимое значение ваучера, то оплата происходит с помощью нескольких ваучеров. Например, если сумма равна 1250,00 EUR, то оплата произойдет двумя ваучерами с номиналом 500,00 EUR и одним с номиналом 250,00 EUR.

**ПроектыПлатежные методы**Уточнить минимальную и максимальную сумму платежа, доступную в вашем проекте, вы можете в Dashboard. Для этого в Dashboard перейдите в раздели выберите вкладку.

|  | Интерфейсы | Суммы * |  |  |  |
| --- | --- | --- | --- | --- | --- |
| Payment Widget | Gate | Dashboard | минимум | максимум |  |
| Оплата |  |  |  |  |  |

## Сценарии использования

Для оплаты с использованием метода title пользователю необходимо ввести данные ваучера (номер, код безопасности и срок действия). Проведение оплаты выполняется с передачей пользователю данных ваучера через веб-сервис мерчанта.

Детальные сведения о том, что необходимо делать со стороны мерчанта для проведения платежей, а также о том, что можно использовать для анализа информации о проведенных платежах и операциях, представлены далее.

| title — платежный метод для проведения платежей с использованием ваучеров. Оплата осуществляется через Payment Widget и Gate.здесьЗагрузить логотип этого платежного метода в векторном формате можно. |  |
| --- | --- |

| Тип платежного метода | Платежи с использованием ваучеров |
| --- | --- |
| Регионы использования | Все страны |
| Валюты платежей | references/ru/currencies/CAD.ditareferences/ru/currencies/GBP.ditareferences/ru/currencies/EUR.ditareferences/ru/currencies/USD.dita,,, |
| Конвертация валют | На стороне Some Platform |
| Оплата |  |
| Выплата |  |
| Оплата по сохраненным данным |  |
| Полный возврат |  |
| Частичный возврат |  |
| Опротестование |  |
| Особенности | Сумму платежа устанавливает пользователь на стороне сервиса title, выбирая ваучер установленного номинала, поэтому в запросе на оплату можно передавать нулевое значение |
| Валюты расчетов с мерчантами | *по умолчанию:**по запросу:* |
| Скорость расчетов с мерчантами | без задержек |
| Организация и стоимость подключения | По согласованию с курирующим менеджером Some Platform |

---

## Оплата через Gate

### Общая информация

Для оплаты через Gate с использованием метода title со стороны веб-сервиса необходимо:

Полная схема проведения оплаты представлена далее.

ru_gate_sts_protocol.ditaИнформация о формате запросов и параметрах инициации оплат методом title через Gate, а также о формате оповещений о результатах оплат приведена далее, общая информация о работе с API представлена в разделе.

1. Отправить запрос, содержащий требуемые параметры и подпись, на рабочий URL Some Platform.
2. Осуществить перенаправление пользователя в сервис title.
3. Обработать запрос на подтверждение суммы оплаты на стороне веб-сервиса.
4. Отправить ответ с подтверждением суммы оплаты.
5. Принять оповещение о результате оплаты.

### Формат запросов на инициирование оплаты

При работе с запросами на оплаты с применением метода title необходимо учитывать следующее:

Вот пример данных из запроса на оплату с использованием метода title:

1. `/v2/payment/voucher/eco-voucher/sale`Должен использоваться запрос, отправляемый методом POST. Этот запрос относится к запросам для платежей с использованием ваучеров: request-payment-voucher-sale
2. generalproject_id— идентификатор проекта, полученный от Some Platform при интеграции;payment_id— идентификатор платежа, уникальный в рамках проекта;signatureИспользование подписи к данным— подпись запроса, составленная после указания целевых параметров (подробнее — в разделе);— объект, содержащий основные сведения о запросе:customerid— идентификатор, уникальный в рамках проекта;ip_address— IP-адрес устройства пользователя;first_name— имя пользователя;last_name— фамилия пользователя;email— адрес электронной почты пользователя;— объект, содержащий сведения о пользователе:paymentamount*за исключениемосновных единицах валюты*ru_currency_codes.dita— сумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.;currency— код валюты оплаты в формате ISO-4217 alpha-3;— объект, содержащий сведения об оплате:return_urlsuccess— URL-адрес, на который нужно перенаправить пользователя в случае успешного завершения оплаты;decline— URL-адрес, на который нужно перенаправить пользователя в случае отклонения оплаты.— объект с информацией, куда следует перенаправлять пользователя при прерывании или завершении оплаты:В запросе должны присутствовать следующие объекты и параметры:
3. return_urlreturn_urlreturnsuccessdecline— URL-адрес, куда нужно перенаправить пользователя в случае прерывания оплаты пользователем до ее завершения. Этот же адрес используется, если не заданы параметрыи.— объект, содержащий URL-адрес для возвращения пользователя со стороны сервиса после оплаты:Следующие параметры с адресами возврата пользователя необязательны, но их настоятельно рекомендуется передавать в запросе, чтобы сделать процесс оплаты максимально удобным для пользователя. Если не задан ни один из параметров объекта, то Gate по умолчанию перенаправляет пользователя по URL-адресу, указанному в вашем проекте в Some Platform.
4. ru_pm_ecovoucher_overview.ditaВалютой платежа может быть любая из перечисленных в разделе.
5. Сумму платежа устанавливает пользователь на стороне сервиса title, выбирая ваучер установленного номинала, поэтому в запросе на оплату можно передавать нулевое значение
6. Проведение оплаты возможно с использованием ваучеров следущих номиналов: 10, 15, 25, 50, 75, 100, 150, 250. В случае если сумма платежа превышает максимально допустимое значение ваучера, то оплата происходит с помощью нескольких ваучеров. Например, если сумма равна 1250,00 EUR, то оплата произойдет двумя ваучерами с номиналом 500,00 EUR и одним с номиналом 250,00 EUR.
7. Дополнительно могут использоваться все параметры, указанные в спецификации.

### Форматы данных для перенаправления пользователей

После получения и обработки запроса на оплату платежная платформа направляет в ваш веб-сервис оповещение (callback) с данными для перенаправления пользователя в сервис title.

redirect_dataЧтобы перенаправить пользователя, используйте значения следующих параметров, находящихся в объекте:

Вот пример фрагмента оповещения, содержащего данные для перенаправления пользователя.

- url— адрес для перенаправления пользователя;
- body— данные для отправки в теле запроса;
- method— метод запроса HTML-страницы сайта (например, POST или GET);
- encrypted— это служебный параметр. Игнорируйте содержащиеся в нем данные.

```json
"redirect_data": {
    "method": "POST",
    "body": {
        "example_parameter_1": "value_1",
        "example_parameter_2": "value_2"
    },
    "encrypted": [],
    "url": "https://example.com/redirect"
}
```

### Формат запросов и ответов на подтверждение суммы оплаты

После получения положительного ответа на запрос на инициирование оплаты от сервиса title со стороны платежной платформы мерчанту отправляется запрос на подтверждение суммы оплаты, указанной пользователем.

`check_deposit`Запросдля подтверждения суммы оплаты отправляется методом POST на заданный URL веб-сервиса.type`check_deposit`— тип операции на стороне веб-сервиса, значение параметра всегда;customer_id— идентификатор пользователя в системе мерчанта;project_id— идентификатор проекта, полученный от Some Platform при интеграции;payment_id— идентификатор платежа, уникальный в рамках проекта;amount*за исключениемосновных единицах валюты*ru_currency_codes.dita— сумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.;currency— валюта платежа в формате ISO-4217 alpha-3;payment_method`eco-voucher`— название платежного метода, значение параметра всегда;signature— подпись запроса.Пример запроса на подтверждение суммы оплаты{
    "type": "check_deposit",
    "project_id": 258,
    "payment_id": "X03936",
    "customer_id": "123",
    "amount": 1000,
    "currency": "EUR",
    "payment_method": "eco-voucher",
    "signature": "+5O+5Qix8kbnOgIQe5JddN50ardeRdGh7Jfi7SJXULy\/+6GbR5nwToLrUXmQ=="
}Запрос содержит следующие обязательные параметры:code`0decline4xx5xxdecline`— код ответа на запрос. Для подтверждения суммы необходимо указать значение, для отклонения — любое другое значение (в этом случае платеж переводится в статуси со стороны платежной платформы отправляется оповещение об отказе в проведении оплаты). Если в ответе используется HTTP-код классаили, то платеж также переводится в статус.payment_id— идентификатор платежа, уникальный в рамках проекта.Пример ответа на запрос{
   "code":0,
   "payment_id":"X03936"
}Ответ на запрос подтверждения суммы оплаты должен быть отправлен синхронно и содержать следующие обязательные параметры:

### Формат оповещений

ru_gate_callbacks.ditaДля оповещений о результатах оплат с применением метода title используется стандартный формат, описание которого представлено в разделе.

`2381210,00 EUR`В следующем примере оповещение свидетельствует о том, что в рамках проектадля пользователябыла успешно проведена оплата в размере.

В следующем примере оплата была отклонена из-за истечения времени ожидания..

### Дополнительные материалы

Для организации работы с оплатами через Gate также могут быть полезны следующие материалы:

- ru_gate_interaction_organisation.dita
- ru_gate_authentication.dita
- ru_gate_payment_model.dita
- ru_gate_statuses_and_response_codes.dita

---

## Оплата через Payment Widget

### Общая информация

ru_pp__preselectingps.ditaДля оплаты через Payment Widget с использованием метода title со стороны веб-сервиса необходимо отправить запрос, содержащий требуемые параметры и подпись, на рабочий URL Some Platform, подтвердить сумму оплаты и принять оповещение о результате оплаты. При этом метод title можно сделать предварительно выбранным (подробнее — в разделе). Полная схема проведения оплаты представлена далее.

Описание Payment Widget APIИнформация о формате запросов и параметрах вызова Payment Widget при работе с title, а также о формате оповещений о результатах оплат приведена далее; общая информация о работе с API представлена в разделе.

### Формат запросов на инициирование оплаты

При формировании запросов на открытие платежной формы с применением метода title необходимо учитывать следующее:

Вот пример данных из запроса на открытие платежной формы с использованием метода title:

1. project_id— идентификатор проекта, полученный от Some Platform при интеграции;payment_id— идентификатор платежа, уникальный в рамках проекта;payment_currency— код валюты платежа в формате ISO-4217 alpha-3;payment_amount*за исключениемосновных единицах валюты*ru_currency_codes.dita— сумма платежа в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см..customer_id— идентификатор пользователя, уникальный в рамках проекта;customer_first_name— имя пользователя;customer_last_name— фамилия пользователя;customer_email— адрес электронной почты пользователя.Должен использоваться базовый минимум параметров — параметры, обязательные для любого платежного метода:
2. ru_pm_ecovoucher_overview.ditaВалютой платежа может быть любая из перечисленных в разделе.
3. Сумму платежа устанавливает пользователь на стороне сервиса title, выбирая ваучер установленного номинала, поэтому в запросе на оплату можно передавать нулевое значение
4. Проведение оплаты возможно с использованием ваучеров следущих номиналов: 10, 15, 25, 50, 75, 100, 150, 250. В случае если сумма платежа превышает максимально допустимое значение ваучера, то оплата происходит с помощью нескольких ваучеров. Например, если сумма равна 1250,00 EUR, то оплата произойдет двумя ваучерами с номиналом 500,00 EUR и одним с номиналом 250,00 EUR.
5. force_payment_method`force_pm`Для предварительного выбора метода title необходимо указывать код платежного метода в параметре—.
6. ru_pp_parameters.ditaДополнительно могут использоваться любые другие параметры из числа доступных для работы с Payment Widget. Детальная информация обо всех параметрах приведена в разделе.
7. ru_pp_authentication.ditaПосле определения всех параметров необходимо составить подпись. Подробную информацию см. в.

### Формат запросов и ответов на подтверждение суммы оплаты

После получения положительного ответа на запрос на инициирование оплаты от сервиса title со стороны платежной платформы мерчанту отправляется запрос на подтверждение суммы оплаты, указанной пользователем.

`check_deposit`Запросдля подтверждения суммы оплаты отправляется методом POST на заданный URL веб-сервиса.type`check_deposit`— тип операции на стороне веб-сервиса, значение параметра всегда;customer_id— идентификатор пользователя в системе мерчанта;project_id— идентификатор проекта, полученный от Some Platform при интеграции;payment_id— идентификатор платежа, уникальный в рамках проекта;amount*за исключениемосновных единицах валюты*ru_currency_codes.dita— сумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.;currency— валюта платежа в формате ISO-4217 alpha-3;payment_method`eco-voucher`— название платежного метода, значение параметра всегда;signature— подпись запроса.Пример запроса на подтверждение суммы оплаты{
    "type": "check_deposit",
    "project_id": 258,
    "payment_id": "X03936",
    "customer_id": "123",
    "amount": 1000,
    "currency": "EUR",
    "payment_method": "eco-voucher",
    "signature": "+5O+5Qix8kbnOgIQe5JddN50ardeRdGh7Jfi7SJXULy\/+6GbR5nwToLrUXmQ=="
}Запрос содержит следующие обязательные параметры:code`0decline4xx5xxdecline`— код ответа на запрос. Для подтверждения суммы необходимо указать значение, для отклонения — любое другое значение (в этом случае платеж переводится в статуси со стороны платежной платформы отправляется оповещение об отказе в проведении оплаты). Если в ответе используется HTTP-код классаили, то платеж также переводится в статус.payment_id— идентификатор платежа, уникальный в рамках проекта.Пример ответа на запрос{
   "code":0,
   "payment_id":"X03936"
}Ответ на запрос подтверждения суммы оплаты должен быть отправлен синхронно и содержать следующие обязательные параметры:

### Формат оповещений

ru_pp_callbacks.ditaДля оповещений о результатах оплат с применением метода title используется стандартный формат, описание которого представлено в разделе.

`2381210,00 EUR`В следующем примере оповещение свидетельствует о том, что в рамках проектадля пользователябыла успешно проведена оплата в размере.

В следующем примере оплата была отклонена из-за истечения времени ожидания.

### Дополнительные материалы

Для организации работы с оплатами через Payment Widget также могут быть полезны следующие материалы:

- ru_pp_interaction_organisation.dita
- ru_pp_authentication.dita
- ru_pp_payment_model.dita
- ru_pp_statuses_and_response_codes.dita

---

## Оплата через Gate

### Общая информация

Для оплаты через Gate с использованием метода title со стороны веб-сервиса необходимо:

Полная схема проведения оплаты представлена далее.

ru_gate_sts_protocol.ditaИнформация о формате запросов и параметрах инициации оплат методом title через Gate, а также о формате оповещений о результатах оплат приведена далее, общая информация о работе с API представлена в разделе.

1. Отправить запрос, содержащий требуемые параметры и подпись, на рабочий URL Some Platform.
2. Осуществить перенаправление пользователя в сервис title.
3. Обработать запрос на подтверждение суммы оплаты на стороне веб-сервиса.
4. Отправить ответ с подтверждением суммы оплаты.
5. Принять оповещение о результате оплаты.

### Формат запросов на инициирование оплаты

При работе с запросами на оплаты с применением метода title необходимо учитывать следующее:

Вот пример данных из запроса на оплату с использованием метода title:

1. `/v2/payment/voucher/eco-voucher/sale`Должен использоваться запрос, отправляемый методом POST. Этот запрос относится к запросам для платежей с использованием ваучеров: request-payment-voucher-sale
2. generalproject_id— идентификатор проекта, полученный от Some Platform при интеграции;payment_id— идентификатор платежа, уникальный в рамках проекта;signatureИспользование подписи к данным— подпись запроса, составленная после указания целевых параметров (подробнее — в разделе);— объект, содержащий основные сведения о запросе:customerid— идентификатор, уникальный в рамках проекта;ip_address— IP-адрес устройства пользователя;first_name— имя пользователя;last_name— фамилия пользователя;email— адрес электронной почты пользователя;— объект, содержащий сведения о пользователе:paymentamount*за исключениемосновных единицах валюты*ru_currency_codes.dita— сумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.;currency— код валюты оплаты в формате ISO-4217 alpha-3;— объект, содержащий сведения об оплате:return_urlsuccess— URL-адрес, на который нужно перенаправить пользователя в случае успешного завершения оплаты;decline— URL-адрес, на который нужно перенаправить пользователя в случае отклонения оплаты.— объект с информацией, куда следует перенаправлять пользователя при прерывании или завершении оплаты:В запросе должны присутствовать следующие объекты и параметры:
3. return_urlreturn_urlreturnsuccessdecline— URL-адрес, куда нужно перенаправить пользователя в случае прерывания оплаты пользователем до ее завершения. Этот же адрес используется, если не заданы параметрыи.— объект, содержащий URL-адрес для возвращения пользователя со стороны сервиса после оплаты:Следующие параметры с адресами возврата пользователя необязательны, но их настоятельно рекомендуется передавать в запросе, чтобы сделать процесс оплаты максимально удобным для пользователя. Если не задан ни один из параметров объекта, то Gate по умолчанию перенаправляет пользователя по URL-адресу, указанному в вашем проекте в Some Platform.
4. ru_pm_ecovoucher_overview.ditaВалютой платежа может быть любая из перечисленных в разделе.
5. Сумму платежа устанавливает пользователь на стороне сервиса title, выбирая ваучер установленного номинала, поэтому в запросе на оплату можно передавать нулевое значение
6. Проведение оплаты возможно с использованием ваучеров следущих номиналов: 10, 15, 25, 50, 75, 100, 150, 250. В случае если сумма платежа превышает максимально допустимое значение ваучера, то оплата происходит с помощью нескольких ваучеров. Например, если сумма равна 1250,00 EUR, то оплата произойдет двумя ваучерами с номиналом 500,00 EUR и одним с номиналом 250,00 EUR.
7. Дополнительно могут использоваться все параметры, указанные в спецификации.

### Форматы данных для перенаправления пользователей

После получения и обработки запроса на оплату платежная платформа направляет в ваш веб-сервис оповещение (callback) с данными для перенаправления пользователя в сервис title.

redirect_dataЧтобы перенаправить пользователя, используйте значения следующих параметров, находящихся в объекте:

Вот пример фрагмента оповещения, содержащего данные для перенаправления пользователя.

- url— адрес для перенаправления пользователя;
- body— данные для отправки в теле запроса;
- method— метод запроса HTML-страницы сайта (например, POST или GET);
- encrypted— это служебный параметр. Игнорируйте содержащиеся в нем данные.

```json
"redirect_data": {
    "method": "POST",
    "body": {
        "example_parameter_1": "value_1",
        "example_parameter_2": "value_2"
    },
    "encrypted": [],
    "url": "https://example.com/redirect"
}
```

### Формат запросов и ответов на подтверждение суммы оплаты

После получения положительного ответа на запрос на инициирование оплаты от сервиса title со стороны платежной платформы мерчанту отправляется запрос на подтверждение суммы оплаты, указанной пользователем.

`check_deposit`Запросдля подтверждения суммы оплаты отправляется методом POST на заданный URL веб-сервиса.type`check_deposit`— тип операции на стороне веб-сервиса, значение параметра всегда;customer_id— идентификатор пользователя в системе мерчанта;project_id— идентификатор проекта, полученный от Some Platform при интеграции;payment_id— идентификатор платежа, уникальный в рамках проекта;amount*за исключениемосновных единицах валюты*ru_currency_codes.dita— сумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.;currency— валюта платежа в формате ISO-4217 alpha-3;payment_method`eco-voucher`— название платежного метода, значение параметра всегда;signature— подпись запроса.Пример запроса на подтверждение суммы оплаты{
    "type": "check_deposit",
    "project_id": 258,
    "payment_id": "X03936",
    "customer_id": "123",
    "amount": 1000,
    "currency": "EUR",
    "payment_method": "eco-voucher",
    "signature": "+5O+5Qix8kbnOgIQe5JddN50ardeRdGh7Jfi7SJXULy\/+6GbR5nwToLrUXmQ=="
}Запрос содержит следующие обязательные параметры:code`0decline4xx5xxdecline`— код ответа на запрос. Для подтверждения суммы необходимо указать значение, для отклонения — любое другое значение (в этом случае платеж переводится в статуси со стороны платежной платформы отправляется оповещение об отказе в проведении оплаты). Если в ответе используется HTTP-код классаили, то платеж также переводится в статус.payment_id— идентификатор платежа, уникальный в рамках проекта.Пример ответа на запрос{
   "code":0,
   "payment_id":"X03936"
}Ответ на запрос подтверждения суммы оплаты должен быть отправлен синхронно и содержать следующие обязательные параметры:

### Формат оповещений

ru_gate_callbacks.ditaДля оповещений о результатах оплат с применением метода title используется стандартный формат, описание которого представлено в разделе.

`2381210,00 EUR`В следующем примере оповещение свидетельствует о том, что в рамках проектадля пользователябыла успешно проведена оплата в размере.

В следующем примере оплата была отклонена из-за истечения времени ожидания..

### Дополнительные материалы

Для организации работы с оплатами через Gate также могут быть полезны следующие материалы:

- ru_gate_interaction_organisation.dita
- ru_gate_authentication.dita
- ru_gate_payment_model.dita
- ru_gate_statuses_and_response_codes.dita

---

## Оплата через Payment Widget

### Общая информация

ru_pp__preselectingps.ditaДля оплаты через Payment Widget с использованием метода title со стороны веб-сервиса необходимо отправить запрос, содержащий требуемые параметры и подпись, на рабочий URL Some Platform, подтвердить сумму оплаты и принять оповещение о результате оплаты. При этом метод title можно сделать предварительно выбранным (подробнее — в разделе). Полная схема проведения оплаты представлена далее.

Описание Payment Widget APIИнформация о формате запросов и параметрах вызова Payment Widget при работе с title, а также о формате оповещений о результатах оплат приведена далее; общая информация о работе с API представлена в разделе.

### Формат запросов на инициирование оплаты

При формировании запросов на открытие платежной формы с применением метода title необходимо учитывать следующее:

Вот пример данных из запроса на открытие платежной формы с использованием метода title:

1. project_id— идентификатор проекта, полученный от Some Platform при интеграции;payment_id— идентификатор платежа, уникальный в рамках проекта;payment_currency— код валюты платежа в формате ISO-4217 alpha-3;payment_amount*за исключениемосновных единицах валюты*ru_currency_codes.dita— сумма платежа в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см..customer_id— идентификатор пользователя, уникальный в рамках проекта;customer_first_name— имя пользователя;customer_last_name— фамилия пользователя;customer_email— адрес электронной почты пользователя.Должен использоваться базовый минимум параметров — параметры, обязательные для любого платежного метода:
2. ru_pm_ecovoucher_overview.ditaВалютой платежа может быть любая из перечисленных в разделе.
3. Сумму платежа устанавливает пользователь на стороне сервиса title, выбирая ваучер установленного номинала, поэтому в запросе на оплату можно передавать нулевое значение
4. Проведение оплаты возможно с использованием ваучеров следущих номиналов: 10, 15, 25, 50, 75, 100, 150, 250. В случае если сумма платежа превышает максимально допустимое значение ваучера, то оплата происходит с помощью нескольких ваучеров. Например, если сумма равна 1250,00 EUR, то оплата произойдет двумя ваучерами с номиналом 500,00 EUR и одним с номиналом 250,00 EUR.
5. force_payment_method`force_pm`Для предварительного выбора метода title необходимо указывать код платежного метода в параметре—.
6. ru_pp_parameters.ditaДополнительно могут использоваться любые другие параметры из числа доступных для работы с Payment Widget. Детальная информация обо всех параметрах приведена в разделе.
7. ru_pp_authentication.ditaПосле определения всех параметров необходимо составить подпись. Подробную информацию см. в.

### Формат запросов и ответов на подтверждение суммы оплаты

После получения положительного ответа на запрос на инициирование оплаты от сервиса title со стороны платежной платформы мерчанту отправляется запрос на подтверждение суммы оплаты, указанной пользователем.

`check_deposit`Запросдля подтверждения суммы оплаты отправляется методом POST на заданный URL веб-сервиса.type`check_deposit`— тип операции на стороне веб-сервиса, значение параметра всегда;customer_id— идентификатор пользователя в системе мерчанта;project_id— идентификатор проекта, полученный от Some Platform при интеграции;payment_id— идентификатор платежа, уникальный в рамках проекта;amount*за исключениемосновных единицах валюты*ru_currency_codes.dita— сумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.;currency— валюта платежа в формате ISO-4217 alpha-3;payment_method`eco-voucher`— название платежного метода, значение параметра всегда;signature— подпись запроса.Пример запроса на подтверждение суммы оплаты{
    "type": "check_deposit",
    "project_id": 258,
    "payment_id": "X03936",
    "customer_id": "123",
    "amount": 1000,
    "currency": "EUR",
    "payment_method": "eco-voucher",
    "signature": "+5O+5Qix8kbnOgIQe5JddN50ardeRdGh7Jfi7SJXULy\/+6GbR5nwToLrUXmQ=="
}Запрос содержит следующие обязательные параметры:code`0decline4xx5xxdecline`— код ответа на запрос. Для подтверждения суммы необходимо указать значение, для отклонения — любое другое значение (в этом случае платеж переводится в статуси со стороны платежной платформы отправляется оповещение об отказе в проведении оплаты). Если в ответе используется HTTP-код классаили, то платеж также переводится в статус.payment_id— идентификатор платежа, уникальный в рамках проекта.Пример ответа на запрос{
   "code":0,
   "payment_id":"X03936"
}Ответ на запрос подтверждения суммы оплаты должен быть отправлен синхронно и содержать следующие обязательные параметры:

### Формат оповещений

ru_pp_callbacks.ditaДля оповещений о результатах оплат с применением метода title используется стандартный формат, описание которого представлено в разделе.

`2381210,00 EUR`В следующем примере оповещение свидетельствует о том, что в рамках проектадля пользователябыла успешно проведена оплата в размере.

В следующем примере оплата была отклонена из-за истечения времени ожидания.

### Дополнительные материалы

Для организации работы с оплатами через Payment Widget также могут быть полезны следующие материалы:

- ru_pp_interaction_organisation.dita
- ru_pp_authentication.dita
- ru_pp_payment_model.dita
- ru_pp_statuses_and_response_codes.dita

