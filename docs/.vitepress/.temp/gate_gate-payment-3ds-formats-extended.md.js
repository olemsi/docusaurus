import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Extended workflow messages","description":"","frontmatter":{},"headers":[],"relativePath":"gate/gate-payment-3ds-formats-extended.md","filePath":"gate/gate-payment-3ds-formats-extended.md"}');
const _sfc_main = { name: "gate/gate-payment-3ds-formats-extended.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="extended-workflow-messages" tabindex="-1">Extended workflow messages <a class="header-anchor" href="#extended-workflow-messages" aria-label="Permalink to &quot;Extended workflow messages&quot;">​</a></h1><h2 id="format-of-the-callback-with-iframe-data" tabindex="-1">Format of the callback with iframe data <a class="header-anchor" href="#format-of-the-callback-with-iframe-data" aria-label="Permalink to &quot;Format of the callback with iframe data&quot;">​</a></h2><p>When the extended authentication flow via Gate is used, the callback containing the data for an iframe element is sent from the payment platform to the web service. These callbacks are arranged in a standard format described in greater detail in the following article.</p><p>The payment platform does not send callbacks with iframe data when the issuer decides to use either of the following options without factoring in the customer&#39;s device information:</p><p><code>urlactioninput</code>The following example contains data for creating an iframe element. Use the value of theparameter from the iframe data as the value of theattribute and specify the values of other parameters inside the correspondingtags of the iframe code.</p><ul><li>belowthe challenge flow: the customer is redirected to the authentication page for verification. In this case, your web service is required to accept and process the callback with the redirection data. For more information, see.</li><li>the frictionless flow: the issuer authenticates the customer. In this case, your web service is required to accept and process the callback with the payment result.</li></ul><h2 id="callback-format-when-authentication-is-needed" tabindex="-1">Callback format when authentication is needed <a class="header-anchor" href="#callback-format-when-authentication-is-needed" aria-label="Permalink to &quot;Callback format when authentication is needed&quot;">​</a></h2><p>When the extended authentication flow via Gate is used, the callback containing the data for redirecting the customer to the issuer&#39;s ACS page is sent from the payment platform to the web service. These callbacks are arranged in a standard format described in greater detail in the following article. If the issuer decides to use the frictionless flow, the payment platform does not send callbacks with the customer redirection data. In this case, your web service is required to accept and process the callback with the payment result.</p><p><code>urlactioninput</code>The following example contains data for redirecting the customer to the authentication page. Use the value of theparameter as the value of theattribute, and specify the values of other parameters inside the correspondingtags of the HTML code.</p><h2 id="format-of-the-message-acknowledging-the-receipt-of-the-customer-s-device-information" tabindex="-1">Format of the message acknowledging the receipt of the customer&#39;s device information <a class="header-anchor" href="#format-of-the-message-acknowledging-the-receipt-of-the-customer-s-device-information" aria-label="Permalink to &quot;Format of the message acknowledging the receipt of the customer&#39;s device information&quot;">​</a></h2><p><code>3ds_notification_url</code>In the extended authentication workflow, the issuer&#39;s Access Control Server sends a message with an acknowledgement of receiving the customer&#39;s device data to the web service URL provided in the initial payment request (in theparameter). The message is arranged in the format chosen by the issuer.</p><h2 id="format-of-the-authentication-initiation-request" tabindex="-1">Format of the authentication initiation request <a class="header-anchor" href="#format-of-the-authentication-initiation-request" aria-label="Permalink to &quot;Format of the authentication initiation request&quot;">​</a></h2><p>When you use the extended authentication workflow, you initiate the authentication by sending a HTTP POST request to theendpoint. The request must contain the following objects and parameters:</p><p>Thus, a correct request for the 3‑D Secure authentication initiation contains a project identifier, a payment identifier, and a signature.</p><ul><li><code>general\`\`project_id</code>—the project identifier obtained from Platform.<code>payment_id</code>—the payment identifier unique within the merchant project.<code>signature</code>—the request signature generated after all required parameters have been specified. For more information about signature generation, see .—the object containing the general identification information of the request:</li><li><code>threeds_completion_indicatortruefalse</code>—the parameter that indicates whether the message acknowledging the receipt of the customer&#39;s device information was received within 10 seconds after the iframe element was closed. If the acknowledgement was received within 10 seconds, pass; if not, pass.</li></ul><h2 id="format-of-the-message-with-the-authentication-result" tabindex="-1">Format of the message with the authentication result <a class="header-anchor" href="#format-of-the-message-with-the-authentication-result" aria-label="Permalink to &quot;Format of the message with the authentication result&quot;">​</a></h2><p><code>cres</code>When the extended authentication flow via Gate is used, the authentication result is sent to the web service in the format chosen by the issuer. This information is passed in theparameter contained in the message.</p><h2 id="format-of-the-payment-completion-request" tabindex="-1">Format of the payment completion request <a class="header-anchor" href="#format-of-the-payment-completion-request" aria-label="Permalink to &quot;Format of the payment completion request&quot;">​</a></h2><p>Both the basic and the extended authentication flows via Gate imply that to resume processing of the payment once the authentication procedure has been completed, you have to send a request to theendpoint using the POST HTTP method. The request must contain the following objects and parameters:</p><p>Thus, a correct request for payment completion following the 3‑D Secure authentication contains project and payment identifiers, the signature, and the authentication result.</p><ul><li><code>general\`\`project_id</code>—the project identifier obtained from Platform .<code>payment_id</code>—the payment identifier unique within the merchant project.<code>signature</code>—the request signature generated after all required parameters have been specified. For more information about signature generation, see .—the object containing the general identification information of the request:</li><li><code>cres</code>—the parameter containing the customer&#39;s 3‑D Secure authentication result sent in the message from the Access Control Server.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("gate/gate-payment-3ds-formats-extended.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const gatePayment3dsFormatsExtended = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  gatePayment3dsFormatsExtended as default
};
