import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Authentication workflows","description":"","frontmatter":{},"headers":[],"relativePath":"gate/gate-payment-3ds-workflow.md","filePath":"gate/gate-payment-3ds-workflow.md"}');
const _sfc_main = { name: "gate/gate-payment-3ds-workflow.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="authentication-workflows" tabindex="-1">Authentication workflows <a class="header-anchor" href="#authentication-workflows" aria-label="Permalink to &quot;Authentication workflows&quot;">​</a></h1><h2 id="basic-workflow" tabindex="-1">Basic workflow <a class="header-anchor" href="#basic-workflow" aria-label="Permalink to &quot;Basic workflow&quot;">​</a></h2><p>In the payment platform, communicating the need for the 3‑D Secure authentication to merchants is carried out via callbacks. Your web service is expected to respond to these callbacks the same way it responds to any other callbacks received from the payment platform.</p><p><code>acs</code><em>redirectacceptsend</em>Basic authentication workflow implies that the callback communicating the need for authentication contains theobject. To respond to such callbacks, you need tothe customer to the issuer&#39;s ACS page,the authentication result, andthe payment completion request with the result to the payment platform. Note that once the need for authentication is established, the payment platform waits for the payment completion request with the authentication result from the web service; as a rule, the waiting time is 30 minutes. If the payment platform does not receive the request with the authentication result within the waiting time, the payment is automatically declined.</p><p><code>acsredirectData</code>Basic authentication workflow implies that the callback communicating the need for authentication contains either theobject (if the provider is not involved in the authentication process), or theobject (if the provider is involved). Responding to such callbacks requires the following:</p><p><code>cascading_with_redirecttrue</code>If the cascading payments option is enabled, the platform may request the 3‑D Secure authentication in one or more additional callbacks. If the additional callback contains theparameter that is set to, your web service is required to do the following:When you submit the authentication result, the payment platform continues payment processing.</p><p>For more information, see or contact the technical support specialists at support@platform.dev.</p><p>Information about the format of requests and callbacks is provided in the sections that follow. General information about using the Gate API can be found in .</p><h2 id="extended-workflow" tabindex="-1">Extended workflow <a class="header-anchor" href="#extended-workflow" aria-label="Permalink to &quot;Extended workflow&quot;">​</a></h2><p>In the payment platform, communicating the need for the 3‑D Secure authentication is carried out via callbacks. Your web service is expected to respond to these callbacks the same way it responds to any other callbacks received from the payment platform. However, there is no authentication-specific communication when the issuer makes the decision not to request any additional data while implementing the frictionless flow. In this case, the web service is not required to perform any actions other than those implied by the standard payment processing workflow while the authentication result is passed in the final callback.</p><p><code>iframethreeds2</code>In the extended workflow, the callback communicating the need for authentication contains theobject (in theobject). Responding to such callbacks requires your web service to do the following:</p><p><code>redirectcascading_with_redirecttrue</code>If the cascading payments option is enabled, your web service may need to accept one or more additional callbacks with theobject. If the additional callback contains theparameter that is set to, your web service is required to do the following:When you submit the authentication result, the payment platform continues payment processing.</p><p>For more information, see or contact the technical support specialists support@platform.dev.</p><p>Information about the format of callbacks and requests used in the extended workflow can be found in . General information about using the Gate API is provided in .</p><ul><li><em>Display</em>an error message to a customer on a page and obtain their consent to re-authenticate.</li><li><em>Respond</em>to the callback accordingly.</li></ul><ol><li><em>display</em>Using the data received in the callback,the iframe element to the customer.</li><li><em>Acceptsend</em>the acknowledgement message from the issuer&#39;s Access Control Server andthe authentication initiation request to the payment platform.</li><li><em>acceptcomplete**Acceptrespond</em><code>redirectthreeds2</code>a callback from the payment platform with theobject (in theobject) andwith the callback acknowledgement.<em>Redirect</em>the customer to the authentication page within 30 seconds after the callback was received.<em>Accept</em>the authentication result from the issuer.<em>Send</em><code>3ds_result</code>a payment completion request with the authentication result in theparameter to the payment platform within 30 minutes after the need for authentication was determined.If the issuer decides to use the frictionless flow, the web service is required toa callback with the authentication result information from the payment platform. If the issuer selects the challenge flow, the web service is required tothe following steps:</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("gate/gate-payment-3ds-workflow.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const gatePayment3dsWorkflow = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  gatePayment3dsWorkflow as default
};
