import { _ as _export_sfc, o as openBlock, c as createElementBlock, a2 as createStaticVNode } from "./chunks/framework.BFJzPV4p.js";
const __pageData = JSON.parse('{"title":"Request format","description":"","frontmatter":{},"headers":[],"relativePath":"gate/gate-money-transfer-format-request.md","filePath":"gate/gate-money-transfer-format-request.md"}');
const _sfc_main = { name: "gate/gate-money-transfer-format-request.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="request-format" tabindex="-1">Request format <a class="header-anchor" href="#request-format" aria-label="Permalink to &quot;Request format&quot;">​</a></h1><p>When creating a request for money transfer processing, follow the specification of theendpoint. As for a card-to-card transfer (which can be regarded as a typical use case), consider the following:</p><p>Thus, a correct request for processing a card-to-card money transfer contains project and payment identifiers, the signature, information about the money transfer sender, the sender’s and the recipient’s payment instruments, the payment methods, amounts, and currency codes for debit and credit operations.</p><ol><li>Send the request to theendpoint.</li><li><code>general``project_id</code>—the project identifier assigned by Platform at the stage of integration<code>payment_id</code>—the payment identifier unique within the merchant project<code>signature</code>—the request signature generated after all required parameters have been specified (for more information, see )—the object containing the general identification information of the request:<code>customer``id</code>—the identifier of the customer (money transfer sender) within the merchant project;<code>ip_address</code>—the customer&#39;s IP address relevant for the initiated request<code>country</code>—the country code in the customer&#39;s address in ISO 3166-1 alpha-2<code>city</code>—the place of residence (e.g., town, city, or other settlement type) in the customer&#39;s address<code>first_name</code>—the first name of the customer<code>last_name</code>—the last name of the customer<code>zip</code>—the postal or zip code in the customer&#39;s address<code>address</code>—the name of the street and the house number (including any additional parts of the address such as building indicators and apartment numbers) in the customer&#39;s address, separated by a comma<code>phone</code>—the sender&#39;s phone number that must be passed if the funds are credited to a Visa card issued in Brazil or Qatar—the object containing the sender information:<code>recipient_customer``first_name</code>— the first name of the recipient<code>last_name</code>— the last name of the recipient<code>phone</code>— the phone number of the recipient<code>email</code>— email address of the recipient.—the object containing the recipient information:<code>payment``sender_payment_method</code>—the code of the payment method for debit<code>recipient_payment_method</code>—the code of the payment method for credit<code>sender_amount</code>—the amount to be debited from the sender in minor currency units<code>sender_currency</code>—the currency code for the debited amount in ISO-4217 alpha-3<code>recipient_amount</code>—the amount to be credited to the recipient in minor currency units<code>recipient_currency</code>—the currency code for the credited amount in ISO-4217 alpha-3—the object containing the money transfer information:The request must contain the following objects and parameters:</li><li><code>sender``pan</code>—the number of the payment card (specified as is, without masked characters, spaces, or other separators)<code>year</code>—the year when the card expires (in four-digit format YYYY according to the Gregorian calendar)<code>month</code>—the month when the card expires (provided as a number without a leading zero)<code>card_holder</code>—the sender&#39;s first and last name (as specified on the card)<code>cvv</code>—card verification codeThe request must contain the sender&#39;s payment instrument information. When sending the actual card details, include the following parameters in theobject:</li><li><code>recipient``pan</code>—the number of the payment card (specified as is, without masked characters, spaces, or other separators);<code>card_holder</code>—the recipient&#39;s first and last name (as specified on the card).The request must contain the following information about the recipient&#39;s payment instrument. When sending the actual card details, include the following parameters in theobject:</li><li>If necessary, you can also add any other additional parameters and objects specified in the API specification.</li></ol>', 4)
  ])]);
}
const gateMoneyTransferFormatRequest = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  gateMoneyTransferFormatRequest as default
};
