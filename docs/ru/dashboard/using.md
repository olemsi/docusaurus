# Получение информации о балансах и платежах

## Общие сведения

В структуре statistics_api используются разные конечные точки для получения разных видов данных:

в спецификации интерфейсаОбщий порядок работы с каждой из этих конечных точек соответствует описанному в предыдущих разделах. В этом разделе представлена информация об особенностях запросов к этим конечным точкам, дополняющая описания структур данных.

- `request-db-balance-get-text`— для получения данных о балансах по проектам мерчанта (в настоящее время поддерживается получение информации только о балансах типа OUT);
- `request-db-operations-get-text`— для получения данных об операциях за заданный период;
- `request-db-operations-get-by-payment-text`— для получения данных об операциях конкретного платежа.

## Контроль балансов

`tokensignature`Для получения данных о балансах по проектам мерчанта следует отправлять запросы в конечную точку. В этих запросах должны указываться параметры(токен учетной записи) и(подпись). В ответах на такие запросы содержатся сведения о текущем состоянии балансов типа OUT.

`*валютасумма*"<>": "<>"`Сведения о каждом балансе это наименование баланса и доступная сумма в валюте этого баланса, причем валюта указывается в названии параметра, а сумма — в его значения, например.

## Общий контроль операций

Для получения данных об операциях за заданный период по проектам мерчанта следует отправлять запросы к конечной точке. При этом необходимо учитывать следующее:

В ответе на каждый из таких запросов содержатся данные об операциях за указанный период с учетом условий, заданных в запросе.

`fields`Если достаточно параметров, используемых по умолчанию, можно не указывать в запросах массив.

`fieldsoperation_typeopeartion_statuscustomer_email`Если нужны нетиповые параметры по операциям, можно задавать целевые параметры через массив, а если еще и необходимо применить фильтрацию операций, то можно задавать критерии фильтрации через различные параметры, например,и.

1. `interval``fromГГГГ-ММ-ДД чч:мм:сс`— дата и время начала интервала, в формате;`toГГГГ-ММ-ДД чч:мм:сс`— дата и время окончания интервала, в формате;— объект с информацией о границах временно́го интервала, к которому относятся последние действия (и соответствующие обновления информации) по целевым операциям:`token`— токен учетной записи;`signature`— подпись запроса, составленная после указания целевых параметров.В каждом запросе должны использоваться следующие объекты и параметры:
2. `project_idproject_id4, 12`Для фильтрации операций по их отношению к проектам используется параметр, а также учитывается наличие прав доступа к проектам через токен, указанный в запросе. По умолчанию, если в запросе не указан этот параметр, в ответе от платежной платформы отправляются данные об операциях по всем проектам, к которым есть права доступа через используемый токен. При необходимости получить данные по конкретным проектам (одному или нескольким), в параметреследует указать идентификаторы этих проектов (через запятую с пробелом, если необходимо указать несколько идентификаторов; например,).
3. `tzintervaloperation_created_atoperation_completed_attz+10:30Asia/Singapore`Для использования часового пояса, отличного от заданного по умолчанию UTC +00:00, используется параметр. Выбор часового пояса с помощью этого параметра влияет на то, какие операции попадают в заданный интервал времени в объекте, и на значения параметров в ответе, связанных с датой и временем — например,и. Часовой пояс в параметреуказывается через время смещения в формате UTC (например,) или через название в соответствии с базой данных часовых поясов IANA Time Zone Database (например,).
4. `limit010001000`Для ограничения количества записей с информацией об операциях, возвращаемых в одном ответе, используется параметр. Этот параметр может принимать значения в диапазоне–и по умолчанию равен, при этом количество записей, возвращаемых в ответе, может быть меньше заданного значения.`offsetlimitoffsetoffsetlimit`При необходимости получить информацию более чем о тысяче операций, следует отправлять несколько запросов и в каждом из них использовать параметр. Этот параметр определяет величину сдвига для отбора операций. Когда в запросе указываются оба параметра — и, и, то сначала операции пропускаются в количестве, заданном значением, а затем записи с информацией о следующих операциях возвращаются в ответе в количестве, не превышающем значения.`1125"limit": 1000"offset": 0"limit": 125"offset": 1000`Например, если необходимо получить информацию ооперациях, то в первом запросе можно указатьи, а во втором —и.
5. `fieldsfields`Для фильтрации данных об операциях по различным параметрам используется массив. По умолчанию, если в запросе не указан этот массив, в ответе от платежной платформы отправляется типовой набор данных для каждой операции. Если же необходимо получить данные по конкретным параметрам (одному или нескольким), в качестве элементов массиваследует указать названия этих параметров.`operations`Параметры указываются в массиве через запятую с пробелом (если необходимо указать несколько названий), при этом их можно указывать в произвольном порядке, но в ответах используется фиксированный порядок данных. Полный перечень параметров представлен в спецификации statistics_api — в виде параметров объектав описании формата ответа на запрос к конечной точке. В этом описании атрибуты указаны в порядке, который используется по умолчанию, и отмечены как обязательные.
6. `operation_typeoperation_statusoperation_typeoperation_status`ru_gate_payment_model.ditaДля получения данных об операциях по конкретным типам и (или) статусам используются массивыи. По умолчанию, если в запросе не указаны эти массивы, в ответе отправляются данные об операциях всех допустимых типов и статусов. В качестве элементов массивовиследует указывать названия конкретных типов и статусов операций соответственно, через запятую с пробелом, если необходимо указать несколько названий. Полный перечень типов и статусов операций представлен в разделе.`operation_typeoperation_status`Параметрыимогут иметь тип массив строк (если необходимо указать одно или несколько значений) или строковый тип (если необходимо указать одно значение).
7. `customer_idcustomer_email`Для фильтрации данных об операциях с учетом идентификаторов и (или) адресов электронной почты пользователей используются параметры(массив строк) и(строка). По умолчанию, если в запросе не указаны эти параметры, в ответе отправляются данные об операциях с любыми идентификаторами и адресами электронной почты пользователей.`customer_id`Параметрможет иметь тип массив строк (если необходимо указать одно или несколько значений) или строковый тип (если необходимо указать одно значение).

## Контроль операций по отдельным платежам

`payment_idtokensignature`Для получения данных обо всех операциях конкретного платежа следует использовать запрос к конечной точке. В этом запросе должны указываться параметры(идентификатор целевого платежа),(токен учетной записи) и(подпись). В ответе на такой запрос содержатся сведения об операциях целевого платежа.
