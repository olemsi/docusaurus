# Дополнительные возможности

- ru_pp_addendum_data.dita
- ru_pp_clarification.dita
- ru_pp_cascading.dita
- ru_pp_cascading_cards.dita
- ru_pp_time_limit.dita
- ru_pp_qr_code.dita
- ru_pp_receipt_data.dita
- ru_pp_debt_repayments.dita
- ru_pp_js-callbacks.dita
- ru_pp__preselectingps.dita
- ru_pp_payment_by_token.dita
- ru_pp_try_again.dita
- ru_pp_avs.dita
- all_pp__54-fz.dita
- all_fiscal_checks_kz.dita
- ru_pp_split_payment.dita
- ru_pp_gathering_customer_data.dita
- ru_pp_crypto_script.dita
- ru_pp_saved_data.dita
- ru_pp_wigetlanguages.dita
- ru_pp_hide_methods.dita
- ru_pp_amount_modification.dita

## Дополнительная кастомизация

В случае, если вы открываете виджет в модальном окне, вы можете стилизовать модальное окно в соответствии с дизайном вашего сайта.

CSS-классы должны быть доступны на странице сайта, на которой производится запуск виджета. Определить CSS-классы можно любым удобным для вас способом.

Доступные CSS-селекторы приведены в таблице ниже.

| Селектор | Описание |
| --- | --- |
| .custom_wrap_class .ep-modal-wrap | Оболочка модального окна |
| .custom_wrap_class #ep-modal-body | Тело модального окна. Внутрь этого элемента будет помещен iframe с виджетом |
| .custom_wrap_class #ep-modal-close | Кнопка закрытия виджета |

::: tip
css_modal_wrapЧтобы подключить кастомизацию CSS, передайте в объекте в параметреCSS-класс используемого элемента.
:::

## Оповещения (callbacks) в Payment Widget

Оповещение (callback) — это техническое сообщение, которое платежная платформа Some Platform отправляет на заданный URL-адрес мерчанта, чтобы сообщить определенную информацию, например результат выполнения платежа.

### Как выглядит оповещение в Some Platform

Оповещение представляет собой HTTP-запрос, отправляемый методом POST на URL-адрес, который вы предоставляете Some Platform в процессе интеграции.

**JSON**Параметры оповещения передаются в теле запроса в формате, при этом массив параметров преобразуется в одну строку. Вы можете настраивать, какие параметры должны передаваться в теле запроса. Для этого обращайтесь в службу поддержки Some Platform.

Вот пример данных из тела оповещения типового формата. Содержимое JSON-строки в этом примере разбито на несколько строк для удобства чтения.

Ниже приведен пример данных из тела оповещения, настроенного индивидуально под потребности мерчанта.

ru_pp_authentication.ditaОповещения от Some Platform содержат подпись. Подробнее о том, как проверить подпись в оповещении, см. раздел.

### Когда Some Platform отправляет оповещения

Платежная платформа отправляет вашей системе оповещения, когда:

- **изменяется статус платежа**ru_pp_payment_model.dita`200 OK`Если вы проводите платежи с использованием Payment Widget, данные оповещения для вас несут информационный характер. Ваша система должна отвечать на них кодом состояния HTTPи не выполнять других действий. Вы можете отказаться от получения этих оповещений, если необходимо. Для этого обращайтесь в службу поддержки Some Platform;. Это может быть связано, например, с перенаправлением пользователя, отображением пользователю инструкции для оплаты, прохождением процедуры аутентификации 3-D Secure пользователем, запросом у пользователя данных, недостающих для проведения платежа и т. д. Подробнее о статусах различных операций см. раздел.
- **создан, удален или истек токен**ru_pp_token.dita#ru_pp_token/section_x3c_5zd_plbдля банковской карты. Пример такого оповещения см. в разделе;
- **завершена обработка операции**в платежной платформе.

### Адреса для приема оповещений

Оповещения отправляются на URL-адрес, который вы предоставляете Some Platform в процессе интеграции. Если у вас есть несколько проектов, вы можете предоставить для каждого проекта отдельный URL-адрес, на который Some Platform будет отправлять оповещения.

Вы можете предоставить Some Platform отдельные URL-адреса, чтобы получать оповещения с определенной информацией по платежу. Например, чтобы оповещения об успешном завершении платежа платежная платформа отправляла на один адрес, а об отклонении — на другой. Вы также можете использовать отдельный URL-адрес для получения оповещений по платежам определенного типа или платежам, проведенным с использованием конкретного платежного метода.

**ПроектыОповещения**ru_db_projects_callbacks.ditaВы можете самостоятельно указать дополнительные адреса для приема оповещений в Dashboard. Для этого в Dashboard перейдите в раздели выберите вкладку. Инструменты в Dashboard позволят вам определить, в каких случаях и на какие адреса Some Platform будет отправлять вашей системе оповещения. Подробнее о том, как задать адреса для отправки оповещений в Dashboard, см. раздел.

Обратите внимание, что ваша система должна принимать оповещения только с IP-адреса, полученного от команды интеграции Some Platform. Чтобы уточнить IP-адрес, с которого Some Platform отправляет оповещения, обратитесь в службу поддержки Some Platform.

**Адреса для приема оповещений по конкретному платежу**

merchant_callback_urlВы можете указать отдельный URL-адрес для приема оповещений по конкретному платежу. Для этого добавьте в запрос на открытие страницы оплаты параметри задайте в качестве значения URL-адрес, на который Some Platform будет отправлять оповещения. После этого все оповещения по этому платежу (и только по нему) будут отправляться на указанный URL-адрес независимо от того, какой URL-адрес для приема оповещений задан для вашего проекта.

Вы также можете указать в запросе параметры с URL-адресами, на которые Some Platform будет отправлять оповещения с результатом конкретного платежа:

merchant_callback_urlmerchant_callback_urlmerchant_success_callback_urlmerchant_success_callback_urlmerchant_callback_urlmerchant_decline_callback_urlЭти параметры приоритетнее параметраи URL-адреса, указанного в настройках вашего проекта. Так, если задать значения обоих параметров —и, то в случае успеха Some Platform отправит оповещение с результатом платежа на URL-адрес, указанный в, игнорируя как параметр, так и URL-адрес, указанный для вашего проекте в платежной платформе. Аналогичная ситуация с параметром, но с поправкой на то, что речь идет об итоговом оповещении об отклонении платежа.

- merchant_success_callback_url*успешно проведен*— URL-адрес, на который Some Platform отправит callback-оповещение о результате платежа, если он будет;
- merchant_decline_callback_url*отклонен*— URL-адрес, на который Some Platform отправит callback-оповещение о результате платежа, если он будет.

### Как отреагировать на оповещение

`200 OK`Ваша система должна ответить на оповещение кодом состояния HTTP, если она успешно приняла его. При этом, ваша система не должна ограничивать время приема оповещений, потому что оповещения от Some Platform могут поступать с задержкой.

`400 Bad Request500 Internal Server Error`Если ваша система приняла оповещение с ошибкой, она должна ответить на него кодом состояния HTTP, соответствующим ошибке, например(если вам не удалось преобразовать строку параметров в массив) или(если оповещение пришло на неправильный URL-адрес). Несмотря на то что ваша система приняла оповещение с ошибкой, она должна быть готова принять последующие оповещения, относящиеся к этому же платежу.

`200 OK`Если ваша система не ответила на оповещение кодом состояния HTTP, Some Platform будет отправлять вам оповещения снова по следующему расписанию:

Всего Some Platform отправляет 120 оповещений в течение 11 суток. Расписание отправки оповещений может незначительно изменяться в зависимости от загрузки системы.

- 6 попыток с интервалом в 10 секунд;
- ``*n*далее 62 попытки в течение четырех часов с увеличивающимся интервалом (интервал увеличивается в соответствии с формулой, где— порядковый номер попытки отправки);
- потом каждые 4 часа до достижения максимального количества попыток.

::: warning
`200 OK`В некоторых случаях Some Platform отправляет повторные оповещения о результате платежа по уже выполненным платежам. Если ваша система получила такое оповещение, она должна ответить на него кодом состояния HTTPи не предоставлять повторных услуг или дополнительных товаров в рамках этого платежа.
:::

## Способы возврата пользователя в проект после оплаты

Payment Widget позволяет управлять способом возврата пользователей в проект после совершения платежа на Payment Widget. Данные настройки управляются несколькими параметрами, отправляемыми в запросе на проведение платежа. Далее приведены эти параметры и их возможные значения.

### URL-адреса для возврата

merchant_success_urlmerchant_fail_urlredirect_fail_urlredirect_success_urlmerchant_success_urlmerchant_fail_urlredirect_fail_urlredirect_success_urlДля настройки URL-адреса, на который вернется пользователь передайтеиилиис указанием ссылок для перенаправления в случае проведенного или отклоненного платежа. В первом случае сипользователю отобразится страница с результатом на Payment Widget и кнопкой возврата, по нажатии на которую происходит перенаправление на указанный URL. Во втором случае сипользователь перенаправится автоматически после оплаты на указанный URL без дополнительных шагов и страницы с результатом.

merchant_return_urlДля возможности пользователю вернуться в проект со страницы оплаты по кнопке возврата на любом шаге до завершения оплаты передайте URL-адрес в параметре.

redirect_return_urlДля возможности пользователю вернуться в проект со страницы оплаты на стороне внешнего провайдера по кнопке возврата на любом шаге до завершения оплаты передайте URL-адрес в параметре. Подробнее о возможности подключения данной функциональности уточняйте у курирующего менеджера Some Platform.

Также вы можете управлять способами возврата в зависимости от браузера, который использует пользователь. Подробную информацию уточняйте у службы технической поддержки support@example.com

### Возможность возврата

merchant_success_enabledmerchant_fail_enabledmerchant_return_enabled0— выключено1— только в режиме перенаправления (полная страница)2— в режиме перенаправления и в режиме iframe (на сайте мерчанта)Для включения возможности возврата пользователя после проведенного платежа передайте, после отклоненного платежа —, на любом шаге оплаты по кнопке возвратас одним из значений:

### Режим возврата

merchant_success_redirect_moderedirect_success_modemerchant_fail_redirect_moderedirect_fail_modemerchant_return_redirect_modeiframe— открытие в режиме iframe (встроенной в веб-страницу)parent_page— открытие в модальном окнеblank_page— открытие на новой вкладке браузераДля настройки режима открытия страницы проекта во время возврата пользователя после проведенного платежа передайте/, после отклоненного платежа —/, на любом шаге оплаты по кнопке возвратас одним из значений:

### Примеры режимов возврата

iframemerchant_success_redirect_mode merchant_fail_redirect_mode merchant_return_redirect_modemerchant_success_enabled merchant_fail_enabled merchant_return_enabledОжидаемый результатiframe0Нет кнопки для возврата1Перенаправления нет, обработка JavaScript оповещений2Перенаправление в iframeparent_page0Нет кнопки для возврата1Перенаправления нет, обработка JavaScript оповещений2Перенаправление всей страницыblank_page0Нет кнопки для возврата1Перенаправления нет, обработка JavaScript оповещений2Перенаправление в новой вкладкеЕсли Payment Widget открыт в режиме:

merchant_fail_urlmerchant_success_urlmerchant_return_url**Вернуться на сайт**В случае если отправлены только параметры//при нажатии на кнопкуперенаправления не происходит, доступна обработка JavaScript оповещений.

merchant_success_enabledmerchant_fail_enabledЕслиине переданы — возврат происходит в режиме "2".

**модальном окне**merchant_success_redirect_mode merchant_fail_redirect_mode merchant_return_redirect_modemerchant_success_enabled merchant_fail_enabled merchant_return_enabledОжидаемый результатiframe0Нет кнопки для возврата1Перенаправления нет, модальное окно закрывается при нажатии на кнопку2Перенаправление в iframeparent_page0Нет кнопки для возврата1Перенаправления нет, модальное окно закрывается при нажатии на кнопку2Перенаправление всей страницыblank_page0Нет кнопки для возврата1Перенаправления нет, модальное окно закрывается при нажатии на кнопку2Перенаправление в новой вкладкеЕсли Payment Widget открыт в:

merchant_fail_urlmerchant_success_urlmerchant_return_url**Вернуться на сайт**В случае если отправлены только параметры//при нажатии на кнопкупроисходит закрытие модальной страницы.

merchant_success_enabledmerchant_fail_enabledЕслиине переданы — возврат происходит в режиме "2".

merchant_success_redirect_mode merchant_fail_redirect_mode merchant_return_redirect_modemerchant_success_enabled merchant_fail_enabled merchant_return_enabledОжидаемый результатiframe0Нет кнопки для возврата1Перенаправление всей страницы2Перенаправление всей страницыparent_page0Нет кнопки для возврата1Перенаправление всей страницы2Перенаправление всей страницыblank_page0Нет кнопки для возврата1Перенаправление в новой вкладке2Перенаправление в новой вкладкеЕсли Payment Widget открыт в отдельной вкладке:

merchant_fail_urlmerchant_success_urlmerchant_return_url**Вернуться на сайт**В случае если отправлены только параметры//при нажатии на кнопкупроисходит перенаправление всей страницы.

merchant_success_enabledmerchant_fail_enabledЕслиине переданы — возврат происходит в режиме "2".

---

## Схема работы через Payment Widget

Аутентификация пользователя с использованием протокола 3ds2 может выполняться при проведении одностадийных и двухстадийных оплат с применением платежных карт, а также при проверках действительности платежных карт.

Для проведения платежа с такой аутентификацией со стороны веб-сервиса необходимо отправить запрос, содержащий требуемые параметры и подпись, на рабочий URL Some Platform и принять оповещение о результате платежа.

Если аутентификация с использованием протокола 3ds2 не выполнена из-за сбоя на стороне эмитента или платежной системы либо из-за того, что эмитент не поддерживает протокол 3ds2, со стороны платежной платформы инициируется попытка аутентификации пользователя с использованием протокола 3ds1. При этом со стороны веб-сервиса не требуется дополнительных действий, однако пользователь может получить от эмитента не только проверочный код и уведомление об успешном платеже, но и уведомление об отклонении платежа.

Далее представлена схема аутентификации 3ds2 в контексте оплаты в одну стадию.
