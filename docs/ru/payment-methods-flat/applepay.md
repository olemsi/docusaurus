# Обзор

## Схема работы

В проведении отдельного платежа с использованием title задействуется ваша система, один из интерфейсов и платежная платформа Some Platform, а также технические средства title.

## Основные операции

* Дополнительную информацию необходимо уточнять у курирующего менеджера Some Platform.

**ПроектыПлатежные методы**Уточнить минимальную и максимальную сумму платежа, доступную в вашем проекте, вы можете в Dashboard. Для этого в Dashboard перейдите в раздели выберите вкладку.

|  | Интерфейсы | Суммы | Время |  |  |  |  |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Payment Widget | Gate | Dashboard | минимум | максимум | базовое | предельное |  |
| Оплата в одну стадию |  |  |  | * | * | * | * |
| Блокировка средств в рамках оплаты в две стадии |  |  |  | * | * | * | * |
| Списание и отмена заблокированных средств в рамках оплаты в две стадии |  |  |  |  |  | * | * |
| Регистрация повторяемой оплаты |  |  |  |  |  | * | * |
| Повторные списания в рамках повторяемой оплаты |  |  |  |  |  | * | * |
| Возвраты |  |  |  |  |  | * | * |

## Сценарии использования

Для проведения оплат с помощью title пользователю достаточно один раз зарегистрировать свою карту на устройстве title и после использовать Touch ID, Face ID, пароль или двойное нажатие на боковую кнопку (Apple Watch) для подтверждения оплаты.

Детальные сведения о том, что необходимо делать со стороны мерчанта для проведения платежей, а также о том, что можно использовать для анализа информации о проведенных платежах и операциях, представлены далее.

| и Gate, а также возвраты и проверка действительности карты пользователя через Gatetitle — платежный метод для оплаты товаров и услуг в приложениях iOS или на сайтах. При использовании title аутентификация пользователей выполняется с помощью Face ID, Touch ID или пароля, а платежи проводятся с применением токенизированных учетных данных кредитных и дебетовых карт. Вместе с этими данными на пользовательских устройствах могут храниться и использоваться при проведении платежей дополнительные сведения, такие как данные счетов, адреса доставки и контактная информация. Для работы с title в платежной платформе доступны разовые и повторяемые оплаты через Payment Widget.здесьЗагрузить логотип этого платежного метода в векторном формате можно. |  |
| --- | --- |

| Тип платежного метода | Бесконтактные платежи |
| --- | --- |
| Регионы использования | [ссылке](https://support.apple.com/ru-ru/HT207957)Список доступен по |
| Валюты платежей | Все валюты, поддерживаемые платежными системами American Express, Maestro, Mastercard и Visa в доступных регионах |
| Конвертация валют | На стороне Some Platform |
| Одностадийная оплата |  |
| Двухстадийная оплата |  |
| Повторяемые оплаты |  |
| Выплата |  |
| Полный возврат |  |
| Частичный возврат |  |
| Проверка действительности |  |
| Особенности | [из ряда поддерживаемых устройств Apple](https://support.apple.com/ru-ru/KM207105)Устройства —Браузер — SafariПлатежные карты — American Express, Maestro, Mastercard, Visaоткрытия платежной страницы, встроенной в веб-страницу (iframe)Способ открытия Payment Widget — любой, кромеДоступно выполнение опротестования, подробности уточняйте у курирующего менеджера Some Platform.[документации](https://developer.apple.com/documentation/applepaywebmerchantregistrationapi/registering_with_apple_pay_and_applying_to_use_the_api)Домен сайта зарегистрирован в сервисах Apple. Регистрацию необходимо выполнить самостоятельно (подробнее в).[документации](https://developer.apple.com/documentation/apple_pay_on_the_web)Сайт поддерживает функциональность запуска скрипта для начала сессии Apple согласно.Требования к вашей системе для проведения оплат через Gate: |
| Валюты расчетов с мерчантами | *по умолчанию:**по запросу:* |
| Скорость расчетов с мерчантами | без задержек |
| Организация и стоимость подключения | По согласованию с курирующим менеджером Some Platform |

---

## Оплата в две стадии через Gate

### Общая информация

При выполнении оплаты в две стадии через Gate с использованием метода title веб-сервис должен выполнить следующее:

Проведение оплаты в две стадии выполняется поэтапно:

Полная схема оплаты с использованием метода title выглядит следующим образом.

ru_gate_sts_protocol.ditaИнформация о форматах запросов и оповещений, используемых при проведении оплаты в две стадии с использованием метода title через Gate приведена далее. Общую информацию о работе с Gate API можно найти в разделе.

::: tip
Для выполнения оплат через Gate настройте взаимодействие вашего веб-сервиса с сервисом provider_service.
:::

### Запрос на создание платежной сессии

Далее представлена информация, необходимая для создания платежной сессии с использованием метода title.

Пример корректного запроса на создание платежной сессии с title представлен далее.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/session/applepay` |
| Полная спецификация конечной точки |  |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| validation_urltype_string type_strictly_required | `apple-pay-gateway.apple.com/paymentservices/startSession`Пример:URL для проверки. |  |
| domain_nametype_string type_strictly_required | `appay.en.ngrok.io`Пример:Доменное имя вашего веб-сервиса. |  |
| display_nametype_string type_strictly_required | `test payment`Пример:Название проекта для отображения, не более 64 символов. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Запрос на блокировку средств

Далее представлена информация, необходимая для создания и отправки запроса на блокировку средств с использованием метода title.

Вот пример данных из запроса на блокировку средств с использованием метода title:

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/applepay/auth` |
| Полная спецификация конечной точки |  |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе.type_optional — параметр необязателен, но в общем случае его наличие в запросе способствует оптимизации проведения платежа, например упрощает процесс платежа для пользователя или повышает процент успешных платежей. |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| customertype_object type_strictly_required | idtype_string type_strictly_required | `customer_123`Пример:Идентификатор пользователя, уникальный в рамках проекта. |
| ip_addresstype_string type_strictly_required | `198.51.100.47`Пример:IP-адрес устройства пользователя. |  |
| emailtype_string type_strictly_required | `johndoe@example.com`Пример:Адрес электронной почты пользователя. |  |
| first_nametype_string type_strictly_required | `John`Пример:Имя пользователя. |  |
| last_nametype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |  |
| paymenttype_object type_strictly_required | amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.dita`example_amount`Пример: example_amount_in_majors                                     example_currency передается какСумма платежа в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| currencytype_string type_strictly_required | `example_currency`Пример:Код валюты платежа в формате ISO-4217 alpha-3. |  |
| etokentype_object type_strictly_required | tokentype_string type_strictly_required | `"{\"paymentData\":{...},\"paymentMethod\":{...},\"transactionIdentifier\":\"...\"}"`Пример:Токен, полученный от title после идентификации пользователя. |
| recurringtype_object type_optional | registertype_boolean type_optional | `true`ru_gate_payment_recurring_registration.ditaПодробнее о регистрации повторяемой оплаты с использованием Gate, см.`true`Пример:Признак регистрации повторяемой оплаты, необходимо использовать значение. |
| typetype_string type_optional | `RCU`ru_gate__saved_cards_payments_type.dita`R`Пример:Тип повторяемой оплаты:(для регулярной оплаты),(для экспресс-оплаты), или(для автооплаты), подробную информацию см. в разделе. |  |
| periodtype_string type_optional | `D`— ежедневно,`W`— еженедельно,`M`— ежемесячно,`Q`— ежеквартально,`Y`— ежегодно.`W`Пример:Период выполнения регулярной оплаты. Возможные варианты: |  |
| timetype_string type_optional | `hh:mm:ss (UTC0)``10:00:00`Пример:Время выполнения регулярной оплаты в формате. |  |
| scheduled_payment_idtype_integer type_optional | `567890`Пример:Идентификатор следующей повторяемой оплаты. |  |
| start_datetype_string type_optional | `ДД-ММ-ГГГГ``10-10-2020`Пример:Дата начала регулярной оплаты, в формате(должна быть как минимум на один день позже, чем дата регистрации). |  |
| intervaltype_integer type_optional | `periodWinterval31100``3`Пример:Интервал выполнения регулярной оплаты. Множитель для кратного увеличения периода списаний, например чтобы списания выполнялись раз в три недели, в параметренадо задать значение, а в параметрезначение; возможные значения: отдо. |  |
| expiry_yeartype_integer type_optional | `2025`Пример:Год окончания действия повторяемой оплаты |  |
| expiry_monthtype_integer type_optional | `5`Пример:Месяц окончания действия повторяемой оплаты. |  |
| expiry_daytype_integer type_optional | `5`Пример:День окончания действия повторяемой оплаты. |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Запрос на списание заблокированных средств

Далее представлена информация, необходимая для создания запроса на списание заблокированных средств с использованием метода title.

Вот пример данных из запроса на списание заблокированных средств с использованием метода title:

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/applepay/capture` |
| Полная спецификация конечной точки |  |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| paymenttype_object type_strictly_required | amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.dita`example_amount`Пример: example_amount_in_majors                                     example_currency передается какСумма платежа в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Сумма платежа должна совпадать со значением, переданным в запросе на авторизацию. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| currencytype_string type_strictly_required | `example_currency`Пример:Код валюты платежа в формате ISO-4217 alpha-3. Валюта платежа должна совпадать со значением, переданным в запросе на авторизацию. |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Запрос на отмену блокировки средств

Далее представлена информация, необходимая для создания запроса на отмену блокировки средств с использованием метода title.

Пример запроса на отмену блокировки средств с применением метода title.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/applepay/cancel` |
| Полная спецификация конечной точки |  |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Оповещение (callback)

ru_gate_callbacks.ditaВ методе title результат оплаты в две стадии платежная платформа возвращает в оповещении. Подробнее о структуре оповещений см..

idrecurringПри успешной регистрации повторяемой оплаты оповещение будет содержать идентификатор зарегистрированной оплаты в параметреобъекта.

`123example_amount_in_majors&#160;example_currency`В следующем примере в оповещении содержится информация о том, что в рамках проектапроизведена блокировка средств в размере.

В следующем примере содержится информация об отклонении блокировки средств.

`123`В следующем примере в оповещении содержится информация о том, что в рамках проектапроизведено списание ранее заблокированных средств.

В следующем примере содержится информация об отклонении списания средств.

В следующем примере в оповещении содержится информация об отмене блокировки средств.

В следующем примере содержится информация об отклонении отмены блокировки средств.

### Дополнительные материалы

- ru_gate_interaction_organisation.dita
- ru_gate_payment_model.dita
- ru_gate_authentication.dita
- ru_gate_statuses_and_response_codes.dita

---

## Оплата в одну стадию через Gate

### Общая информация

Для оплаты через Gate с использованием метода provider_service со стороны веб-сервиса необходимо:

Полная схема оплаты в одну стадию с использованием метода title выглядит следующим образом.

ru_gate_sts_protocol.ditaИнформация о формате запросов и параметрах инициализации оплат методом title через Gate, а также о формате оповещений о результатах оплат приведена далее, общая информация о работе с API см. в разделе.

1. Отправить запрос на создание платежной сессии, содержащий требуемые параметры и подпись, на рабочий URL Some Platform.
2. Принять данные платежной сессии и пользователя.
3. Отправить запрос оплату, содержащий требуемые параметры и подпись, на рабочий URL Some Platform.
4. Принять от платежной платформы Some Platform оповещение (callback) о результате оплаты.

### Запрос на создание платежной сессии

Далее представлена информация, необходимая для создания платежной сессии с использованием метода title.

Пример корректного запроса на создание платежной сессии с title представлен далее.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/session/applepay` |
| Полная спецификация конечной точки |  |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| validation_urltype_string type_strictly_required | `apple-pay-gateway.apple.com/paymentservices/startSession`Пример:URL для проверки. |  |
| domain_nametype_string type_strictly_required | `appay.en.ngrok.io`Пример:Доменное имя вашего веб-сервиса. |  |
| display_nametype_string type_strictly_required | `test payment`Пример:Название проекта для отображения, не более 64 символов. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Запрос на проведение оплаты

Далее представлена информация, необходимая для создания и отправки запроса на оплату с использованием метода title.

Вот пример данных из запроса на оплату с использованием метода title:

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/applepay/sale` |
| Полная спецификация конечной точки |  |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе.type_optional — параметр необязателен, но в общем случае его наличие в запросе способствует оптимизации проведения платежа, например упрощает процесс платежа для пользователя или повышает процент успешных платежей. |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| customertype_object type_strictly_required | idtype_string type_strictly_required | `customer_123`Пример:Идентификатор пользователя, уникальный в рамках проекта. |
| ip_addresstype_string type_strictly_required | `198.51.100.47`Пример:IP-адрес устройства пользователя. |  |
| first_nametype_string type_strictly_required | `John`Пример:Имя пользователя. |  |
| last_nametype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |  |
| emailtype_string type_strictly_required | `johndoe@example.com`Пример:Адрес электронной почты пользователя. |  |
| paymenttype_object type_strictly_required | amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.dita`example_amount`Пример: example_amount_in_majors                                     example_currency передается какСумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| currencytype_string type_strictly_required | `example_currency`Пример:Код валюты платежа в формате ISO-4217 alpha-3. |  |
| etokentype_object type_strictly_required | tokentype_string type_strictly_required | `"{\"paymentData\":{...},\"paymentMethod\":{...},\"transactionIdentifier\":\"...\"}"`Пример:Токен, полученный от title после идентификации пользователя. |
| recurringtype_object type_optional | registertype_boolean type_optional | `true`ru_gate_payment_recurring_registration.dita`true`Пример:Признак регистрации повторяемой оплаты, необходимо использовать значение. Подробнее о регистрации повторяемой оплаты с использованием Gate, см. |
| typetype_string type_optional | `RCU`ru_gate__saved_cards_payments_type.dita`R`Пример:Тип повторяемой оплаты:(для регулярной оплаты),(для экспресс-оплаты), или(для автооплаты), подробную информацию см. в разделе. |  |
| periodtype_string type_optional | `D`— ежедневно,`W`— еженедельно,`M`— ежемесячно,`Q`— ежеквартально,`Y`— ежегодно.`W`Пример:Период выполнения регулярной оплаты. Возможные варианты: |  |
| timetype_string type_optional | `hh:mm:ss (UTC0)``10:00:00`Пример:Время выполнения регулярной оплаты в формате. |  |
| scheduled_payment_idtype_integer type_optional | `567890`Пример:Идентификатор следующей повторяемой оплаты. |  |
| start_datetype_string type_optional | `ДД-ММ-ГГГГ``10-10-2020`Пример:Дата начала регулярной оплаты, в формате(должна быть как минимум на один день позже, чем дата регистрации). |  |
| intervaltype_integer type_optional | `periodWinterval31100``3`Пример:Интервал выполнения регулярной оплаты. Множитель для кратного увеличения периода списаний, например чтобы списания выполнялись раз в три недели, в параметренадо задать значение, а в параметрезначение; возможные значения: отдо. |  |
| expiry_yeartype_integer type_optional | `2025`Пример:Год окончания действия повторяемой оплаты |  |
| expiry_monthtype_integer type_optional | `5`Пример:Месяц окончания действия повторяемой оплаты. |  |
| expiry_daytype_integer type_optional | `5`Пример:День окончания действия повторяемой оплаты. |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Оповещение (callback)

ru_gate_callbacks.ditaВ методе title результат оплаты платежная платформа возвращает в оповещении. Подробнее о структуре оповещений см..

idrecurringПри успешной регистрации повторяемой оплаты оповещение будет содержать идентификатор зарегистрированной оплаты в параметреобъекта.

`123example_amount_in_majors&#160;example_currency`В следующем примере оповещение свидетельствует о том, что в рамках проектабыла успешно проведена оплата в размере.

В следующем примере оплата была отклонена из-за недостатка средств на счете карты.

### Дополнительные материалы

Для организации работы с оплатами через Gate также могут быть полезны следующие материалы:

- ru_gate_interaction_scheme.dita
- ru_gate_authentication.dita
- ru_gate_payment_model.dita
- ru_gate_statuses_and_response_codes.dita

---

## Возврат через Gate

### Общая информация

Чтобы выполнить возврат ранее выполненной оплаты через Gate с использованием метода title:

Полная схема возврата по оплате с использованием метода title выглядит следующим образом.

ru_gate_sts_protocol.ditaИнформация о формате запросов и параметрах инициализации возвратов методом title через Gate, а также о формате оповещений о результатах возвратов приведена далее, общая информация о работе с API см. в разделе.

1. Отправьте запрос с нужными параметрами и подписью на рабочий URL-адрес платежной платформы Some Platform.
2. Примите от платежной платформы Some Platform оповещение (callback) с результатом возврата.

### Запрос на возврат

Далее представлена информация, необходимая для создания и отправки запроса на возврат с использованием метода title.

Вот пример данных из запроса на возврат с использованием метода title:

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/applepay/refund` |
| Полная спецификация конечной точки |  |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| customertype_object type_strictly_required | ip_addresstype_string type_strictly_required | `198.51.100.47`Пример:IP-адрес устройства пользователя. |
| emailtype_string type_strictly_required | `johndoe@example.com`Пример:Адрес электронной почты пользователя. |  |
| first_nametype_string type_strictly_required | `John`Пример:Имя пользователя. |  |
| last_nametype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |  |
| paymenttype_object type_strictly_required | descriptiontype_string type_strictly_required | `refund_02`Пример:Комментарий или описание возврата. |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Оповещение (callback)

ru_gate_callbacks.ditaДля оповещений о результатах возврата с применением метода title используется стандартный формат, описание которого представлено в разделе.

`123example_amount_in_majors&#160;example_currency`В следующем примере оповещение свидетельствует о том, что в рамках проектадля пользователя был успешно проведен возврат в размере.

В следующем примере возврат был отклонен, так как значение суммы в запросе на возврат больше суммы, указанной в начальном запросе.

### Дополнительные материалы

Для организации работы с возвратами через Gate также могут быть полезны следующие материалы:

- ru_gate_interaction_organisation.dita
- ru_gate_authentication.dita
- ru_gate_payment_model.dita
- ru_gate_statuses_and_response_codes.dita

---

## Повторяемая оплата через Gate

В следующем примере оповещение свидетельствует о том, что проведение повторяемой оплаты успешно отменено.

В следующем примере оповещение свидетельствует о том, что проведение повторяемой оплаты было отклонено.

### Общая информация

ru_gate_recurring_model.ditaПлатежный метод title предоставляет возможность проводить повторяемые оплаты — повторяемые оплаты со списаниями по запросу. Подробная информация представлена в разделе.

**Формат запроса**В этом разделе содержится информация о проведении повторных списаний в рамках уже зарегистрированной повторяемой оплаты. Информация о том, как зарегистрировать повторяемую оплату, приведена в секциив разделах о проведении оплат в одну и две стадии.

При выполнении повторного списания в рамках уже зарегистрированной повторяемой оплаты через Gate с использованием метода title ваша система должена выполнить следующее:

Полная схема проведения повторного списания в рамках уже зарегистрированной повторяемой оплаты выглядит следующим образом.

ru_gate_sts_protocol.ditaИнформация о формате запросов и параметрах инициирования повторного списания методом title через Gate, а также о формате оповещений о результатах списания приведена далее. Общую информацию о работе с Gate API можно найти в разделе.

1. Отправить запрос с нужными параметрами и подписью на рабочий URL-адрес Some Platform для проведения повторного списания.
2. Принять от платежной платформы Some Platform оповещение о результате повторного списания.

### Запрос на повторяемую оплату

Далее представлена информация, необходимая для создания и отправки запроса на повторяемую оплату с использованием метода title.

Вот пример запроса на проведение повторяемой оплаты.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/applepay/recurring` |
| Полная спецификация конечной точки | request-payment-applepay-recurring |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| customertype_object type_strictly_required | idtype_string type_strictly_required | `customer_123`Пример:Идентификатор пользователя, уникальный в рамках проекта. |
| ip_addresstype_string type_strictly_required | `198.51.100.47`Пример:IP-адрес устройства пользователя. |  |
| emailtype_string type_strictly_required | `johndoe@example.com`Пример:Адрес электронной почты пользователя. |  |
| first_nametype_string type_strictly_required | `John`Пример:Имя пользователя. |  |
| last_nametype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |  |
| paymenttype_object type_strictly_required | amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.dita`example_amount`Пример: example_amount_in_majors                                     example_currency передается какСумма платежа в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Сумма платежа должна быть целочисленной, иначе платеж будет отклонен. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| currencytype_string type_strictly_required | `example_currency`Пример:Код валюты платежа в формате ISO-4217 alpha-3. |  |
| recurringtype_object type_strictly_required | idtype_string type_strictly_required | `12345`Пример:Идентификатор зарегистрированной повторяемой оплаты. |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Запрос на отмену повторяемой оплаты

До момента истечения срока выполнения повторяемых оплат их можно отменить. Также для изменения условий выполнения повторяемой оплаты ее необходимо отменить и отправить запрос на создание новой.

Далее представлена информация, необходимая для создания и отправки запроса на отмену повторяемой оплаты с использованием метода title.

Корректный запрос на отмену проведения повторямой оплаты с применением метода title должен содержать идентификаторы проекта и платежа, подпись, а также идентификатор зарегистрированной повторяемой оплаты.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/applepay/recurring/cancel` |
| Полная спецификация конечной точки | request-payment-applepay-recurring-cancel |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| recurringtype_object type_strictly_required | idtype_string type_strictly_required | `12345`Пример:Идентификатор зарегистрированной повторяемой оплаты. |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Оповещение (callback)

ru_gate_callbacks.ditaДля оповещений о результатах действий с повторяемыми оплатами с применением метода title используется стандартный формат, описание которого представлено в разделе.

`123`В следующем примере оповещение свидетельствует о том, что в рамках проектадля пользователя была успешно зарегистрирована повторяемая оплата.

### Дополнительные материалы

Для организации работы с оплатами через Gate также могут быть полезны следующие материалы:

- ru_gate_interaction_organisation.dita
- ru_gate_authentication.dita
- ru_gate_payment_model.dita
- ru_gate_statuses_and_response_codes.dita

---

## Оплата в две стадии через Payment Widget

### Общая информация

ru_pm_applepay_gate_purchase_dms.dita#ru_pm_applepay_gate_purchase_dms/section_cq2_1sn_nkbПри выполнении оплаты в две стадии с использованием метода title при использовании Payment Widget возможно выполнение только первой стадии оплаты — блокировки средств на счете пользователя. Для списания заблокированных средств нужно отправить отдельный запрос с использованием Gate API. Подробнее о запросе на списание заблокированных средств см..

При блокировке средств через Payment Widget веб-сервис отправляет запрос, содержащий требуемые параметры и подпись, на рабочий URL-адрес Some Platform и принимает оповещение о результате блокировки средств.

Полная схема выполнения блокировки средств выглядит следующим образом.

ru_pp_api.ditaИнформация о формате запросов и параметрах вызова Payment Widget при работе с методом title, а также о формате оповещений о результатах блокировки средств приведена далее. Общую информацию о работе с Payment Widget API можно найти в разделе.

### Запрос на открытие платежной формы

В запросе на открытие платежной формы c использованием метода title укажите необходимые параметры:

`EPayWidget`Вот пример параметров из запроса на открытие платежной формы с использованием виджета:

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе.type_optional — параметр необязателен, но в общем случае его наличие в запросе способствует оптимизации проведения платежа, например упрощает процесс платежа для пользователя или повышает процент успешных платежей. |  |
| --- | --- |
| Параметр | Описание |
| project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |
| customer_idtype_string type_strictly_required | `customer_123`Пример:Идентификатор пользователя, уникальный в рамках проекта. |
| customer_first_nametype_string type_strictly_required | `John`Пример:Имя пользователя. |
| customer_last_nametype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |
| customer_emailtype_string type_strictly_required | `johndoe@example.com`Пример:Адрес электронной почты пользователя. |
| card_holdertype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |
| payment_currencytype_string type_strictly_required | `example_currency`Пример:Код валюты оплаты в формате ISO-4217 alpha-3. |
| payment_amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.dita`example_amount`Пример: example_amount_in_majors                                     example_currency передается какСумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| signaturetype_string type_strictly_required | ru_pp_authentication.ditaПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |
| force_payment_methodtype_string type_optional | force_payment_methodЧтобы пропустить страницу выбора метода и принудительно выбрать title в качестве платежного метода, добавьте в запрос параметрсо значением:ru_pp__preselectingps.ditaВ данном случае оба платежных метода доступны пользователю. Подробнее о предварительном выборе метода см..`force_payment_method: 'force_pm'`Пример:`apple_pay_core`— для открытия Payment Widget с предварительно выбранным платежным методом title;`card`— для открытия Payment Widget с возможностью выбрать оплату с использованием платежной карты, подключенной к сервису title.Параметр, позволяющий пропустить страницу выбора метода и принудительно выбрать title в качестве платежного метода. |
| recurringtype_string type_optional | {"register":true}ru_pp_recurring.ditaПодробнее о регистрации повторяемой оплаты с использованием Payment Widget см..Параметр, необходимый для регистрации повторяемой оплаты. Содержит в себе строку, полученную в результате кодирования с помощью URL-encoding JSON-объекта с набором параметров повторяемой оплаты. Пример такого JSON-объекта: |
| ru_pp_parameters.ditaПри необходимости добавьте в запрос необязательные параметры из числа доступных для работы с Payment Widget. Подробнее о параметрах запросов в Payment Widget см.. |  |

### Оповещение (callback)

ru_pp_callbacks.ditaВ методе title результат оплаты платежная платформа возвращает в оповещении. Подробнее о структуре оповещений см..

idrecurringПри успешной регистрации повторяемой оплаты оповещение будет содержать идентификатор зарегистрированной оплаты в параметреобъекта.

`123example_amount_in_majors&#160;example_currency`В следующем примере в оповещении содержится информация о том, что в проектевыполнена блокировка средств в размере.

Вот пример тела оповещения с информацией об отклонении запроса на блокировку средств.

### Дополнительные материалы

- ru_pp_interaction_organisation.dita
- ru_pp_payment_model.dita
- ru_pp_authentication.dita
- ru_pp_statuses_and_response_codes.dita

---

## Оплата в одну стадию через Payment Widget

### Общая информация

Чтобы выполнить оплату через Payment Widget с использованием метода title:

Полная схема оплаты через Payment Widget выглядит следующим образом.

ru_pp_api.ditaИнформация о формате запросов и параметрах вызова Payment Widget при работе с title, а также о формате оповещений о результатах оплат приведена далее. Общую информацию о работе с Payment Widget API можно найти в разделе.

1. Отправьте запрос с нужными параметрами и подписью на рабочий URL-адрес платежной платформы Some Platform.
2. Примите от платежной платформы Some Platform оповещение (callback) с результатом оплаты.

### Запрос на открытие платежной формы

В запросе на открытие платежной формы c использованием метода title укажите необходимые параметры:

`EPayWidget`Вот пример параметров из запроса на открытие платежной формы с использованием виджета:

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе.type_optional — параметр необязателен, но в общем случае его наличие в запросе способствует оптимизации проведения платежа, например упрощает процесс платежа для пользователя или повышает процент успешных платежей. |  |
| --- | --- |
| Параметр | Описание |
| project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |
| customer_idtype_string type_strictly_required | `customer_123`Пример:Идентификатор пользователя, уникальный в рамках проекта. |
| customer_first_nametype_string type_strictly_required | `John`Пример:Имя пользователя. |
| customer_last_nametype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |
| customer_emailtype_string type_strictly_required | `johndoe@example.com`Пример:Адрес электронной почты пользователя. |
| card_holdertype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |
| payment_currencytype_string type_strictly_required | `example_currency`Пример:Код валюты оплаты в формате ISO-4217 alpha-3. |
| payment_amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.dita`example_amount`Пример: example_amount_in_majors                                     example_currency передается какСумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| signaturetype_string type_strictly_required | ru_pp_authentication.ditaПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |
| force_payment_methodtype_string type_optional | force_payment_methodЧтобы пропустить страницу выбора метода и принудительно выбрать title в качестве платежного метода, добавьте в запрос параметрсо значением:ru_pp__preselectingps.ditaВ данном случае оба платежных метода доступны пользователю. Подробнее о предварительном выборе метода см..`force_payment_method: 'force_pm'`Пример:`apple_pay_core`— для открытия Payment Widget с предварительно выбранным платежным методом title;`card`— для открытия Payment Widget с возможностью выбрать оплату с использованием платежной карты, подключенной к сервису title.Параметр, позволяющий пропустить страницу выбора метода и принудительно выбрать title в качестве платежного метода. |
| recurringtype_string type_optional | recurring{"register":true}ru_pp_recurring.ditaПодробнее о регистрации повторяемой оплаты с использованием Payment Widget см..Для регистрации повторяемой оплаты добавьте в запрос параметр, содержащий в себе строку, полученную в результате кодирования с помощью URL-encoding JSON-объекта с набором параметров повторяемой оплаты. Пример такого JSON-объекта (до кодирования): |
| ru_pp_parameters.ditaПри необходимости добавьте в запрос необязательные параметры из числа доступных для работы с Payment Widget. Подробнее о параметрах запросов в Payment Widget см.. |  |

### Оповещение (callback)

ru_pp_callbacks.ditaВ методе title результат оплаты платежная платформа возвращает в оповещении. Подробнее о структуре оповещений см..

idrecurringПри успешной регистрации повторяемой оплаты оповещение будет содержать идентификатор зарегистрированной оплаты в параметреобъекта.

`123example_amount_in_majors&#160;example_currency`В следующем примере оповещение свидетельствует о том, что в рамках проектабыла успешно проведена оплата в размере.

Пример тела оповещения с информацией об отклоненной оплате.

### Дополнительные материалы

Для организации работы с оплатами через Payment Widget также могут быть полезны следующие материалы:

- ru_pp_interaction_organisation.dita
- ru_pp_authentication.dita
- ru_pp_payment_model.dita
- ru_pp_statuses_and_response_codes.dita

---

## Проверка действительности карты

### Общая информация

ru_gate_account_verification.ditaДля проверки действительности карты через Gate с использованием title метода со стороны веб-сервиса необходимо отправить запрос, содержащий требуемые параметры и подпись, на рабочий URL Some Platform и принять оповещение о результате. Схема проведения проверки действительности карты через сервис title аналогична стандартной проверке, описание которой представлено в разделе.

### Запрос на проверку действительности карты

Далее представлена информация, необходимая для проверки действительности карты с использованием метода title.

Пример запроса на проверку действительности карты с помощью метода title.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/applepay/account_verification` |
| Полная спецификация конечной точки |  |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| customertype_object type_strictly_required | ip_addresstype_string type_strictly_required | `198.51.100.47`Пример:IP-адрес устройства пользователя. |
| paymenttype_object type_strictly_required | amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.dita`example_amount`Пример: example_amount_in_majors                                     example_currency передается какСумма платежа в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| currencytype_string type_strictly_required | `example_currency`Пример:Код валюты платежа в формате ISO-4217 alpha-3. |  |
| etokentype_object type_strictly_required | tokentype_string type_strictly_required | Пример:`"{\"paymentData\":{...},\"paymentMethod\":{...},\"transactionIdentifier\":\"...\"}"`Пример:Токен, полученный от title после идентификации пользователя. |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Оповещение (callback)

ru_gate_account_verification.ditaДля оповещений о результатах проведения проверки действительности карты используется стандартный формат, описание которого представлено в разделе.

### Дополнительные материалы

Для организации работы с операциями через Gate также могут быть полезны следующие материалы:

- ru_gate_interaction_organisation.dita
- ru_gate_authentication.dita
- ru_gate_payment_model.dita
- ru_gate_statuses_and_response_codes.dita

### Тестирование

#### Общая информация

Для метода title доступно тестирование оплат в одну и две стадии через Payment Widget и Gate. Также доступно тестирование проверки действительности карты через Gate. При тестировании оплат через Gate веб-сервис мерчанта должен соответствовать следующим условиям:

ru_pm_applepay_overview.ditaТакже при тестировании следует учитывать другие особенности работы с методом title, указанные в разделе.

Тестирование может выполняться в рамках тестового проекта, для подключения и отключения этой функциональности необходимо обращаться к специалистам технической поддержки Some Platform
                support@example.com

При проведении тестовых платежей следует учитывать, что в запросах должен указываться идентификатор тестового проекта, а интерфейсы эмулятора платежных форм Payment Widget и страницы аутентификации 3ds могут отличаться от рабочих. Вместе с тем для проведения тестовых платежей необходимо использовать действительные платежные карты, предварительно зарегистрированные в сервисе Apple. Это никак не влияет на проведение реальных платежей с использованием этих карт, но позволяет выполнять полноценное тестирование.

- Домен сайта зарегистрирован в сервисах Apple. Для регистрации домена необходимо обратиться в службу технической поддержки Some Platform
                    support@example.com
- [документации](https://developer.apple.com/documentation/apple_pay_on_the_web)Сайт поддерживает функциональность запуска скрипта для начала сессии Apple согласно.

#### Статусы тестовых платежей

При тестировании оплат в одну и две стадии их итоговые статусы определяются исходя из сумм, указанных в запросах:

При тестировании проверки действительности карты статус этой проверки определяется исходя из сумм, указанных в запросах:

#### Оплата в одну стадию через Payment Widget

Для проведения тестовой оплаты в одну стадию через Payment Widget необходимо:

ru_pm_applepay_pp_purchase.ditaПодробная информация о проведении оплат с использованием метода title через Payment Widget представлена в пункте.

1. Отправить в платежную платформу корректный тестовый запрос на открытие Payment Widget.
2. `force_pm`Если в запросе не был указан метод— выбрать метод title на странице эмулятора.
3. Подтвердить оплату.
4. Принять оповещение с информацией о результате оплаты.

#### Оплата в одну стадию через Gate

Для проведения тестовой оплаты через Gate необходимо:

ru_pm_applepay_gate_purchase.ditaПодробная информация о проведении оплат с использованием метода title через Gate представлена в пункте.

1. Отправить в платежную платформу корректный тестовый запрос на создание платежной сессии.
2. Принять данные платежной сессии и пользователя.
3. Подтвердить оплату.
4. Отправить в платежную платформу корректный тестовый запрос на оплату.
5. Принять оповещение с информацией о результате оплаты.

#### Оплата в две стадии с блокировкой средств через Payment Widget и подтверждением или отменой через Gate

Для проведения тестовой оплаты в две стадии с блокировкой через Payment Widget и подтверждением или отменой через Gate необходимо:

ru_pm_applepay_pp_purchase.ditaru_pm_applepay_gate_purchase.ditaПодробная информация о проведении оплат с использованием метода title через Payment Widget и Gate представлена в пунктахи.

1. Отправить в платежную платформу корректный тестовый запрос на открытие Payment Widget.
2. `cardforce_pm`Если в запросе не был указан код предварительного выбора методаили— выбрать оплату методом title на странице эмулятора.
3. Подтвердить оплату.
4. Принять оповещение с информацией о результате блокировки.
5. Отправить запрос на подтверждение или отмену оплаты.
6. Принять оповещение с информацией о результате оплаты.

#### Оплата в две стадии с блокировкой средств и подтверждением или отменой через Gate

Для проведения тестовой оплаты в две стадии с блокировкой и подтверждением или отменой через Gate необходимо:

ru_pm_applepay_gate_purchase.ditaПодробная информация о проведении оплат с использованием метода title через Gate представлена в пункте.

1. Отправить в платежную платформу корректный тестовый запрос на создание платежной сессии.
2. Принять данные платежной сессии и пользователя.
3. Отправить в платежную платформу корректный тестовый запрос на оплату.
4. Подтвердить оплату.
5. Принять оповещение с информацией о результате блокировки.
6. Отправить запрос на подтверждение или отмену оплаты.
7. Принять оповещение с информацией о результате оплаты.

#### Проверка действительности карты через Gate

Для проведения тестовой проверки действительности карты через Gate необходимо отправить в платежную платформу корректный тестовый запрос на проверку действительности и принять оповещение с информацией о результате проверки.

ru_pm_applepay_acc_ver.ditaПодробная информация о проведении проверки действительности карты с использованием метода title через Gate представлена в пункте.

---

## Оплата в две стадии через Gate

### Общая информация

При выполнении оплаты в две стадии через Gate с использованием метода title веб-сервис должен выполнить следующее:

Проведение оплаты в две стадии выполняется поэтапно:

Полная схема оплаты с использованием метода title выглядит следующим образом.

ru_gate_sts_protocol.ditaИнформация о форматах запросов и оповещений, используемых при проведении оплаты в две стадии с использованием метода title через Gate приведена далее. Общую информацию о работе с Gate API можно найти в разделе.

::: tip
Для выполнения оплат через Gate настройте взаимодействие вашего веб-сервиса с сервисом provider_service.
:::

### Запрос на создание платежной сессии

Далее представлена информация, необходимая для создания платежной сессии с использованием метода title.

Пример корректного запроса на создание платежной сессии с title представлен далее.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/session/applepay` |
| Полная спецификация конечной точки |  |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| validation_urltype_string type_strictly_required | `apple-pay-gateway.apple.com/paymentservices/startSession`Пример:URL для проверки. |  |
| domain_nametype_string type_strictly_required | `appay.en.ngrok.io`Пример:Доменное имя вашего веб-сервиса. |  |
| display_nametype_string type_strictly_required | `test payment`Пример:Название проекта для отображения, не более 64 символов. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Запрос на блокировку средств

Далее представлена информация, необходимая для создания и отправки запроса на блокировку средств с использованием метода title.

Вот пример данных из запроса на блокировку средств с использованием метода title:

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/applepay/auth` |
| Полная спецификация конечной точки |  |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе.type_optional — параметр необязателен, но в общем случае его наличие в запросе способствует оптимизации проведения платежа, например упрощает процесс платежа для пользователя или повышает процент успешных платежей. |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| customertype_object type_strictly_required | idtype_string type_strictly_required | `customer_123`Пример:Идентификатор пользователя, уникальный в рамках проекта. |
| ip_addresstype_string type_strictly_required | `198.51.100.47`Пример:IP-адрес устройства пользователя. |  |
| emailtype_string type_strictly_required | `johndoe@example.com`Пример:Адрес электронной почты пользователя. |  |
| first_nametype_string type_strictly_required | `John`Пример:Имя пользователя. |  |
| last_nametype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |  |
| paymenttype_object type_strictly_required | amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.dita`example_amount`Пример: example_amount_in_majors                                     example_currency передается какСумма платежа в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| currencytype_string type_strictly_required | `example_currency`Пример:Код валюты платежа в формате ISO-4217 alpha-3. |  |
| etokentype_object type_strictly_required | tokentype_string type_strictly_required | `"{\"paymentData\":{...},\"paymentMethod\":{...},\"transactionIdentifier\":\"...\"}"`Пример:Токен, полученный от title после идентификации пользователя. |
| recurringtype_object type_optional | registertype_boolean type_optional | `true`ru_gate_payment_recurring_registration.ditaПодробнее о регистрации повторяемой оплаты с использованием Gate, см.`true`Пример:Признак регистрации повторяемой оплаты, необходимо использовать значение. |
| typetype_string type_optional | `RCU`ru_gate__saved_cards_payments_type.dita`R`Пример:Тип повторяемой оплаты:(для регулярной оплаты),(для экспресс-оплаты), или(для автооплаты), подробную информацию см. в разделе. |  |
| periodtype_string type_optional | `D`— ежедневно,`W`— еженедельно,`M`— ежемесячно,`Q`— ежеквартально,`Y`— ежегодно.`W`Пример:Период выполнения регулярной оплаты. Возможные варианты: |  |
| timetype_string type_optional | `hh:mm:ss (UTC0)``10:00:00`Пример:Время выполнения регулярной оплаты в формате. |  |
| scheduled_payment_idtype_integer type_optional | `567890`Пример:Идентификатор следующей повторяемой оплаты. |  |
| start_datetype_string type_optional | `ДД-ММ-ГГГГ``10-10-2020`Пример:Дата начала регулярной оплаты, в формате(должна быть как минимум на один день позже, чем дата регистрации). |  |
| intervaltype_integer type_optional | `periodWinterval31100``3`Пример:Интервал выполнения регулярной оплаты. Множитель для кратного увеличения периода списаний, например чтобы списания выполнялись раз в три недели, в параметренадо задать значение, а в параметрезначение; возможные значения: отдо. |  |
| expiry_yeartype_integer type_optional | `2025`Пример:Год окончания действия повторяемой оплаты |  |
| expiry_monthtype_integer type_optional | `5`Пример:Месяц окончания действия повторяемой оплаты. |  |
| expiry_daytype_integer type_optional | `5`Пример:День окончания действия повторяемой оплаты. |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Запрос на списание заблокированных средств

Далее представлена информация, необходимая для создания запроса на списание заблокированных средств с использованием метода title.

Вот пример данных из запроса на списание заблокированных средств с использованием метода title:

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/applepay/capture` |
| Полная спецификация конечной точки |  |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| paymenttype_object type_strictly_required | amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.dita`example_amount`Пример: example_amount_in_majors                                     example_currency передается какСумма платежа в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Сумма платежа должна совпадать со значением, переданным в запросе на авторизацию. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| currencytype_string type_strictly_required | `example_currency`Пример:Код валюты платежа в формате ISO-4217 alpha-3. Валюта платежа должна совпадать со значением, переданным в запросе на авторизацию. |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Запрос на отмену блокировки средств

Далее представлена информация, необходимая для создания запроса на отмену блокировки средств с использованием метода title.

Пример запроса на отмену блокировки средств с применением метода title.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/applepay/cancel` |
| Полная спецификация конечной точки |  |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Оповещение (callback)

ru_gate_callbacks.ditaВ методе title результат оплаты в две стадии платежная платформа возвращает в оповещении. Подробнее о структуре оповещений см..

idrecurringПри успешной регистрации повторяемой оплаты оповещение будет содержать идентификатор зарегистрированной оплаты в параметреобъекта.

`123example_amount_in_majors&#160;example_currency`В следующем примере в оповещении содержится информация о том, что в рамках проектапроизведена блокировка средств в размере.

В следующем примере содержится информация об отклонении блокировки средств.

`123`В следующем примере в оповещении содержится информация о том, что в рамках проектапроизведено списание ранее заблокированных средств.

В следующем примере содержится информация об отклонении списания средств.

В следующем примере в оповещении содержится информация об отмене блокировки средств.

В следующем примере содержится информация об отклонении отмены блокировки средств.

### Дополнительные материалы

- ru_gate_interaction_organisation.dita
- ru_gate_payment_model.dita
- ru_gate_authentication.dita
- ru_gate_statuses_and_response_codes.dita

---

## Оплата в одну стадию через Gate

### Общая информация

Для оплаты через Gate с использованием метода provider_service со стороны веб-сервиса необходимо:

Полная схема оплаты в одну стадию с использованием метода title выглядит следующим образом.

ru_gate_sts_protocol.ditaИнформация о формате запросов и параметрах инициализации оплат методом title через Gate, а также о формате оповещений о результатах оплат приведена далее, общая информация о работе с API см. в разделе.

1. Отправить запрос на создание платежной сессии, содержащий требуемые параметры и подпись, на рабочий URL Some Platform.
2. Принять данные платежной сессии и пользователя.
3. Отправить запрос оплату, содержащий требуемые параметры и подпись, на рабочий URL Some Platform.
4. Принять от платежной платформы Some Platform оповещение (callback) о результате оплаты.

### Запрос на создание платежной сессии

Далее представлена информация, необходимая для создания платежной сессии с использованием метода title.

Пример корректного запроса на создание платежной сессии с title представлен далее.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/session/applepay` |
| Полная спецификация конечной точки |  |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| validation_urltype_string type_strictly_required | `apple-pay-gateway.apple.com/paymentservices/startSession`Пример:URL для проверки. |  |
| domain_nametype_string type_strictly_required | `appay.en.ngrok.io`Пример:Доменное имя вашего веб-сервиса. |  |
| display_nametype_string type_strictly_required | `test payment`Пример:Название проекта для отображения, не более 64 символов. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Запрос на проведение оплаты

Далее представлена информация, необходимая для создания и отправки запроса на оплату с использованием метода title.

Вот пример данных из запроса на оплату с использованием метода title:

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/applepay/sale` |
| Полная спецификация конечной точки |  |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе.type_optional — параметр необязателен, но в общем случае его наличие в запросе способствует оптимизации проведения платежа, например упрощает процесс платежа для пользователя или повышает процент успешных платежей. |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| customertype_object type_strictly_required | idtype_string type_strictly_required | `customer_123`Пример:Идентификатор пользователя, уникальный в рамках проекта. |
| ip_addresstype_string type_strictly_required | `198.51.100.47`Пример:IP-адрес устройства пользователя. |  |
| first_nametype_string type_strictly_required | `John`Пример:Имя пользователя. |  |
| last_nametype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |  |
| emailtype_string type_strictly_required | `johndoe@example.com`Пример:Адрес электронной почты пользователя. |  |
| paymenttype_object type_strictly_required | amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.dita`example_amount`Пример: example_amount_in_majors                                     example_currency передается какСумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| currencytype_string type_strictly_required | `example_currency`Пример:Код валюты платежа в формате ISO-4217 alpha-3. |  |
| etokentype_object type_strictly_required | tokentype_string type_strictly_required | `"{\"paymentData\":{...},\"paymentMethod\":{...},\"transactionIdentifier\":\"...\"}"`Пример:Токен, полученный от title после идентификации пользователя. |
| recurringtype_object type_optional | registertype_boolean type_optional | `true`ru_gate_payment_recurring_registration.dita`true`Пример:Признак регистрации повторяемой оплаты, необходимо использовать значение. Подробнее о регистрации повторяемой оплаты с использованием Gate, см. |
| typetype_string type_optional | `RCU`ru_gate__saved_cards_payments_type.dita`R`Пример:Тип повторяемой оплаты:(для регулярной оплаты),(для экспресс-оплаты), или(для автооплаты), подробную информацию см. в разделе. |  |
| periodtype_string type_optional | `D`— ежедневно,`W`— еженедельно,`M`— ежемесячно,`Q`— ежеквартально,`Y`— ежегодно.`W`Пример:Период выполнения регулярной оплаты. Возможные варианты: |  |
| timetype_string type_optional | `hh:mm:ss (UTC0)``10:00:00`Пример:Время выполнения регулярной оплаты в формате. |  |
| scheduled_payment_idtype_integer type_optional | `567890`Пример:Идентификатор следующей повторяемой оплаты. |  |
| start_datetype_string type_optional | `ДД-ММ-ГГГГ``10-10-2020`Пример:Дата начала регулярной оплаты, в формате(должна быть как минимум на один день позже, чем дата регистрации). |  |
| intervaltype_integer type_optional | `periodWinterval31100``3`Пример:Интервал выполнения регулярной оплаты. Множитель для кратного увеличения периода списаний, например чтобы списания выполнялись раз в три недели, в параметренадо задать значение, а в параметрезначение; возможные значения: отдо. |  |
| expiry_yeartype_integer type_optional | `2025`Пример:Год окончания действия повторяемой оплаты |  |
| expiry_monthtype_integer type_optional | `5`Пример:Месяц окончания действия повторяемой оплаты. |  |
| expiry_daytype_integer type_optional | `5`Пример:День окончания действия повторяемой оплаты. |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Оповещение (callback)

ru_gate_callbacks.ditaВ методе title результат оплаты платежная платформа возвращает в оповещении. Подробнее о структуре оповещений см..

idrecurringПри успешной регистрации повторяемой оплаты оповещение будет содержать идентификатор зарегистрированной оплаты в параметреобъекта.

`123example_amount_in_majors&#160;example_currency`В следующем примере оповещение свидетельствует о том, что в рамках проектабыла успешно проведена оплата в размере.

В следующем примере оплата была отклонена из-за недостатка средств на счете карты.

### Дополнительные материалы

Для организации работы с оплатами через Gate также могут быть полезны следующие материалы:

- ru_gate_interaction_scheme.dita
- ru_gate_authentication.dita
- ru_gate_payment_model.dita
- ru_gate_statuses_and_response_codes.dita

---

## Повторяемая оплата через Gate

В следующем примере оповещение свидетельствует о том, что проведение повторяемой оплаты успешно отменено.

В следующем примере оповещение свидетельствует о том, что проведение повторяемой оплаты было отклонено.

### Общая информация

ru_gate_recurring_model.ditaПлатежный метод title предоставляет возможность проводить повторяемые оплаты — повторяемые оплаты со списаниями по запросу. Подробная информация представлена в разделе.

**Формат запроса**В этом разделе содержится информация о проведении повторных списаний в рамках уже зарегистрированной повторяемой оплаты. Информация о том, как зарегистрировать повторяемую оплату, приведена в секциив разделах о проведении оплат в одну и две стадии.

При выполнении повторного списания в рамках уже зарегистрированной повторяемой оплаты через Gate с использованием метода title ваша система должена выполнить следующее:

Полная схема проведения повторного списания в рамках уже зарегистрированной повторяемой оплаты выглядит следующим образом.

ru_gate_sts_protocol.ditaИнформация о формате запросов и параметрах инициирования повторного списания методом title через Gate, а также о формате оповещений о результатах списания приведена далее. Общую информацию о работе с Gate API можно найти в разделе.

1. Отправить запрос с нужными параметрами и подписью на рабочий URL-адрес Some Platform для проведения повторного списания.
2. Принять от платежной платформы Some Platform оповещение о результате повторного списания.

### Запрос на повторяемую оплату

Далее представлена информация, необходимая для создания и отправки запроса на повторяемую оплату с использованием метода title.

Вот пример запроса на проведение повторяемой оплаты.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/applepay/recurring` |
| Полная спецификация конечной точки | request-payment-applepay-recurring |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| customertype_object type_strictly_required | idtype_string type_strictly_required | `customer_123`Пример:Идентификатор пользователя, уникальный в рамках проекта. |
| ip_addresstype_string type_strictly_required | `198.51.100.47`Пример:IP-адрес устройства пользователя. |  |
| emailtype_string type_strictly_required | `johndoe@example.com`Пример:Адрес электронной почты пользователя. |  |
| first_nametype_string type_strictly_required | `John`Пример:Имя пользователя. |  |
| last_nametype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |  |
| paymenttype_object type_strictly_required | amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.dita`example_amount`Пример: example_amount_in_majors                                     example_currency передается какСумма платежа в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Сумма платежа должна быть целочисленной, иначе платеж будет отклонен. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| currencytype_string type_strictly_required | `example_currency`Пример:Код валюты платежа в формате ISO-4217 alpha-3. |  |
| recurringtype_object type_strictly_required | idtype_string type_strictly_required | `12345`Пример:Идентификатор зарегистрированной повторяемой оплаты. |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Запрос на отмену повторяемой оплаты

До момента истечения срока выполнения повторяемых оплат их можно отменить. Также для изменения условий выполнения повторяемой оплаты ее необходимо отменить и отправить запрос на создание новой.

Далее представлена информация, необходимая для создания и отправки запроса на отмену повторяемой оплаты с использованием метода title.

Корректный запрос на отмену проведения повторямой оплаты с применением метода title должен содержать идентификаторы проекта и платежа, подпись, а также идентификатор зарегистрированной повторяемой оплаты.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/applepay/recurring/cancel` |
| Полная спецификация конечной точки | request-payment-applepay-recurring-cancel |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| recurringtype_object type_strictly_required | idtype_string type_strictly_required | `12345`Пример:Идентификатор зарегистрированной повторяемой оплаты. |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Оповещение (callback)

ru_gate_callbacks.ditaДля оповещений о результатах действий с повторяемыми оплатами с применением метода title используется стандартный формат, описание которого представлено в разделе.

`123`В следующем примере оповещение свидетельствует о том, что в рамках проектадля пользователя была успешно зарегистрирована повторяемая оплата.

### Дополнительные материалы

Для организации работы с оплатами через Gate также могут быть полезны следующие материалы:

- ru_gate_interaction_organisation.dita
- ru_gate_authentication.dita
- ru_gate_payment_model.dita
- ru_gate_statuses_and_response_codes.dita

---

## Возврат через Gate

### Общая информация

Чтобы выполнить возврат ранее выполненной оплаты через Gate с использованием метода title:

Полная схема возврата по оплате с использованием метода title выглядит следующим образом.

ru_gate_sts_protocol.ditaИнформация о формате запросов и параметрах инициализации возвратов методом title через Gate, а также о формате оповещений о результатах возвратов приведена далее, общая информация о работе с API см. в разделе.

1. Отправьте запрос с нужными параметрами и подписью на рабочий URL-адрес платежной платформы Some Platform.
2. Примите от платежной платформы Some Platform оповещение (callback) с результатом возврата.

### Запрос на возврат

Далее представлена информация, необходимая для создания и отправки запроса на возврат с использованием метода title.

Вот пример данных из запроса на возврат с использованием метода title:

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/applepay/refund` |
| Полная спецификация конечной точки |  |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе |  |  |
| --- | --- | --- |
| Объект | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| customertype_object type_strictly_required | ip_addresstype_string type_strictly_required | `198.51.100.47`Пример:IP-адрес устройства пользователя. |
| emailtype_string type_strictly_required | `johndoe@example.com`Пример:Адрес электронной почты пользователя. |  |
| first_nametype_string type_strictly_required | `John`Пример:Имя пользователя. |  |
| last_nametype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |  |
| paymenttype_object type_strictly_required | descriptiontype_string type_strictly_required | `refund_02`Пример:Комментарий или описание возврата. |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

### Оповещение (callback)

ru_gate_callbacks.ditaДля оповещений о результатах возврата с применением метода title используется стандартный формат, описание которого представлено в разделе.

`123example_amount_in_majors&#160;example_currency`В следующем примере оповещение свидетельствует о том, что в рамках проектадля пользователя был успешно проведен возврат в размере.

В следующем примере возврат был отклонен, так как значение суммы в запросе на возврат больше суммы, указанной в начальном запросе.

### Дополнительные материалы

Для организации работы с возвратами через Gate также могут быть полезны следующие материалы:

- ru_gate_interaction_organisation.dita
- ru_gate_authentication.dita
- ru_gate_payment_model.dita
- ru_gate_statuses_and_response_codes.dita

---

## Оплата в две стадии через Payment Widget

### Общая информация

ru_pm_applepay_gate_purchase_dms.dita#ru_pm_applepay_gate_purchase_dms/section_cq2_1sn_nkbПри выполнении оплаты в две стадии с использованием метода title при использовании Payment Widget возможно выполнение только первой стадии оплаты — блокировки средств на счете пользователя. Для списания заблокированных средств нужно отправить отдельный запрос с использованием Gate API. Подробнее о запросе на списание заблокированных средств см..

При блокировке средств через Payment Widget веб-сервис отправляет запрос, содержащий требуемые параметры и подпись, на рабочий URL-адрес Some Platform и принимает оповещение о результате блокировки средств.

Полная схема выполнения блокировки средств выглядит следующим образом.

ru_pp_api.ditaИнформация о формате запросов и параметрах вызова Payment Widget при работе с методом title, а также о формате оповещений о результатах блокировки средств приведена далее. Общую информацию о работе с Payment Widget API можно найти в разделе.

### Запрос на открытие платежной формы

В запросе на открытие платежной формы c использованием метода title укажите необходимые параметры:

`EPayWidget`Вот пример параметров из запроса на открытие платежной формы с использованием виджета:

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе.type_optional — параметр необязателен, но в общем случае его наличие в запросе способствует оптимизации проведения платежа, например упрощает процесс платежа для пользователя или повышает процент успешных платежей. |  |
| --- | --- |
| Параметр | Описание |
| project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |
| customer_idtype_string type_strictly_required | `customer_123`Пример:Идентификатор пользователя, уникальный в рамках проекта. |
| customer_first_nametype_string type_strictly_required | `John`Пример:Имя пользователя. |
| customer_last_nametype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |
| customer_emailtype_string type_strictly_required | `johndoe@example.com`Пример:Адрес электронной почты пользователя. |
| card_holdertype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |
| payment_currencytype_string type_strictly_required | `example_currency`Пример:Код валюты оплаты в формате ISO-4217 alpha-3. |
| payment_amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.dita`example_amount`Пример: example_amount_in_majors                                     example_currency передается какСумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| signaturetype_string type_strictly_required | ru_pp_authentication.ditaПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |
| force_payment_methodtype_string type_optional | force_payment_methodЧтобы пропустить страницу выбора метода и принудительно выбрать title в качестве платежного метода, добавьте в запрос параметрсо значением:ru_pp__preselectingps.ditaВ данном случае оба платежных метода доступны пользователю. Подробнее о предварительном выборе метода см..`force_payment_method: 'force_pm'`Пример:`apple_pay_core`— для открытия Payment Widget с предварительно выбранным платежным методом title;`card`— для открытия Payment Widget с возможностью выбрать оплату с использованием платежной карты, подключенной к сервису title.Параметр, позволяющий пропустить страницу выбора метода и принудительно выбрать title в качестве платежного метода. |
| recurringtype_string type_optional | {"register":true}ru_pp_recurring.ditaПодробнее о регистрации повторяемой оплаты с использованием Payment Widget см..Параметр, необходимый для регистрации повторяемой оплаты. Содержит в себе строку, полученную в результате кодирования с помощью URL-encoding JSON-объекта с набором параметров повторяемой оплаты. Пример такого JSON-объекта: |
| ru_pp_parameters.ditaПри необходимости добавьте в запрос необязательные параметры из числа доступных для работы с Payment Widget. Подробнее о параметрах запросов в Payment Widget см.. |  |

### Оповещение (callback)

ru_pp_callbacks.ditaВ методе title результат оплаты платежная платформа возвращает в оповещении. Подробнее о структуре оповещений см..

idrecurringПри успешной регистрации повторяемой оплаты оповещение будет содержать идентификатор зарегистрированной оплаты в параметреобъекта.

`123example_amount_in_majors&#160;example_currency`В следующем примере в оповещении содержится информация о том, что в проектевыполнена блокировка средств в размере.

Вот пример тела оповещения с информацией об отклонении запроса на блокировку средств.

### Дополнительные материалы

- ru_pp_interaction_organisation.dita
- ru_pp_payment_model.dita
- ru_pp_authentication.dita
- ru_pp_statuses_and_response_codes.dita

---

## Оплата в одну стадию через Payment Widget

### Общая информация

Чтобы выполнить оплату через Payment Widget с использованием метода title:

Полная схема оплаты через Payment Widget выглядит следующим образом.

ru_pp_api.ditaИнформация о формате запросов и параметрах вызова Payment Widget при работе с title, а также о формате оповещений о результатах оплат приведена далее. Общую информацию о работе с Payment Widget API можно найти в разделе.

1. Отправьте запрос с нужными параметрами и подписью на рабочий URL-адрес платежной платформы Some Platform.
2. Примите от платежной платформы Some Platform оповещение (callback) с результатом оплаты.

### Запрос на открытие платежной формы

В запросе на открытие платежной формы c использованием метода title укажите необходимые параметры:

`EPayWidget`Вот пример параметров из запроса на открытие платежной формы с использованием виджета:

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе.type_optional — параметр необязателен, но в общем случае его наличие в запросе способствует оптимизации проведения платежа, например упрощает процесс платежа для пользователя или повышает процент успешных платежей. |  |
| --- | --- |
| Параметр | Описание |
| project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |
| customer_idtype_string type_strictly_required | `customer_123`Пример:Идентификатор пользователя, уникальный в рамках проекта. |
| customer_first_nametype_string type_strictly_required | `John`Пример:Имя пользователя. |
| customer_last_nametype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |
| customer_emailtype_string type_strictly_required | `johndoe@example.com`Пример:Адрес электронной почты пользователя. |
| card_holdertype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |
| payment_currencytype_string type_strictly_required | `example_currency`Пример:Код валюты оплаты в формате ISO-4217 alpha-3. |
| payment_amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.dita`example_amount`Пример: example_amount_in_majors                                     example_currency передается какСумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| signaturetype_string type_strictly_required | ru_pp_authentication.ditaПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |
| force_payment_methodtype_string type_optional | force_payment_methodЧтобы пропустить страницу выбора метода и принудительно выбрать title в качестве платежного метода, добавьте в запрос параметрсо значением:ru_pp__preselectingps.ditaВ данном случае оба платежных метода доступны пользователю. Подробнее о предварительном выборе метода см..`force_payment_method: 'force_pm'`Пример:`apple_pay_core`— для открытия Payment Widget с предварительно выбранным платежным методом title;`card`— для открытия Payment Widget с возможностью выбрать оплату с использованием платежной карты, подключенной к сервису title.Параметр, позволяющий пропустить страницу выбора метода и принудительно выбрать title в качестве платежного метода. |
| recurringtype_string type_optional | recurring{"register":true}ru_pp_recurring.ditaПодробнее о регистрации повторяемой оплаты с использованием Payment Widget см..Для регистрации повторяемой оплаты добавьте в запрос параметр, содержащий в себе строку, полученную в результате кодирования с помощью URL-encoding JSON-объекта с набором параметров повторяемой оплаты. Пример такого JSON-объекта (до кодирования): |
| ru_pp_parameters.ditaПри необходимости добавьте в запрос необязательные параметры из числа доступных для работы с Payment Widget. Подробнее о параметрах запросов в Payment Widget см.. |  |

### Оповещение (callback)

ru_pp_callbacks.ditaВ методе title результат оплаты платежная платформа возвращает в оповещении. Подробнее о структуре оповещений см..

idrecurringПри успешной регистрации повторяемой оплаты оповещение будет содержать идентификатор зарегистрированной оплаты в параметреобъекта.

`123example_amount_in_majors&#160;example_currency`В следующем примере оповещение свидетельствует о том, что в рамках проектабыла успешно проведена оплата в размере.

Пример тела оповещения с информацией об отклоненной оплате.

### Дополнительные материалы

Для организации работы с оплатами через Payment Widget также могут быть полезны следующие материалы:

- ru_pp_interaction_organisation.dita
- ru_pp_authentication.dita
- ru_pp_payment_model.dita
- ru_pp_statuses_and_response_codes.dita

