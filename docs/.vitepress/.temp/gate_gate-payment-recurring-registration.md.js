import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Registering COF purchase","description":"","frontmatter":{},"headers":[],"relativePath":"gate/gate-payment-recurring-registration.md","filePath":"gate/gate-payment-recurring-registration.md"}');
const _sfc_main = { name: "gate/gate-payment-recurring-registration.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="registering-cof-purchase" tabindex="-1">Registering COF purchase <a class="header-anchor" href="#registering-cof-purchase" aria-label="Permalink to &quot;Registering COF purchase&quot;">​</a></h1><p>If there are changes in the payment provider&#39;s service settings, you may need to register COF purchase again. In such cases, the merchant receives an email from the Platform technical support with a list of IDs of COF purchases that should be re-registered. For this registration, merchant must notify customers of the termination of previous debits and the need to initiate new ones by unlinking the saved card, and then initiate registration in the platform. Each newly registered COF purchase receives a new ID, which is sent to the merchant in the callback with information on successful registration.</p><p>There are several things you must consider when performing a COF purchase with customer instrument credentials stored by the payment platform:</p><p>Thus, along all the required parameters, complete request must contain registration flags for selected COF purchase and its type; for regular purchases, request must also contain debiting schedule.</p><p>Depending on the specific characteristics of providers involved in payment processing, the set of required parameters can vary. For the detailed information about the providers&#39; requirements, contact the Platform key account manager.</p><p>The payment platform includes the COF purchase registration information in its operation result callback to your web service. The payment platform uses the standard format for callbacks. For more information, see .</p><p><code>1001648059</code>The following callback contains information about successful registration of a COF purchase with a debiting series record identifier.</p><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>as well as by migrating information about COF purchases from an external acquirer (details)To register a COF purchase with the payment platform, you need to save customer payment instrument credentials. You can do it in a variety of ways by processing payments via Gate, Payment Widget (details), and Dashboard (details).</p><p>To register a COF purchase with the payment platform, you need to save customer payment instrument credentials., payment link purchase,You can register a COF purchase via Gate by sending a request for one-time purchaseor for payment instrument verification with parameters indicating that the payment instrument credentials need to be saved.</p><p><code>31843301</code>COF purchase is registered for the time specified in the underlying request; if this time is not specified in the request, COF purchase registration term is set to the card expiration date. Once COF purchase registration expires, the payment platform sends a callback with this information to merchant&#39;s web service. Any further purchases by using this registration are no longer possible; if web service attempts to initiate the COF purchase, the payment platform will respond with a callback with the(or) error code.</p><p>COF purchase registration may be blocked for specific merchant projects or providers in which case the requests with registration demand may be declined. To prevent declining of such requests, you can contact support service at support@platform.dev and ask them to configure your project to ignore registration demands when processing COF purchases, if COF purchase registration is blocked.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>According to Visa and Mastercard recommendations issued in 2020, they temporary allow COF purchases with expired bank cards, provided the payment provider or the payment system support this option. In this case, purchases are possible both before and after card expiration date, if the COF registration date is set to the card expiration date. To prevent using expired bank cards for COF purchases, when registering COF purchase, set its expiry date to the date which is different from the card expiry date (seefor more information).</p></div><h2 id="registration-procedure" tabindex="-1">Registration procedure <a class="header-anchor" href="#registration-procedure" aria-label="Permalink to &quot;Registration procedure&quot;">​</a></h2><p><code>saleauthaccount verificationinvoice</code><em>and</em>To register a COF purchase, you need to send a request to initiate one of the following operations:,,, orand to populate the request with all the parameters required to process the operationthe parameters required to register the COF purchase.</p><p><code>success</code>The payment platform initiates and performs the requested operation including any auxiliary procedures which may be required. If credentials are stored on the payment platform, after the operation is successfully completed and is assigned thestatus, a record about debiting series is created on the payment platform. The record is assigned the following attributes:</p><p>If credentials are stored in the web service, a record about debiting series is not created.</p><p>The payment platform sends your web service a callback with operation result that contains ID of the debiting series record, if this record has been created. Such callback confirms that COF purchase is successfully registered.</p><ul><li><em>ID</em><code>idrecurring</code>Once the record about debiting series is created, web service receives its ID in the callback inside theparameter of theobject. You use this ID to perform and to manage COF purchases.</li><li><em>Status</em><code>activecanceled</code>Any active debiting series record is assigned thestatus. The status may change toon merchant or customer demand and in some other cases.</li></ul><h2 id="registration-when-credentials-are-stored-by-web-service" tabindex="-1">Registration when credentials are stored by web service <a class="header-anchor" href="#registration-when-credentials-are-stored-by-web-service" aria-label="Permalink to &quot;Registration when credentials are stored by web service&quot;">​</a></h2><p>There are several things you must consider when performing a COF purchase with customer instrument credentials stored by web service:</p><p>Thus, along all the required parameters, complete request must contain registration flags for selected COF purchase.</p><p>The payment platform includes the COF purchase registration information in its operation result callback to your web service. The payment platform uses the standard format for callbacks. For more information, see .</p><ol><li>—when processing a one-time one-step purchase—when processing a one-time two-step purchase—when verifying the payment instrumentwhen verifying the payment instrument with the use of a tokenYou need to use a POST request to one of the following endpoints:</li><li>Your request must contain all the required parameters and objects.</li><li><code>stored_card_type\`\`3</code>—autopurchase<code>5</code>—regular purchase (except for requests to the endpoint).Also, your request must include theparameter and specify COF purchase type by using one of the following values:</li></ol><h2 id="registration-when-credentials-are-stored-by-the-payment-platform" tabindex="-1">Registration when credentials are stored by the payment platform <a class="header-anchor" href="#registration-when-credentials-are-stored-by-the-payment-platform" aria-label="Permalink to &quot;Registration when credentials are stored by the payment platform&quot;">​</a></h2><ol><li>—when processing a one-time one-step purchase—when processing a one-time two-step purchase—when verifying the payment instrument—when initiating a payment link purchase—when initiating a payment link purchase with the use of a tokenYou need to use a POST request to one of the following endpoints:</li><li>Your request must contain all the required parameters and objects.</li><li><code>recurring\`\`registertrue</code>—COF purchase registration flag; usevalue to have the COF purchase registered.<code>type\`\`C</code>—one-click purchase<code>U</code>—autopurchase<code>R</code>—regular purchase—type of the COF purchase to register, possible values:<code>timehh:mm:ss</code>—time of subsequent debiting for regular purchase informat<code>period\`\`D</code>—daily<code>W</code>—weekly<code>M</code>—monthly<code>Q</code>—quarterly<code>Y</code>—yearly—debiting period for regular purchase:Also, your request must include aobject with the parameters of the COF purchase to register:</li><li><code>recurring\`\`expiry_year</code>—expiration year of the COF purchase<code>expiry_month</code>—expiration month of the COF purchase<code>expiry_day</code>—expiration day of the COF purchase<code>intervalperiodWinterval31100</code>—multiplicator to increase debiting period, for example if you need to run debiting every third week, you can settoandto. Possible values: fromto<code>amount</code>—amount to debit<code>start_date</code>—date to perform the first debit<code>scheduled_payment_ididrecurring\`\`scheduled_payment_idpayment_id</code>If the identifier that should be assigned to the COF purchase () matches the identifier of the payment made to register a COF purchase (), the request to register a COF purchase is declined.—identifier assigned to the payment within which scheduled debits are performed; it must differ from the identifier of the payment made to register a COF purchase and must be unique within the project; (also, not to be confused with the debiting series record identifier specified in theparameter of theobject that is passed in the callback with the COF purchase registration information)You can also use other parameters of theobject:</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("gate/gate-payment-recurring-registration.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const gatePaymentRecurringRegistration = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  gatePaymentRecurringRegistration as default
};
