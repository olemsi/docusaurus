# Проведение оплаты с автоматическими списаниями

## Общая информация

*Повторяемая оплата с автоматическими списаниями*— это тип платежа, в рамках которого на основании одного исходного запроса осуществляется серия переводов денежных средств от пользователя к мерчанту с использованием сохраненных платежных данных и без подтверждения подлинности платежного инструмента пользователя (такого, как ввод кода проверки подлинности карты).

ru_gate_sheduled_recurring_model.ditaВ рамках платежной платформы оплаты со списаниями по запросу проводятся, как описано в разделе.

## Схема оплаты

Чтобы выполнить повторяемую оплату с автоматическим списанием:

ru_gate_payment_recurring_manage.ditaДля изменения условий повторяемой оплаты или ее отмены, а также для возврата средств после одного или нескольких списаний необходимо передать соответствующие запросы в платежную платформу (подробнее — в разделе).

ru_gate_interaction_organisation.ditaИнформация о форматах запросов и оповещений приведена далее; общая информация о работе с API — в разделе.

1. Зарегистрируйтеповторяемую оплату.
2. запрос на повторяемую оплату`scheduled_payment_id`Этот шаг пропускается, если при регистрации оплаты был передан параметрс идентификатором уже существующей повторяемой оплаты.Передайтес идентификатором записи о серии списаний.
3. оповещение о результате списанияПримите от платежной платформы.
4. Продолжайте принимать оповещения о результате каждого списания в рамках платежа.

## Формат запроса

*платежных карт*Формат запросов в этом разделе представлен для инициирования повторяемых оплат с автоматическими списаниями с использованием.При формировании запросов необходимо учитывать следующее:

Таким образом, корректный запрос должен содержать идентификаторы проекта и платежа, подпись, IP-адрес пользователя, валюту и сумму проведения платежа, а также идентификатор серии списаний.

В зависимости от особенностей провайдеров, участвующих в проведении платежа, набор обязательных параметров может варьироваться. Подробную информацию о требованиях провайдеров можно уточнить у курирующего менеджера Some Platform.

1. Должен использоваться POST-запрос к конечной точке request-payment-card-recurring.
2. `general``project_id`— идентификатор проекта, полученный от Some Platform при интеграции;`payment_id`— идентификатор платежа, уникальный в рамках проекта;`signature`ru_pp_authentication.ditaru_gate_authentication.ditaru_signature_intro.dita— подпись запроса, составленная после указания целевых параметров (подробнее — в разделе);— объект, содержащий основные идентификационные сведения запроса:`customer``ip_address`— используемый IP-адрес;`id`— идентификатор пользователя в рамках проекта мерчанта;— объект, содержащий сведения о пользователе:`payment``amount`*за исключениемосновных единицах валюты*ru_currency_codes.dita— сумма платежа в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.;`currency`— валюта платежа в формате ISO-4217 alpha-3;— объект, содержащий сведения о платеже:`recurring``id`— идентификатор записи о серии списаний, полученный в оповещении с данными о регистрации.— объект, содержащий сведения повторяемой оплате:В запросе должны использоваться следующие объекты и параметры:
3. Дополнительно могут использоваться любые другие параметры, указанные в спецификации.

## Формат оповещений

ru_gate_callbacks.ditaПлатежная платформа передает в веб-сервис оповещение с данными о регистрации повторяемой оплаты. Для этого оповещения используется стандартный формат, описание которого представлено в разделе.

`42customer_124,00 USD№ 424242******4243`В данном случае оповещение свидетельствует о том, что в рамках проектадля пользователябыло выполнено списание в размерес платежной картыи ожидаются последующие списания.
