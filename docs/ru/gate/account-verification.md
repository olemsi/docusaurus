# Проверка платежной карты

ru_gate__payments_on_saved_data.ditaПроверка действительности платежной карты нужна, чтобы узнать, может ли карта использоваться для проведения платежей. Это может быть актуально перед проведением выплаты пользователю или при регистрации подписки на товары и услуги без списания средств за первый (пробный) период. Подробнее о регистрации повторяемой оплаты см..

В этом разделе рассказывается, как проверить действительность платежной карты.

`account verification`Чтобы инициировать проверку платежной карты, отправьте запрос в платежную платформу (пример запроса будет представлен далее). При выполнении такого запроса в платформе формируется операция.

Есть два варианта проверки:

условный (нулевой) перевод денежных средств от пользователя на ваш счет;реальная (ненулевая) блокировка средств пользователя с последующей отменой. Сумму блокировки вы можете согласовать со своим курирующим менеджером, а срок отмены блокировки может составлять до 45 дней.

ru_gate_payment_auth.ditaВ данном разделе мы рассмотрим вариант проверки карты с помощью нулевого платежа.  Подробнее о блокировке средств см.

## Шаги проверки карт

Для проверки действительности карты через Gate:

Далее в данном разделе представлена схема проведения проверки в базовом случае, без выполнения дополнительных процедур.

Далее рассказывается о структуре запросов на проверку действительности платежных карт и о структуре оповещений с результатами проверки.

1. Отправьте запрос с нужными параметрами и подписью на рабочий URL-адрес платежной платформы Some Platform.
2. **Аутентификация 3-D Secure**ru_gate_payment_3ds.ditaru_3ds2_about_gate.ditaТакая аутентификация предназначена для обеспечения безопасности оплаты с использованием карт через интернет. Подробнее об этой процедуре см.и.**Дополнение информации о платеже**ru_gate_clarification.ditaЭта процедура используется, когда по запросу одной из сторон, участвующих в проведении платежа, требуется предоставить дополнительную информацию. Подробная информация о процедуре представлена в разделе.При необходимости выполните вспомогательные процедуры, инициируемые платежной платформой. Это может быть один из вариантов аутентификации пользователя или дополнение информации о платеже:
3. Примите от платежной платформы оповещение (callback) о результате проверки.

## Запрос

Далее представлена информация, необходимая для создания и отправки запроса для проверки действительности платежной карты.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/card/account_verification`— конечная точка для проверки по номеру карты;`/v2/payment/card/account_verification/token`— конечная точка для проверки по токену, ассоциированному с картой. |
| Полная спецификация конечной точки | — полная спецификация конечной точки для проверки по номеру карты;— полная спецификация конечной точки для проверки по токену, ассоциированному с картой. |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе. |  |  |
| --- | --- | --- |
| Объекты и параметры | Описание |  |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| customertype_object type_strictly_required | idtype_string type_strictly_required | `customer_123`Пример:Идентификатор пользователя, уникальный в рамках проекта. |
| ip_addresstype_string type_strictly_required | `198.51.100.47`Пример:IP-адрес устройства пользователя. |  |
| emailtype_string type_strictly_required | `johndoe@example.com`Пример:Адрес электронной почты пользователя. |  |
| first_nametype_string type_strictly_required | `John`Пример:Имя пользователя. |  |
| last_nametype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |  |
| paymenttype_object type_strictly_required | amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.ditareferences/ru/currencies/USD.dita`10000`Пример: 100,00передается какДля проверки карты сумма платежа должна быть равна нулю.Сумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| currencytype_string type_strictly_required | `references/ru/currencies/USD.dita`Пример:Код валюты оплаты в формате ISO-4217 alpha-3. |  |
| Сведения о платежной карте пользователя при передаче реквизитов карты в явном виде: |  |  |
| cardtype_object type_strictly_required | pantype_string type_strictly_required | `1122334455667788`Пример:Номер карты. |
| yeartype_integer type_strictly_required | `2025`Пример:Год окончания срока действия карты. |  |
| monthtype_integer type_strictly_required | `8`Пример:Месяц окончания срока действия карты. |  |
| card_holdertype_string type_strictly_required | `JOHN DOE`Пример:Имя держателя карты (в соответствии с указанным на карте). |  |
| cvvtype_string type_strictly_required | `123`Пример:Код проверки подлинности карты (в соответствии с указанным на карте). |  |
| Сведения о платежной карте пользователя при передаче токена: |  |  |
| tokentype_string type_strictly_required | `f365bb1729f9b72fd9c79f3becc679f29c3e35c91d070d15654`Пример:ru_gate_token.ditaПодробнее см.Токен банковской карты пользователя. Токен представляет собой уникальную последовательность из 64 символов, связанную с банковской картой. |  |
| cvvtype_string type_strictly_required | `123`Пример:Код проверки подлинности карты (в соответствии с указанным на карте). |  |
| Для регистрации повторяемой оплаты |  |  |
| recurringtype_object type_strictly_required | registertype_boolean type_strictly_required | `true`Пример:Признак регистрации повторяемой оплаты. |
| `recurring`ru_gate__payments_on_saved_data.ditaВ объектетакже могут использоваться и другие параметры. Подробнее о регистрации повторяемой оплаты см.. |  |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

## Оповещение (callback)

ru_gate_callbacks.ditaРезультат проверки платежной карты платежная платформа возвращает в оповещении. Подробнее о структуре оповещений см..

`123112233******7788customer_123`Вот пример тела оповещения с информацией о том, что в рамках проектакартапользователядействительна и зарегистрирована для повторяемой оплаты.

Если карта не прошла проверку, платежная платформа отправит вам оповещение с информацией о том, что проведение платежа было отклонено.

## Статусы платежа

В данном разделе представлена информация о возможных статусах платежа и связанных с ним операций.

В процессе проверки карты платежу могут присваиваться следующие статусы.

| Статус платежа | Описание |
| --- | --- |
| `error` | При проверке запроса возникла ошибка, поэтому платежная платформа не приняла запрос и не начала обработку платежа.*Конечное состояние. Вы можете отправить запрос на проведение этого платежа повторно с тем же идентификатором платежа* |
| `processing` | *Промежуточное состояние*Платеж в процессе выполнения. |
| `awaiting 3ds result` | `3ds_resultdecline`*Промежуточное состояние*Проведение платежа приостановлено. Платежная платформа ожидает от вас запрос с информацией о результате аутентификации 3ds в параметре. Если такой запрос не поступит от вас в платежную платформу в течение 30 минут, то платеж будет отклонен и переведен в статус. |
| `awaiting clarification` | `decline`*Промежуточное состояние*Проведение платежа приостановлено. Платежная платформа ожидает от вас запрос с недостающей информацией. Если такой запрос не поступит от вас в платежную платформу в течение 30 минут, платеж будет отклонен и переведен в статус. |
| `decline` | *Конечное состояние*Платеж отклонен. |
| `success` | *Конечное состояние*Платеж проведен. |

## Статусы операции account verification

`account verification`Следующие статусы могут быть назначены операции.

| Статус операции | Описание |
| --- | --- |
| `processing` | *Промежуточное состояние*Операция выполняется. |
| `awaiting 3ds result` | `3ds_resultdecline`*Промежуточное состояние*Выполнение операции приостановлено. Платежная платформа ожидает от вас запрос с информацией о результате аутентификации 3ds в параметре. Если такой запрос не поступит от вас в платежную платформу в течение 30 минут, операция будет отклонена и переведена в статус. |
| `awaiting clarification` | `decline`*Промежуточное состояние*Выполнение операции приостановлено. Платежная платформа ожидает от вас запрос с недостающей информацией. Если такой запрос не поступит от вас в платежную платформу в течение 30 минут, операция будет отклонена и переведена в статус. |
| `decline` | *Конечное состояние*Операция отклонена. |
| `success` | *Конечное состояние*Операция выполнена. |

## Ссылки по теме

- ru_gate_interaction_organisation.dita
- ru_gate_callbacks.dita
- ru_gate_payment_model_object.dita
- ru_gate_statuses_and_response_codes.dita

::: tip
Информацию о возможности проверки карт в вашем проекте уточняйте у курирующего менеджера Some Platform.
:::

## Схема и статусы проверки платежной карты

### Общая информация

*Проверка действительности карты*— это тип платежа, в рамках которого для проверки возможности использования платежной карты на основании одного исходного запроса выполняется один условный (нулевой) перевод денежных средств пользователя мерчанту или одна реальная (ненулевая) блокировка средств пользователя с последующей отменой. При этом сумма блокировки может согласовываться с мерчантом, а срок отмены блокировки может составлять до 45 дней.

Платежи этого типа могут быть актуальны, например, для регистрации повторяемых оплат.

::: tip
Информацию о возможности проведения проверки карты уточняйте у курирующего менеджера Some Platform.
:::

### Схема проведения

`account verificationaccount verification`Чтобы инициировать проверку платежной карты, отправьте в платформу запрос. При выполнении такого запроса в платформе формируется операция.

При проверке платежной карты дополнительно могут требоваться и другие запросы:

ru_gate_account_verification.ditaДалее в рамках данного раздела представлена информация о возможных статусах проверки карты и связанных с ней операций. Более подробная информация о проведении проверки доступна в разделе.

- *аутентификация пользователя с использованием технологии 3-D Secure*`3ds_result`Если необходима, то от платформы в веб-сервис отправляется оповещение с информацией для формирования запроса к эмитенту, после чего проведение платежа в платформе приостанавливается до получения от веб-сервиса запроса с результатом аутентификации —.
- *уточнение информации*`clarification`Если необходимодля какой-либо из сторон, участвующих в проведении платежа (например, предоставление в платежную систему адреса держателя карты, не переданного в исходном запросе), то от платформы в веб-сервис отправляется оповещение с названиями параметров для уточнения и проведение платежа в платформе приостанавливается до получения от веб-сервиса запроса с необходимой информацией —.

### Статусы платежа

При проверке карт могут использоваться следующие статусы.

| `error` | Проведение платежа не инициировано из-за ошибки, возникшей при проверке принятого запроса | *Конечное состояние. Допускается повторная отправка запроса с тем же идентификатором платежа и повторная попытка проведения этого платежа* |
| --- | --- | --- |
| `processing` | Платеж проводится | *Промежуточное состояние* |
| `awaiting 3ds result` | `3ds_resultdecline`Проведение платежа приостановлено до получения запроса на продолжение с учетом результата аутентификации 3-D Secure —. Если такой запрос не получен в течение 30 минут, то платеж переводится в статус | *Промежуточное состояние* |
| `awaiting clarification` | `clarificationdecline`Проведение платежа приостановлено до получения запроса на продолжение с учетом требуемой дополнительной информации —. Если такой запрос не получен в течение 30 минут, платеж переводится в статус | *Промежуточное состояние* |
| `decline` | Платеж отклонен | *Конечное состояние* |
| `success` | Платеж проведен | *Конечное состояние* |

### Статусы операции account verification

`account verification`При выполнении операциимогут использоваться следующие статусы.

| `processing` | Операция выполняется | *Промежуточное состояние* |
| --- | --- | --- |
| `awaiting 3ds result` | `3ds_resultdecline`Выполнение операции приостановлено до получения запроса на продолжение с учетом результата аутентификации 3-D Secure —. Если такой запрос не получен в течение 30 минут, операция переводится в статус | *Промежуточное состояние* |
| `awaiting clarification` | `clarificationdecline`Выполнение операции приостановлено до получения запроса на продолжение с учетом требуемой дополнительной информации —. Если такой запрос не получен в течение 30 минут, операция переводится в статус | *Промежуточное состояние* |
| `decline` | Операция отклонена | *Конечное состояние* |
| `success` | Операция выполнена | *Конечное состояние* |

### Дополнительные материалы

- ru_gate_interaction_organisation.dita— раздел с общей информацией о взаимодействии с платежной платформой через Gate.
- ru_gate_account_verification.dita— раздел с подробной информацией о проведении проверки карт.
- ru_gate_callbacks.dita— раздел с информацией об оповещениях и работе с ними.
- ru_gate_statuses_and_response_codes.dita— раздел с информацией о кодах ошибок, используемых в платежной платформе.
