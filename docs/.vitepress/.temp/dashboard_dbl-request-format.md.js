import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Request format","description":"","frontmatter":{},"headers":[],"relativePath":"dashboard/dbl-request-format.md","filePath":"dashboard/dbl-request-format.md"}');
const _sfc_main = { name: "dashboard/dbl-request-format.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="request-format" tabindex="-1">Request format <a class="header-anchor" href="#request-format" aria-label="Permalink to &quot;Request format&quot;">​</a></h1><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p><em>requests</em>Data API SpecificationTo interact with the payment platform via the Data API, all data from the merchant&#39;s web service must be passed in—HTTP messages in a specific format—using the POST method. This section covers the general structure of such requests; for the information about the data structure of the specific requests, see the.</p><h2 id="structure" tabindex="-1">Structure <a class="header-anchor" href="#structure" aria-label="Permalink to &quot;Structure&quot;">​</a></h2><p>Each HTTP request to the payment platform must include the following elements in the specified order:</p><p><code>Host</code><a href="https://tools.ietf.org/html/rfc2616#page-31" target="_blank" rel="noreferrer">HTTP version 1.1</a>In addition to the mandatoryfield, the header may include any other fields supported by. Below you will find a sample HTTP request with the recommended header fields and required parameters. The JSON string is formatted for better readability.</p><ul><li><code>POST)v1/operations/getHTTP/1.1</code>The start line which indicates the HTTP method of the request (, the endpoint in the Data API (for example,), the protocol and its version ().</li><li><code>Hostbasic data api text link</code>The header with thefield which contains the domain name of the Data API ().</li><li>An empty line which serves as a separator between the message header and the body.</li><li>The message body which contains the JSON string encoded in UTF-8 with request data including the signature.</li></ul><h2 id="addressing-parameters" tabindex="-1">Addressing parameters <a class="header-anchor" href="#addressing-parameters" aria-label="Permalink to &quot;Addressing parameters&quot;">​</a></h2><p><code>basic data api text link</code>The generated requests must include the base and the relative URLs. The base URL for the Data API requests is the domain name, and the relative URL is the path to the target endpoint in the interface according to the specification.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p><code>https://{domain name for the Data API requests}/{target endpoint path}https://basic data api text link/v1/balance/get</code>The absolute URL in this case takes the following format. For example, the absolute URL for the request to retrieve balance information on merchant&#39;s projects is. However, absolute URLs are not used for generating POST requests.</p></div><h2 id="message-body" tabindex="-1">Message body <a class="header-anchor" href="#message-body" aria-label="Permalink to &quot;Message body&quot;">​</a></h2><p><code>&quot;&amp;lt;parameter name&gt;&quot;: &amp;lt;parameter value&gt;</code>The message body must contain the request data in the JSON format. The JSON string follows the format. To prevent data loss as a result of spillage or spoofing when data is transferred to the payment platform, the JSON string must include the token and the signature with the request data, and the TLS 1.2 encryption protocol must be used to secure the data transfer. For more information about generating tokens, see , and to learn more about generating a signature, see .</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("dashboard/dbl-request-format.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const dblRequestFormat = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  dblRequestFormat as default
};
