# Типы платежей, операции и платежи

с использованием разных платежных методов:ru_pm_intro.ditaили альтернативных платежных инструментов (подробнее — в разделе), независимо от используемого метода,Платежная платформа Some Platform позволяет проводить различные платежис применением платежных карт. Все платежиподразделяются на несколько базовых типов.

В этом разделе представлена общая информация о типах поддерживаемых платежей, о схемах выполнения и возможных статусах платежей, а также о связанных с платежами информационных объектах: запросах и операциях.

- ru_pp_payment_model_object.dita
- ru_pp_sms_model.dita
- ru_pp_dms_model.dita
- ru_pp_recurring_model.dita
- ru_pp_scheduled_recurring_model.dita

## Запросы, платежи и операции

*платежоплатавыплата*В основе работы платежной платформы лежит, то есть набор действий по выполнению заявки мерчанта на перевод денежных средств между ним и пользователем. Это может быть перевод средств от пользователя к мерчанту (такой платеж называется) либо от мерчанта к пользователю (такой платеж называется).

Возврат средств по выполненной оплате выполняется в рамках исходной оплаты и не выделяется в отдельный тип платежей.

*Проверка действительности платежного инструмента*считается отдельным видом платежа и предусматривает условный (нулевой) перевод денежных средств или реальную (ненулевую) блокировку средств с последующей ее отменой.

Существуют следующие типы платежей:

Любой платеж инициируется со стороны мерчанта: путем запроса через один из программных интерфейсов платежной платформы — Gate API или Payment Widget API — или через пользовательский интерфейс Dashboard.

`payment`*операции*При получении корректного запроса в платформе создается объекти инициируется выполнение соответствующей, которая может быть единственной или первой из нескольких в рамках текущего платежа.

необходимоинтерфейс Gate илиКак правило, каждая последующая операция инициируется отдельным запросом со стороны мерчанта, однако иногда операции могут инициироваться автоматически платежной платформой. Например, возврат средств по проведенной оплатеинициировать черезDashboard, а списание предварительно заблокированных средств может выполняться автоматически по истечении заданного времени.

В рамках такой схемы можно выполнять следующее:

В последующих разделах представлена информация о типах платежей, доступных для инициирования через  Payment Widget.
