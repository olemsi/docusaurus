import { _ as _export_sfc, o as openBlock, c as createElementBlock, a2 as createStaticVNode } from "./chunks/framework.BFJzPV4p.js";
const __pageData = JSON.parse('{"title":"Request format","description":"","frontmatter":{},"headers":[],"relativePath":"gate/gate-money-transfer-services-format-request.md","filePath":"gate/gate-money-transfer-services-format-request.md"}');
const _sfc_main = { name: "gate/gate-money-transfer-services-format-request.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="request-format" tabindex="-1">Request format <a class="header-anchor" href="#request-format" aria-label="Permalink to &quot;Request format&quot;">​</a></h1><h2 id="debiting" tabindex="-1">Debiting <a class="header-anchor" href="#debiting" aria-label="Permalink to &quot;Debiting&quot;">​</a></h2><p>When creating a request for debiting, consider the following:</p><p>Thus, a correct request for debiting funds contains project and payment identifiers, the signature, the amount to be debited and the corresponding currency code, information about the sender, and the payment information of the sender and the recipient specified as one of the options described above.</p><p>The following example contains the body of the request for debiting from a Visa with the funds intended for subsequent crediting to a Mastercard.</p><ol><li>when sending the actual card details—;when sending the saved card identifiers—;;.when sending the card tokens—when Apple Pay or Google Pay is used to make a payment—orrespectively.The request must be sent to one of the following endpoints with the use of the HTTP POST method:</li><li><code>general``project_id</code>—the project identifier assigned by Platform;<code>payment_id</code>—the payment identifier unique within the project;<code>signature</code>—the request signature generated after all required parameters have been specified (for more information, see );—the object containing the general identification information of the request:<code>payment``amount</code>—the amount to be debited from the customer’s (sender&#39;s) card in the smallest currency units;<code>currency</code>—the currency code for the debited amount in ISO-4217 alpha-3;<code>cryptocurrency_type``cbdc</code>—a central bank digital currency or tokenized deposit issued by a central bank, reserve bank, or other national monetary authority.<code>stablecoins_fiat_backed</code>—a fiat-backed digital asset with reserves held by a licensed financial institution.<code>native_tokens</code>—a digital currency native to a specific blockchain, required for transactions within its network, including fee payments.<code>other</code>—a non-fiat digital asset that does not fit other types or cannot be classified at the time of transaction initiation.—the indicator that specifies the type of the cryptocurrency, required for Visa payments involving cryptocurrencies. This parameter should be assigned one of the following values:—the object containing the debiting information:<code>customer``id</code>—the identifier of the customer within the project;<code>ip_address</code>—the IP address of the customer;<code>country</code>—the customer’s country code in ISO 3166-1 alpha- 2;<code>address</code>—the customer&#39;s address, required when Visa cards are used;<code>city</code>—the customer’s city (or any other place of residence), required when Visa cards are used;<code>state</code>—the customer’s state or province code, required when the debited funds are credited to Visa cards issued in the USA or Canada;<code>phone</code>—the customer’s phone number, required when the debited funds are credited to Visa cards issued in Brazil or Qatar;<code>account_id</code>—the customer’s wallet number, required when Mastercard cards are used;<code>first_name</code>—the customer&#39;s first name, required when Apple Pay or Google Pay is used;<code>last_name</code>—the customer&#39;s last name, required when Apple Pay or Google Pay is used.— the object containing the information about the customer (sender):The request must contain the following objects and parameters:</li><li><code>card``pan</code>—the card number;<code>year</code>—the card expiration year;<code>month</code>—the card expiration month;<code>card_holder</code>—the first and last name of the cardholder (as specified on the card);<code>cvv</code>—the card verification code.When sending the actual card details, include the following parameters in theobject:<code>card``saved_account_id</code>—the identifier associated with the corresponding card details in the payment platform;<code>cvv</code>—the card verification code.When sending the saved card identifier, include the following parameters in theobject:<code>token</code>—the token associated with the corresponding card details in the payment platform;<code>cvv</code>—the card verification code.When sending the token—include the following parameters:The request must contain the following information about the sender’s (customer’s) card:</li><li><code>recipient``wallet_id</code>—the wallet number;<code>wallet_owner</code>—the first and last name of the wallet owner;<code>country</code>—the wallet owner’s country code in ISO 3166-1 alpha- 2, required when a Mastercard is linked to this wallet.When sending the recipient’s wallet information—include the following parameters in theobject:<code>recipient``pan</code>—the recipient&#39;s card number;<code>card_holder</code>—the first and last name of the cardholder (as specified on the card).<code>card_holder</code>—the first and last name of the cardholder (as specified on the card);<code>day_of_birthDD-MM-YYYY</code>—the recipient&#39;s date of birth informat, required if a Visa card is used to receive the payment.When sending the card details—include the following parameters in theobject:<code>country</code>—the recipient’s country code in ISO 3166-1 alpha-2;<code>address</code>—the recipient’s address;<code>city</code>—the recipient’s city (or any other place of residence);<code>state_code</code>—the recipient’s state or province code.When sending the details of a Visa card issued in Canada, include the following parameters (in addition to the required card data):The request must contain the following information about the recipient and their payment instrument:</li><li>If necessary, you can also add any other additional parameters and objects specified in the API specification.</li></ol><h2 id="crediting" tabindex="-1">Crediting <a class="header-anchor" href="#crediting" aria-label="Permalink to &quot;Crediting&quot;">​</a></h2><p>When creating a request for crediting, consider the following:</p><p>Thus, a correct request for crediting funds contains project and payment identifiers, the signature, the amount to be credited and the corresponding currency code, information about the sender and the recipient, and the payment information of the sender and the recipient specified as one of the options described above.</p><p>The following example contains the body of the request for crediting funds to a Mastercard with the funds previously debited from a Visa.</p><ol><li>when sending the actual card details—;when sending the card tokens—.The request must be sent to one of the following endpoints with the use of the HTTP POST method:</li><li><code>general``project_id</code>—the project identifier assigned by Platform;<code>payment_id</code>—the payment identifier unique within the project;<code>signature</code>—the request signature generated after all required parameters have been specified (for more information, see );—the object containing the general identification information of the request:<code>payment``amount</code>—the amount to be credited the recipient&#39;s card in the smallest currency units;<code>currency</code>—the currency code for the credited amount in ISO-4217 alpha-3;<code>cryptocurrency_type``cbdc</code>—a central bank digital currency or tokenized deposit issued by a central bank, reserve bank, or other national monetary authority;<code>stablecoins_fiat_backed</code>—a fiat-backed digital asset with reserves held by a licensed financial institution;<code>native_tokens</code>—a digital currency native to a specific blockchain, required for transactions within its network, including fee payments;<code>other</code>—a non-fiat digital asset that does not fit other types or cannot be classified at the time of transaction initiation.—the indicator that specifies the type of the cryptocurrency, required for Visa payments involving cryptocurrencies. This parameter should be assigned one of the following values:—the object containing the crediting information::<code>customer``id</code>—the identifier of the customer (sender) within the project;<code>ip_address</code>—the IP address of the customer.—the object containing the information about the sender:The request must contain the following objects and parameters:</li><li><code>card``pan</code>—the recipient&#39;s card number;<code>card_holder</code>—the first and last name of the cardholder (as specified on the card).When sending the card details—include the following parameters in theobject:<code>token</code>—the token associated with the corresponding card details in the payment platform.When sending the token—include the following parameters:The request must contain the following information about the recipient’s card:</li><li><code>sender``pan</code>—the sender&#39;s card number;<code>wallet_id</code>—the sender&#39;s wallet number.Payment instrument information is specified in one of the following parameters:<code>country</code>—the sender’s country code in ISO 3166-1 alpha- 2;<code>address</code>—the sender&#39;s address, required when Visa cards are used;<code>city</code>—the sender’s city (or any other place of residence), required when Visa cards are used;<code>first_name</code>—the sender&#39;s first name;<code>last_name</code>—the sender&#39;s last name;<code>state</code>—the sender’s state or province code, required when funds are credited to Visa cards issued in the USA or Canada;<code>zip</code>—the sender’s postal code, required when Mastercard cards are used;<code>day_of_birth</code>—the sender&#39;s date of birth in the DD-MM-YYYY format, recommended when Visa cards are used;<code>phone</code>—the sender’s phone number, required when funds are credited to Visa cards issued in Brazil or Qatar.Information about the sender is recommended to include the following parameters:The request must contain the following information about the sender in theobject:</li><li><code>recipient``first_name</code>—the first name of the recipient;<code>last_name</code>—the last name of the recipient.The request must contain the following information about the recipient in theobject:</li><li>If necessary, you can also add any other additional parameters and objects specified in the API specification.</li></ol>', 11)
  ])]);
}
const gateMoneyTransferServicesFormatRequest = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  gateMoneyTransferServicesFormatRequest as default
};
