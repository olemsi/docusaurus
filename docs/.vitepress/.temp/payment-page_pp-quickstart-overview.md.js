import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Quick start","description":"","frontmatter":{},"headers":[],"relativePath":"payment-page/pp-quickstart-overview.md","filePath":"payment-page/pp-quickstart-overview.md"}');
const _sfc_main = { name: "payment-page/pp-quickstart-overview.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="quick-start" tabindex="-1">Quick start <a class="header-anchor" href="#quick-start" aria-label="Permalink to &quot;Quick start&quot;">​</a></h1><p>This quick start is for teams that want to launch Payment Widget with the smallest possible amount of reading and the highest possible amount of reusable code.</p><h2 id="what-you-get-from-this-guide" tabindex="-1">What you get from this guide <a class="header-anchor" href="#what-you-get-from-this-guide" aria-label="Permalink to &quot;What you get from this guide&quot;">​</a></h2><p>After completing this guide, you will have:</p><ul><li>a server-side step that signs Payment Widget parameters</li><li>a client-side step that opens the payment form</li><li>a callback endpoint that receives the payment result</li></ul><h2 id="before-you-start" tabindex="-1">Before you start <a class="header-anchor" href="#before-you-start" aria-label="Permalink to &quot;Before you start&quot;">​</a></h2><p>Prepare the following:</p><ul><li><code>project_id</code> for your test project</li><li><code>secret_key</code> for signing requests and validating callbacks</li><li>a callback URL available to the platform</li><li>a test order page or checkout button in your web service</li></ul><h2 id="where-project-id-and-secret-key-come-from" tabindex="-1">Where <code>project_id</code> and <code>secret_key</code> come from <a class="header-anchor" href="#where-project-id-and-secret-key-come-from" aria-label="Permalink to &quot;Where \`project_id\` and \`secret_key\` come from&quot;">​</a></h2><p>For the first integration, these values are issued for your merchant project by Platform.</p><ul><li><code>project_id</code> is the identifier of the test project created for your integration.</li><li><code>secret_key</code> is the signing key used to generate request signatures and verify callbacks for that project.</li></ul><p>If you do not have them yet, ask your Platform account manager or technical support to provision a test Payment Widget project and share the test <code>project_id</code> and <code>secret_key</code>.</p><h2 id="if-you-only-need-the-first-opening-request" tabindex="-1">If you only need the first opening request <a class="header-anchor" href="#if-you-only-need-the-first-opening-request" aria-label="Permalink to &quot;If you only need the first opening request&quot;">​</a></h2><p>Go straight to <a href="./pp-quickstart-basic-implementation.html"><strong>Basic implementation</strong></a>. That page shows:</p><ul><li>the Payment Widget base URL</li><li>the exact parameter set</li><li>how to sign it</li><li>how to open the form</li><li>how to receive the callback</li></ul><p>If you only need the signing part, go straight to <a href="./pp-signature-generation.html"><strong>Signature generation</strong></a>.</p><h2 id="best-path-for-first-integration" tabindex="-1">Best path for first integration <a class="header-anchor" href="#best-path-for-first-integration" aria-label="Permalink to &quot;Best path for first integration&quot;">​</a></h2><ol><li>Read <a href="./pp-quickstart-theory.html"><strong>Brief theory</strong></a> if you need the architecture in one page.</li><li>Read <a href="./pp-signature-generation.html"><strong>Signature generation</strong></a> if signing is the only missing piece.</li><li>Go straight to <a href="./pp-quickstart-basic-implementation.html"><strong>Basic implementation</strong></a> for copy-paste code.</li><li>After the first successful payment, continue with <a href="./pp-ux-configuration.html"><strong>UX configuration</strong></a> and <a href="./pp-parameters.html"><strong>Parameters</strong></a>.</li></ol><h2 id="recommended-implementation-style" tabindex="-1">Recommended implementation style <a class="header-anchor" href="#recommended-implementation-style" aria-label="Permalink to &quot;Recommended implementation style&quot;">​</a></h2><p>For a first launch, use the shortest possible path:</p><ul><li>generate the signed parameter set on the server</li><li>open Payment Widget in a new tab or modal</li><li>rely on callbacks as the source of truth for order status</li></ul><p>Once that works, add return URLs, custom UI behaviour, saved payment data, and any extra checkout features.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("payment-page/pp-quickstart-overview.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ppQuickstartOverview = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ppQuickstartOverview as default
};
