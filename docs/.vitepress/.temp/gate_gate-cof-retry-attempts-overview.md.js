import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"COF retry attempts","description":"","frontmatter":{},"headers":[],"relativePath":"gate/gate-cof-retry-attempts-overview.md","filePath":"gate/gate-cof-retry-attempts-overview.md"}');
const _sfc_main = { name: "gate/gate-cof-retry-attempts-overview.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="cof-retry-attempts" tabindex="-1">COF retry attempts <a class="header-anchor" href="#cof-retry-attempts" aria-label="Permalink to &quot;COF retry attempts&quot;">​</a></h1><h2 id="introduction" tabindex="-1">Introduction <a class="header-anchor" href="#introduction" aria-label="Permalink to &quot;Introduction&quot;">​</a></h2><p>You can add this capability for specific projects and for a specific group of payment methodsupon agreement with your Platform account manager.If a scheduled automatic debiting is declined, for example, in case when the customer&#39;s account has insufficient balance, initiating this debiting can be attempted again in the Platform payment platform after a certain time period.Note that the merchant is responsible for notifying the customers that this functionality is being used, specifically when a scheduled debiting was declined and retry attempts have been set up.</p><p>When retry attempts are used for COF purchases, their number and the time allocated for making them are limited: for each scheduled debiting there can be no more than 7 retry attempts carried out within 6 days. The time interval between an initial debiting and the first retry attempt as well as between the first and the second retry attempts equals 12 hours. The rest of the retry attempts are carried out every 24 hours. Along with that, it is checked that according to the debiting schedule the next debiting is to occur at least 12.5 or 24.5 hours after the retry attempt. If not, the retry attempts are halted. These restrictions are applicable to all payments within any project and cannot be modified in any way.</p><p>(details)If retry attempts are not used for COF purchases, when a scheduled debiting in the series is declined, the next scheduled debiting is expected to be processed in the payment platform according to the schedule. Whether an additional debiting should occur or whether the series of debitings should be cancelled is decided by the merchant.</p><h2 id="special-aspects" tabindex="-1">Special aspects <a class="header-anchor" href="#special-aspects" aria-label="Permalink to &quot;Special aspects&quot;">​</a></h2><p>When working with the debiting retries, consider the following:</p><ul><li>a specific retry schedule: either a default one by Platform or a custom oneonly a default schedule by PlatformFor each project you can implement.</li><li>a limited number of the globally accepted payment methods: Apple Pay, Google Pay, and standardDebiting retries can be used forcard payments.</li><li><code>R</code>Debiting retries can be used only for regular COF purchases when the debiting schedule is stored on the side of the payment platform (type).</li><li>Debiting can be retried only if the previous debiting attempts were declined by the issuer or the payment system.</li><li><code>decline</code>Using the functionality of debiting retries does not guarantee that the funds will be withdrawn eventually. If the allowed number of retries does not result in debiting, then this debit operation is deemed declined (it is assigned thestatus), and the COF purchase in question is subsequently processed in the platform according to its debiting schedule.</li></ul><h2 id="use" tabindex="-1">Use <a class="header-anchor" href="#use" aria-label="Permalink to &quot;Use&quot;">​</a></h2><p>The specifics of using debiting retries can be illustrated with the following examples.</p><p>Suppose within a specific COF purchase automatic debitings are initiated on Monday at 12:00 and, according to the schedule, are to be performed on November 02, 09, 16, and 23, but on November 09 and 16 scheduled debitings do not occur, then the options to respond to this situation can be as follows:</p><p>Since schedules of both regular COF purchases and debiting retry attempts can be flexibly configured, these scenarios can be used in a wide range of situations. However, it is also important to monitor statuses of all operations and maintain appropriate communication with customers depending on the operation statuses and retry schedule.</p><ol><li>If the functionality of debiting retries is not available for this purchase, then regardless whether the scheduled debiting was completed or declined, the next scheduled debiting will be performed.</li><li>If a default retry schedule from Platform is usedIf the functionality of debiting retries is available for this purchase, then after the initial scheduled debiting is declined, the platform will retry it as many times as determined by the schedule: the first two attempts will occur with a delay of 12 hours between them, the following attempts will occur every 24 hours, and the attempts will be stopped at least 24.5 hours before the next scheduled debiting on Monday.</li><li>If a default retry schedule from Platform is usedIf the functionality of debiting retries is available for this purchase, and the web service requests to stop subsequent debiting retries after three attempts are declined one after another, then if the initial debiting is declined, the platform will retry it up to three times in a row.</li><li>If a custom retry schedule is used, then after the initial scheduled debiting is declined, the platform will retry it as many times as configured with a delay of 24 hours between consecutive attempts, and the attempts will be stopped at least 24.5 hours before the next scheduled debiting on Monday.</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("gate/gate-cof-retry-attempts-overview.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const gateCofRetryAttemptsOverview = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  gateCofRetryAttemptsOverview as default
};
