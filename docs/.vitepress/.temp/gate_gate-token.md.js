import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Using tokens","description":"","frontmatter":{},"headers":[],"relativePath":"gate/gate-token.md","filePath":"gate/gate-token.md"}');
const _sfc_main = { name: "gate/gate-token.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="using-tokens" tabindex="-1">Using tokens <a class="header-anchor" href="#using-tokens" aria-label="Permalink to &quot;Using tokens&quot;">​</a></h1><p><strong>Token</strong>is a unique, random sequence of 64 characters associated in Gate with a specific bank card and the customer in the project. Token does not contain a confidential information and can be stored in your system without threatening to violate the standards of secure bank card data storage.</p><p>By using Gate you can create tokens automatically or by request and make puchases and payouts by using existing tokens.</p><h2 id="statuses-of-tokens" tabindex="-1">Statuses of tokens <a class="header-anchor" href="#statuses-of-tokens" aria-label="Permalink to &quot;Statuses of tokens&quot;">​</a></h2><p>The status of a token defines if the token can be used to perform payments and payouts.</p><table tabindex="0"><thead><tr><th>Status</th><th>Description</th></tr></thead><tbody><tr><td><code>active</code></td><td>Token is active and can be used to perform payments and payouts</td></tr><tr><td><code>revoke</code></td><td>Token is revoked and cannot be used to perform payment and payouts</td></tr><tr><td><code>expiry</code></td><td>Token has expired and cannot be used to perform payment and payouts</td></tr></tbody></table><h2 id="automatic-token-generation" tabindex="-1">Automatic token generation <a class="header-anchor" href="#automatic-token-generation" aria-label="Permalink to &quot;Automatic token generation&quot;">​</a></h2><p><em>Automatic generation</em>procedure creates a token when the first successful purchase or payout is performed by using a bank card, as well as after holding the funds on a bank card.</p><p>The generated token and the date when it was created are returned in the payment callback. For more information about callbacks, see in .</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>To enable automatic token generation, contact technical support by email support@platform.dev.</p></div><h2 id="token-generation-by-request" tabindex="-1">Token generation by request <a class="header-anchor" href="#token-generation-by-request" aria-label="Permalink to &quot;Token generation by request&quot;">​</a></h2><p>Another way to generate a token is to send a request to generate a token. In the request you send the data required to generate the token.</p><p>The generated token and the time it was created at are returned in the token generation response.</p><h2 id="token-generation-request" tabindex="-1">Token generation request <a class="header-anchor" href="#token-generation-request" aria-label="Permalink to &quot;Token generation request&quot;">​</a></h2><p>Specify the project id, customer data and data of customer bank card in the request.</p><p>You receive in the callback a bank card token and the time when it was generated. For more information about callbacks, see in .</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>\`\`POST therequest.</p></div><h2 id="payment-by-token" tabindex="-1">Payment by token <a class="header-anchor" href="#payment-by-token" aria-label="Permalink to &quot;Payment by token&quot;">​</a></h2><p>Gate enables customers to process fast payments by using a previously generated bank card token.</p><p>The token is unique to the bank card. When processing payments by bank card a valid token that already exists will be returned. In this case, if the card expiration date is different from the one specified when the token was generated, the token is not generated anew, but the expiration date of the token is updated.</p><p>Additional information about the token-based payments see in .</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>The token is unique to the bank card. When processing payments by bank card a valid token that already exists will be returned.</p></div><h2 id="payout-by-token" tabindex="-1">Payout by token <a class="header-anchor" href="#payout-by-token" aria-label="Permalink to &quot;Payout by token&quot;">​</a></h2><p>Gate enables you to process the payout of funds to a customer&#39;s bank card by using a previously generated token. Additional information about the token-based payouts see .</p><h2 id="receiving-card-data-by-token" tabindex="-1">Receiving card data by token <a class="header-anchor" href="#receiving-card-data-by-token" aria-label="Permalink to &quot;Receiving card data by token&quot;">​</a></h2><p>If you need to receive bank card data and information about the payment instrument to which this card is linked, send a request to Gate.</p><p>In the request, specify the project ID, customer ID and token.</p><p>You receive in the callback the masked bank card data and other information on the customer payment instrument.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>POST therequest.</p></div><h2 id="card-verification-by-token" tabindex="-1">Card verification by token <a class="header-anchor" href="#card-verification-by-token" aria-label="Permalink to &quot;Card verification by token&quot;">​</a></h2><p>Gate enables you to perform the customer&#39;s bank card verification by using a previously generated token. The information about the token-based verification is provided in the section.</p><h2 id="revoke-of-a-token" tabindex="-1">Revoke of a token <a class="header-anchor" href="#revoke-of-a-token" aria-label="Permalink to &quot;Revoke of a token&quot;">​</a></h2><p>A token can be revoked in one of the following happens: you revoke the token or the payment card expires.</p><p>In the former case, if required, you can revoke a token from Gate by sending a request to revoke the token. Gate revokes the token.</p><h2 id="request-to-revoke-a-token" tabindex="-1">Request to revoke a token <a class="header-anchor" href="#request-to-revoke-a-token" aria-label="Permalink to &quot;Request to revoke a token&quot;">​</a></h2><p>Specify the customer data in your system and the token to be revoked.</p><p>You receive in the callback the result of revoking of the token.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>POST therequest.</p></div><h2 id="callback-after-generating-or-revoking-a-token" tabindex="-1">Callback after generating or revoking a token <a class="header-anchor" href="#callback-after-generating-or-revoking-a-token" aria-label="Permalink to &quot;Callback after generating or revoking a token&quot;">​</a></h2></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("gate/gate-token.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const gateToken = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  gateToken as default
};
