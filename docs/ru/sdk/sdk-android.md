# sdk_android

sdk_android — это набор средств разработки для подключения мобильных приложений, работающих на платформе Android, к платежной платформе Some Platform. В этом разделе представлена информация о работе с sdk_android с примерами кода на языках программирования Java и Kotlin.

sdk_android может встраиваться в мобильные приложения, работающие на платформе Android версии 4.4 (уровень API — 19) или выше и поддерживающие AndroidX. Для корректного отображения платежной формы разрешение экрана мобильного устройства должно быть не менее 480x800 пикселей.

Библиотеки для Android: sdk_android_releasesПримеры кода: sdk_android_samplesdk_android можно загрузить с GitHub по следующим ссылкам:

## Обработка оповещений

### Обзор

ru_pp_callbacks.dita#ru_pp_callbacks/section_zlt_rgh_4bcиспользуемом платежном методе,При работе с sdk_android поддерживается использование оповещений, отправляемых платежной платформой Some Platform напрямую к веб-сервису мерчанта, и оповещений, передаваемых вначале от платежной формы к sdk_android, а затем — от sdk_android к мобильному приложению мерчанта. В первом случае набор параметров, передаваемых в оповещении, может отличаться в зависимости от установленных настроек (подробнее:). Во втором случае передается строго определенный набор параметров — данные о статусе, типе и идентификаторе платежа, дате проведения платежа,сумме и валюте платежа:

Далее представлена информация об этих оповещениях.

```
{
    "payment": {
        "status": "success",
        "type": "purchase",
        "id": "12345",
        "date": "2020-09-11T14:49:18+0000",
        "method": "card",
        "sum": 1000,
        "currency": "USD"
         }
}
```

### Оповещения платежной платформы

ru_pp_callbacks.ditaПри проведении платежа платежная платформа отправляет оповещения на указанный URL. Этот URL необходимо сообщить службе технической поддержки Some Platform. Подробная информация об оповещениях представлена в разделе.

### Оповещения sdk_android

Мобильное приложение мерчанта может принимать от sdk_android оповещения, которые передаются к sdk_android от платежной платформы. Оповещение отправляется мерчанту до того, как пользователю в платежной форме отображается страница с информацией о результате проведения платежа.

Информация о платеже передается через широковещательные сообщения, которые поступают в приложение мерчанта от sdk_android. Для получения этой информации мерчанту необходимо выполнить следующие шаги:

BroadcastReceiverДалее представлены примеры реализациидля получения информации о результате проведения платежа.

1. BroadcastReceiverСоздать приемник широковещательных сообщений — объект.
2. BroadcastReceiverIntent`sdk_android_callback_type.getValue()sdk_android_activity`При поступлении вшироковещательных сообщениях от sdk_android, с помощью ключаполучать из объектаинформацию о платеже. Ключ представляет собой константу, предоставляемую.
