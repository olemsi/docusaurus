# Обзор

## Схема работы

В проведении отдельного платежа с использованием title задействуются веб-сервис мерчанта, один из интерфейсов и платежная платформа Some Platform, а также технические средства банка.

## Основные операции

**ПроектыПлатежные методы**Уточнить минимальную и максимальную сумму платежа, доступную в вашем проекте, вы можете в Dashboard. Для этого в Dashboard перейдите в раздели выберите вкладку.

|  | Интерфейсы | references/ru/currencies/RUB.ditaСуммы, |  |  |  |
| --- | --- | --- | --- | --- | --- |
| Payment Widget | Gate | Dashboard | минимум | максимум |  |
| Оплата |  |  |  | 1,00 |  |
| Полный возврат |  |  |  |  |  |
| Частичный возврат |  |  |  |  |  |

## Сценарии использования

Проведение оплат с использованием метода title может выполняться двумя способами: с перенаправлением пользователей к сервису title или с отображением инструкции пользователю для самостоятельного завершения оплаты, проведение возвратов выполняется с уведомлением пользователей через веб-сервис мерчанта.

Детальные сведения о том, что необходимо делать со стороны мерчанта для проведения платежей, а также о том, что можно использовать для анализа информации о проведенных платежах и операциях, представлены далее.

| title — платежный метод, который позволяет проводить платежи через интернет-банкинг Альфа Банка. Проведение оплат реализовано через Payment Widget и Gate, проведение возвратов — через Gate.здесьЗагрузить логотип этого платежного метода в векторном формате можно. |  |
| --- | --- |

| Тип платежного метода | интернет-банкинг |
| --- | --- |
| Регионы использования | country_RU, country_KZ |
| Валюты платежей | references/ru/currencies/EUR.ditareferences/ru/currencies/KZT.ditareferences/ru/currencies/RUB.ditareferences/ru/currencies/USD.dita,,, |
| Конвертация валют |  |
| Оплата |  |
| Выплата |  |
| Оплата по сохраненным данным |  |
| Полный возврат |  |
| Частичный возврат |  |
| Опротестование |  |
| Особенности |  |
| Валюты расчетов с мерчантами | references/ru/currencies/RUB.dita |
| Скорость расчетов с мерчантами | без задержек |
| Организация и стоимость подключения | По согласованию с курирующим менеджером Some Platform |

---

## Оплата через Gate

### Общая информация

Проведение оплат с использованием метода title может выполняться по двум сценариям: с перенаправлением пользователей к сервису title или с отображением инструкции пользователю для самостоятельного завершения оплаты. В случае с перенаправлением для оплаты через Gate со стороны веб-сервиса необходимо отправить запрос, содержащий требуемые параметры и подпись, на рабочий URL Some Platform, осуществить перенаправление пользователя на сайт сервиса title и принять оповещение о результате оплаты. В случае с отображением инструкции — отправить запрос, содержащий требуемые параметры и подпись, на рабочий URL Some Platform, подготовить и отобразить инструкцию пользователю и принять оповещение о результате оплаты.

Полные схемы проведения оплаты представлены далее.

ru_gate_sts_protocol.ditaИнформация о формате запросов и параметрах инициализации оплат методом title через Gate, а также о формате оповещений о результатах оплат приведена далее, общая информация о работе с API см. в разделе.

### Формат запросов

При работе с запросами на оплаты с применением метода title необходимо учитывать следующее:

Вот пример данных из запроса на оплату с использованием метода title:

1. `/v2/payment/online-banking/alfa-click/sale`Должен использоваться запрос, отправляемый методом POST на. Эта точка входа относится к группе для проведения платежей с помощью интернет-банкинга  request-payment-online-banking-sale.
2. generalproject_id— идентификатор проекта, полученный от Some Platform при интеграции;payment_id— идентификатор платежа, уникальный в рамках проекта;signatureИспользование подписи к данным— подпись запроса, составленная после определения всех параметров запроса. (Подробнее см. раздел.);— объект, содержащий основные сведения о запросе:customerid— идентификатор пользователя, уникальный в рамках проекта;ip_address— IP-адрес устройства пользователя;email— адрес электронной почты пользователя;first_name— имя пользователя;last_name— фамилия пользователя;account_id— логин пользователя в сервисе title,phone— номер телефона (обязательный только для пользователей из Казахстана)— объект, содержащий сведения о пользователе:paymentamount*за исключениемосновных единицах валюты*ru_currency_codes.dita— сумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.;currency— код валюты оплаты в формате ISO-4217 alpha-3.— объект, содержащий сведения об оплате:В запросе должны использоваться следующие объекты и параметры:
3. return_urlreturn_urlsuccess— URL-адрес, на который нужно перенаправить пользователя в случае успешного завершения оплаты;decline— URL-адрес, на который нужно перенаправить пользователя в случае отклонения оплаты;returnsuccessdecline— URL-адрес, куда нужно перенаправить пользователя в случае прерывания оплаты пользователем до ее завершения. Этот же адрес используется, если не заданы параметрыи.— объект с информацией, куда следует перенаправлять пользователя при прерывании или завершении оплаты:Следующие параметры с адресами возврата пользователя необязательны, но их настоятельно рекомендуется передавать в запросе, чтобы сделать процесс оплаты максимально удобным для пользователя. Если не задан ни один из параметров объекта, то Gate по умолчанию перенаправляет пользователя по URL-адресу, указанному в вашем проекте в Some Platform.
4. references/ru/currencies/EUR.ditareferences/ru/currencies/KZT.ditareferences/ru/currencies/RUB.ditareferences/ru/currencies/USD.ditaВалютой платежа может быть,,,.
5. Дополнительно могут использоваться все параметры, указанные в спецификации.

### Формат данных для завершения платежа

Так как существует два сценария оплаты через сервис title, вы можете обратиться к вашему курирующему менеджеру Some Platform для консультации и выбора наиболее удобного сценария. Далее представлены формат данных, используемых для перенаправления пользователей, и пример инструкции для отображения пользователю.

После получения и обработки запроса на оплату платежная платформа направляет в ваш веб-сервис оповещение (callback) с данными для перенаправления пользователя в сервис provider_service.

redirect_dataЧтобы перенаправить пользователя, используйте значения следующих параметров, находящихся в объекте:

Вот пример фрагмента оповещения, содержащего данные для перенаправления пользователя.

Пример отображаемой инструкцииДля завершения оплаты с помощью инструкции со стороны веб-сервиса необходимо обеспечить подготовку и отображение пользователю этой инструкции с описанием дальнейших действий. Обязательно должен быть добавлен логотип сервиса title. Далее приведен пример инструкции, подробную информацию о подготовке вы можете уточнить у своего курирующего менеджера Some Platform.

- url— адрес для перенаправления пользователя;
- body— данные для отправки в теле запроса;
- method— метод запроса HTML-страницы сайта (например, POST или GET);
- encrypted— это служебный параметр. Игнорируйте содержащиеся в нем данные.

### Формат оповещений

ru_gate_callbacks.ditaДля оповещений о результатах оплат с применением метода title используется стандартный формат, описание которого представлено в разделе.

`386100,00 RUBaccount1`В следующем примере оповещение свидетельствует о том, что в рамках проектаот пользователя была успешно проведена оплата в размересо счета №.

В следующем примере оплата была отклонена из-за недостатка средств на счете пользователя.

### Дополнительные материалы

Для организации работы с оплатами через Gate также могут быть полезны следующие материалы:

- ru_gate_interaction_organisation.dita
- ru_gate_authentication.dita
- ru_gate_payment_model.dita
- ru_gate_statuses_and_response_codes.dita

---

## Возврат через Gate

### Общая информация

title поддерживает проведение полных и частичных возвратов. Для проведения возврата через Gate с использованием метода title со стороны веб-сервиса необходимо отправить запрос, содержащий требуемые параметры и подпись, на рабочий URL Some Platform и принять оповещение о результате. Полная схема проведения возврата представлена далее.

ru_gate_sts_protocol.ditaИнформация о формате запросов и параметрах инициализации возвратов методом title через Gate, а также о формате оповещений о результатах возвратов приведена далее, общая информация о работе с API см. в разделе.

### Формат запросов

При работе с запросами на возврат с применением метода title необходимо учитывать следующее:

Вот пример данных из запроса на возврат с использованием метода title:

1. `/v2/payment/online-banking/alfa-click/refund`Должен использоваться запрос, отправляемый методом POST на. Эта точка входа относится к группе для проведения платежей с помощью интернет-банкинга request-payment-online-banking-refund.
2. generalproject_id— идентификатор проекта;payment_id— идентификатор платежа;signatureИспользование подписи к данным— подпись запроса, составленная после указания целевых параметров (подробнее — в разделе);— объект, содержащий основные идентификационные сведения о запросе:customerip_address— IP-адрес устройства пользователя;— объект, содержащий сведения о пользователе:paymentdescription— комментарий или описание возврата,amount*за исключениемосновных единицах валюты*ru_currency_codes.ditaПараметр обязателен при частичном возврате.— сумма возврата в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см..currency— валюта возврата в формате ISO-4217 alpha-3 (обязательный при частичном возврате).— объект, содержащий сведения о возврате :В запросе должны использоваться следующие объекты и параметры:
3. Дополнительно могут использоваться все параметры, указанные в спецификации.

### Формат оповещений

ru_gate_callbacks.ditaДля оповещений о результатах возврата с применением метода title используется стандартный формат, описание которого представлено в разделе.

`386100,00 RUBaccount1`В следующем примере оповещение свидетельствует о том, что в рамках проектадля пользователя был успешно проведен частичный возврат в размерена счет.

В следующем примере возврат был отклонен, так как значение суммы в запросе на возврат больше суммы, указанной в начальном запросе.

### Дополнительные материалы

Для организации работы с возвратами через Gate также могут быть полезны следующие материалы:

- ru_gate_interaction_organisation.dita
- ru_gate_authentication.dita
- ru_gate_payment_model.dita
- ru_gate_statuses_and_response_codes.dita

---

## Оплата через Payment Widget

### Общая информация

ru_pp__preselectingps.ditaПроведение оплат с использованием метода title может выполняться двумя способами: с перенаправлением пользователей к сервису title или с отображением инструкции пользователю для самостоятельного завершения оплаты. В любом случае для оплаты через Payment Widget со стороны веб-сервиса необходимо отправить запрос, содержащий требуемые параметры и подпись, на рабочий URL Some Platform и принять оповещение о результате оплаты. При этом метод title можно сделать предварительно выбранным на Payment Widget (подробнее — в разделе). Полные схемы проведения оплаты представлены далее.

Описание Payment Widget APIИнформация о формате запросов и параметрах вызова Payment Widget при работе с title, а также о формате оповещений о результатах оплат приведена далее; общая информация о работе с API см. в разделе.

### Формат запросов

При формировании запросов на открытие платежной формы с применением метода title необходимо учитывать следующее:

Вот пример данных из запроса на открытие платежной формы с использованием метода title:

1. project_id— идентификатор проекта, полученный от Some Platform при интеграции;payment_id— идентификатор платежа, уникальный в рамках проекта;customer_id— идентификатор пользователя, уникальный в рамках проекта;customer_first_name— имя пользователя;customer_last_name— фамилия пользователя;customer_email— адрес электронной почты пользователя;payment_currency— код валюты платежа в формате ISO-4217 alpha-3;payment_amount*за исключениемосновных единицах валюты*ru_currency_codes.dita— сумма платежа в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см..Должен использоваться базовый минимум параметров, которые обязательны для любого платежного метода:
2. references/ru/currencies/EUR.ditareferences/ru/currencies/KZT.ditareferences/ru/currencies/RUB.ditareferences/ru/currencies/USD.ditaВалютой платежа может быть,,или.
3. customer_account_numberДополнительно должен использоваться параметр— идентификатор пользователя в сервисе title.
4. force_payment_method`force_pm`Для предварительного выбора метода title необходимо указывать код платежного метода в параметре—.
5. ru_pp_parameters.ditaДополнительно могут использоваться любые другие параметры из числа доступных для работы с Payment Widget. Детальная информация обо всех параметрах приведена в разделе.
6. ru_pp_authentication.ditaПосле определения всех параметров необходимо составить подпись. Подробную информацию см. в.

### Формат оповещений

ru_pp_callbacks.ditaДля оповещений о результатах оплат с применением метода title используется стандартный формат, описание которого представлено в разделе.

`386100,00 RUBaccount1`В следующем примере оповещение свидетельствует о том, что в рамках проектаот пользователя была успешно проведена оплата в размересо счета №.

В следующем примере оплата была отклонена из-за недостатка средств на счете пользователя.

### Дополнительные материалы

Для организации работы с оплатами через Payment Widget также могут быть полезны следующие материалы:

- ru_pp_interaction_organisation.dita
- ru_pp_authentication.dita
- ru_pp_payment_model.dita
- ru_pp_statuses_and_response_codes.dita

---

## Тестирование

### Общая информация

Для метода title доступно тестирование оплат через Payment Widget и Gate, а также возвратов через Gate.

Тестирование может выполняться в рамках тестового проекта, и для подключения и отключения этой функциональности необходимо обращаться к специалистам технической поддержки Some Platform
                support@example.com

references/ru/currencies/RUB.ditaПри проведении тестовых платежей следует учитывать, что в запросах должен указываться идентификатор тестового проекта, в качестве валюты может использоваться только, а интерфейсы эмулятора платежных форм Payment Widget и title могут отличаться от рабочих.

### Статусы тестовых платежей

При тестировании оплат их итоговые статусы определяются исходя из сумм, указанных в запросах:

При тестировании возвратов их итоговые статусы определяются исходя из сумм, указанных в запросах:

### Оплата через Payment Widget

Для проведения тестовой оплаты через Payment Widget необходимо:

**SuccessDecline**`decline`Если при переходе на страницу эмулятора платежной формы title кнопкаилине щелкнута в течение 30 минут, то со стороны платежной платформы отправляется оповещение о переводе платежа в статус(независимо от суммы, указанной в запросе).

ru_pm_alfa_pp_purchase.ditaПодробная информация о проведении оплат с использованием метода title через Payment Widget представлена в пункте.

1. Отправить в платежную платформу корректный тестовый запрос на открытие Payment Widget.
2. `force_pm`Если в запросе не был указан метод— выбрать его на странице эмулятора.
3. **SuccessDecline**Щелкнуть кнопкуили(в зависимости от запрашиваемой суммы).
4. Принять оповещение с информацией о результате оплаты.

### Оплата через Gate

Для проведения тестовой оплаты через Gate необходимо:

**SuccessDecline**`decline`Если при переходе на страницу эмулятора платежной формы title кнопкаилине щелкнута в течение 30 минут, то со стороны платежной платформы отправляется оповещение о переводе платежа в статус(независимо от суммы, указанной в запросе).

ru_pm_alfa_gate_purchase.ditaПодробная информация о проведении оплат с использованием метода title через Gate представлена в пункте.

1. Отправить в платежную платформу корректный тестовый запрос на оплату.
2. Принять оповещение с данными для перенаправления.
3. **SuccessDecline**Перейти по полученному URL и щелкнуть кнопкуили(в зависимости от запрашиваемой суммы) — на странице эмулятора.
4. Принять оповещение с информацией о результате оплаты.

### Возврат через Gate

Для проведения тестового возврата через Gate необходимо отправить в платежную платформу корректный тестовый запрос на возврат средств, полученных по ранее проведенной тестовой оплате, и принять оповещение с информацией о результате.

ru_pm_alfa_gate_refund.ditaСледует учитывать, что выполнение тестового возврата на полную или частичную сумму возможно только в течение суток после проведения оплаты. Подробная информация о проведении возвратов с использованием метода title через Gate представлена в пункте.

---

## Оплата через Gate

### Общая информация

Проведение оплат с использованием метода title может выполняться по двум сценариям: с перенаправлением пользователей к сервису title или с отображением инструкции пользователю для самостоятельного завершения оплаты. В случае с перенаправлением для оплаты через Gate со стороны веб-сервиса необходимо отправить запрос, содержащий требуемые параметры и подпись, на рабочий URL Some Platform, осуществить перенаправление пользователя на сайт сервиса title и принять оповещение о результате оплаты. В случае с отображением инструкции — отправить запрос, содержащий требуемые параметры и подпись, на рабочий URL Some Platform, подготовить и отобразить инструкцию пользователю и принять оповещение о результате оплаты.

Полные схемы проведения оплаты представлены далее.

ru_gate_sts_protocol.ditaИнформация о формате запросов и параметрах инициализации оплат методом title через Gate, а также о формате оповещений о результатах оплат приведена далее, общая информация о работе с API см. в разделе.

### Формат запросов

При работе с запросами на оплаты с применением метода title необходимо учитывать следующее:

Вот пример данных из запроса на оплату с использованием метода title:

1. `/v2/payment/online-banking/alfa-click/sale`Должен использоваться запрос, отправляемый методом POST на. Эта точка входа относится к группе для проведения платежей с помощью интернет-банкинга  request-payment-online-banking-sale.
2. generalproject_id— идентификатор проекта, полученный от Some Platform при интеграции;payment_id— идентификатор платежа, уникальный в рамках проекта;signatureИспользование подписи к данным— подпись запроса, составленная после определения всех параметров запроса. (Подробнее см. раздел.);— объект, содержащий основные сведения о запросе:customerid— идентификатор пользователя, уникальный в рамках проекта;ip_address— IP-адрес устройства пользователя;email— адрес электронной почты пользователя;first_name— имя пользователя;last_name— фамилия пользователя;account_id— логин пользователя в сервисе title,phone— номер телефона (обязательный только для пользователей из Казахстана)— объект, содержащий сведения о пользователе:paymentamount*за исключениемосновных единицах валюты*ru_currency_codes.dita— сумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.;currency— код валюты оплаты в формате ISO-4217 alpha-3.— объект, содержащий сведения об оплате:В запросе должны использоваться следующие объекты и параметры:
3. return_urlreturn_urlsuccess— URL-адрес, на который нужно перенаправить пользователя в случае успешного завершения оплаты;decline— URL-адрес, на который нужно перенаправить пользователя в случае отклонения оплаты;returnsuccessdecline— URL-адрес, куда нужно перенаправить пользователя в случае прерывания оплаты пользователем до ее завершения. Этот же адрес используется, если не заданы параметрыи.— объект с информацией, куда следует перенаправлять пользователя при прерывании или завершении оплаты:Следующие параметры с адресами возврата пользователя необязательны, но их настоятельно рекомендуется передавать в запросе, чтобы сделать процесс оплаты максимально удобным для пользователя. Если не задан ни один из параметров объекта, то Gate по умолчанию перенаправляет пользователя по URL-адресу, указанному в вашем проекте в Some Platform.
4. references/ru/currencies/EUR.ditareferences/ru/currencies/KZT.ditareferences/ru/currencies/RUB.ditareferences/ru/currencies/USD.ditaВалютой платежа может быть,,,.
5. Дополнительно могут использоваться все параметры, указанные в спецификации.

### Формат данных для завершения платежа

Так как существует два сценария оплаты через сервис title, вы можете обратиться к вашему курирующему менеджеру Some Platform для консультации и выбора наиболее удобного сценария. Далее представлены формат данных, используемых для перенаправления пользователей, и пример инструкции для отображения пользователю.

После получения и обработки запроса на оплату платежная платформа направляет в ваш веб-сервис оповещение (callback) с данными для перенаправления пользователя в сервис provider_service.

redirect_dataЧтобы перенаправить пользователя, используйте значения следующих параметров, находящихся в объекте:

Вот пример фрагмента оповещения, содержащего данные для перенаправления пользователя.

Пример отображаемой инструкцииДля завершения оплаты с помощью инструкции со стороны веб-сервиса необходимо обеспечить подготовку и отображение пользователю этой инструкции с описанием дальнейших действий. Обязательно должен быть добавлен логотип сервиса title. Далее приведен пример инструкции, подробную информацию о подготовке вы можете уточнить у своего курирующего менеджера Some Platform.

- url— адрес для перенаправления пользователя;
- body— данные для отправки в теле запроса;
- method— метод запроса HTML-страницы сайта (например, POST или GET);
- encrypted— это служебный параметр. Игнорируйте содержащиеся в нем данные.

### Формат оповещений

ru_gate_callbacks.ditaДля оповещений о результатах оплат с применением метода title используется стандартный формат, описание которого представлено в разделе.

`386100,00 RUBaccount1`В следующем примере оповещение свидетельствует о том, что в рамках проектаот пользователя была успешно проведена оплата в размересо счета №.

В следующем примере оплата была отклонена из-за недостатка средств на счете пользователя.

### Дополнительные материалы

Для организации работы с оплатами через Gate также могут быть полезны следующие материалы:

- ru_gate_interaction_organisation.dita
- ru_gate_authentication.dita
- ru_gate_payment_model.dita
- ru_gate_statuses_and_response_codes.dita

---

## Возврат через Gate

### Общая информация

title поддерживает проведение полных и частичных возвратов. Для проведения возврата через Gate с использованием метода title со стороны веб-сервиса необходимо отправить запрос, содержащий требуемые параметры и подпись, на рабочий URL Some Platform и принять оповещение о результате. Полная схема проведения возврата представлена далее.

ru_gate_sts_protocol.ditaИнформация о формате запросов и параметрах инициализации возвратов методом title через Gate, а также о формате оповещений о результатах возвратов приведена далее, общая информация о работе с API см. в разделе.

### Формат запросов

При работе с запросами на возврат с применением метода title необходимо учитывать следующее:

Вот пример данных из запроса на возврат с использованием метода title:

1. `/v2/payment/online-banking/alfa-click/refund`Должен использоваться запрос, отправляемый методом POST на. Эта точка входа относится к группе для проведения платежей с помощью интернет-банкинга request-payment-online-banking-refund.
2. generalproject_id— идентификатор проекта;payment_id— идентификатор платежа;signatureИспользование подписи к данным— подпись запроса, составленная после указания целевых параметров (подробнее — в разделе);— объект, содержащий основные идентификационные сведения о запросе:customerip_address— IP-адрес устройства пользователя;— объект, содержащий сведения о пользователе:paymentdescription— комментарий или описание возврата,amount*за исключениемосновных единицах валюты*ru_currency_codes.ditaПараметр обязателен при частичном возврате.— сумма возврата в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см..currency— валюта возврата в формате ISO-4217 alpha-3 (обязательный при частичном возврате).— объект, содержащий сведения о возврате :В запросе должны использоваться следующие объекты и параметры:
3. Дополнительно могут использоваться все параметры, указанные в спецификации.

### Формат оповещений

ru_gate_callbacks.ditaДля оповещений о результатах возврата с применением метода title используется стандартный формат, описание которого представлено в разделе.

`386100,00 RUBaccount1`В следующем примере оповещение свидетельствует о том, что в рамках проектадля пользователя был успешно проведен частичный возврат в размерена счет.

В следующем примере возврат был отклонен, так как значение суммы в запросе на возврат больше суммы, указанной в начальном запросе.

### Дополнительные материалы

Для организации работы с возвратами через Gate также могут быть полезны следующие материалы:

- ru_gate_interaction_organisation.dita
- ru_gate_authentication.dita
- ru_gate_payment_model.dita
- ru_gate_statuses_and_response_codes.dita

---

## Оплата через Payment Widget

### Общая информация

ru_pp__preselectingps.ditaПроведение оплат с использованием метода title может выполняться двумя способами: с перенаправлением пользователей к сервису title или с отображением инструкции пользователю для самостоятельного завершения оплаты. В любом случае для оплаты через Payment Widget со стороны веб-сервиса необходимо отправить запрос, содержащий требуемые параметры и подпись, на рабочий URL Some Platform и принять оповещение о результате оплаты. При этом метод title можно сделать предварительно выбранным на Payment Widget (подробнее — в разделе). Полные схемы проведения оплаты представлены далее.

Описание Payment Widget APIИнформация о формате запросов и параметрах вызова Payment Widget при работе с title, а также о формате оповещений о результатах оплат приведена далее; общая информация о работе с API см. в разделе.

### Формат запросов

При формировании запросов на открытие платежной формы с применением метода title необходимо учитывать следующее:

Вот пример данных из запроса на открытие платежной формы с использованием метода title:

1. project_id— идентификатор проекта, полученный от Some Platform при интеграции;payment_id— идентификатор платежа, уникальный в рамках проекта;customer_id— идентификатор пользователя, уникальный в рамках проекта;customer_first_name— имя пользователя;customer_last_name— фамилия пользователя;customer_email— адрес электронной почты пользователя;payment_currency— код валюты платежа в формате ISO-4217 alpha-3;payment_amount*за исключениемосновных единицах валюты*ru_currency_codes.dita— сумма платежа в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см..Должен использоваться базовый минимум параметров, которые обязательны для любого платежного метода:
2. references/ru/currencies/EUR.ditareferences/ru/currencies/KZT.ditareferences/ru/currencies/RUB.ditareferences/ru/currencies/USD.ditaВалютой платежа может быть,,или.
3. customer_account_numberДополнительно должен использоваться параметр— идентификатор пользователя в сервисе title.
4. force_payment_method`force_pm`Для предварительного выбора метода title необходимо указывать код платежного метода в параметре—.
5. ru_pp_parameters.ditaДополнительно могут использоваться любые другие параметры из числа доступных для работы с Payment Widget. Детальная информация обо всех параметрах приведена в разделе.
6. ru_pp_authentication.ditaПосле определения всех параметров необходимо составить подпись. Подробную информацию см. в.

### Формат оповещений

ru_pp_callbacks.ditaДля оповещений о результатах оплат с применением метода title используется стандартный формат, описание которого представлено в разделе.

`386100,00 RUBaccount1`В следующем примере оповещение свидетельствует о том, что в рамках проектаот пользователя была успешно проведена оплата в размересо счета №.

В следующем примере оплата была отклонена из-за недостатка средств на счете пользователя.

### Дополнительные материалы

Для организации работы с оплатами через Payment Widget также могут быть полезны следующие материалы:

- ru_pp_interaction_organisation.dita
- ru_pp_authentication.dita
- ru_pp_payment_model.dita
- ru_pp_statuses_and_response_codes.dita

