# sdk_js

sdk_js — это набор средств разработки для взаимодействия веб-сервисов, разработанных на JavaScript, с платежной платформой Some Platform при проведении оплат через Payment Widget. В этом разделе представлена информация о работе с sdk_js с примерами кода на языке программирования JavaScript.

sdk_js совместим с Java​Script на платформе Node.js 4.x и доступен для загрузки на GitHub по следующей ссылке:.

## Возможности

подписывать набор параметров платежа и формировать запрос для вызова Payment Widget,проверять подлинность оповещений от Some Platform и получать из них информацию о платежах.sdk_js позволяет:

## Состав

sdk_js содержит библиотеки и служебные файлы. Для работы могут использоваться:

- **src**— библиотека для разработки,
- **_tests_**— библиотека для автоматизированного тестирования.

## Порядок работы

Для использования sdk_js необходимо:

При возникновении вопросов о работе с sdk_js следует обращаться в службу технической поддержки Some Platform по адресу support@example.com

1. Если у компании нет идентификатора проекта и ключа для взаимодействия с Some Platform — отправить заявку на подключение.Если у компании идентификатор и ключ для взаимодействия с Some Platform есть — сообщить специалистам технической поддержки о намерении интеграции с использованием sdk_js и согласовать с ними порядок тестирования.Решить организационные вопросы, касающиеся взаимодействия с Some Platform:
2. Установить библиотеки, входящие в состав sdk_js, в каталог с исходным кодом веб-сервиса и подключить их в коде, а также доработать код для использования необходимой функциональности.
3. **_tests_**Для тестирования следует использовать тестовый идентификатор проекта, тестовые значения параметров платежа и пример оповещения из библиотеки.project_idДля перевода в рабочий режим необходимо заменить тестовое значениена рабочее, полученное от Some Platform.Протестировать и запустить в работу обновленный исходный код веб-сервиса.

## Установка и подключение библиотек

Устанавливать библиотеки, входящие в состав sdk_js, в проект с исходным кодом веб-сервиса можно вручную или автоматически с помощью систем управления пакетами Yarn или npm, которые обеспечивают загрузку необходимых библиотек из репозитория.

Чтобы установить библиотеки с помощью Yarn или npm и подключить их в исходном коде веб-сервиса, необходимо:

1. [https://yarnpkg.com/en/docs/getting-started](https://yarnpkg.com/en/docs/getting-started)Yarn:[https://docs.npmjs.com/](https://docs.npmjs.com/)npm:Если система управления пакетами не установлена — загрузить, установить и настроить. Подробнее:
2. npm install sdk_js_lib // для Yarn
yarn add sdk_js_lib    // для npmВ командной строке операционной системы перейти в каталог с исходным кодом веб-сервиса и выполнить одну из команд:Эта команда служит для загрузки и размещения библиотек и их зависимостей в каталоге с модулями для подключения необходимых модулей в коде веб-сервиса.
3. const { Payment } = require('sdk_js_lib');
const { Callback } = require('sdk_js_lib');Подключить модули в исходном коде веб-сервиса:

## Вызов платежной формы

Запрос для вызова Some Platform формируется из набора параметров, подписываемых для обеспечения защиты данных при передаче запроса в платежную платформу Some Platform. sdk_js позволяет автоматически подписывать параметры и формировать из них запрос. Для вызова Payment Widget с использованием sdk_js следует:

Далее приведен пример формирования запроса для вызова платежной формы Payment Widget с открытием на английском языке в отдельной вкладке браузера. На странице с выбором платежных методов обеспечивается отображение информации о платеже: идентификатора, валюты, суммы и описания платежа.

1. `Payment`*pp_hostsecret_key*const e = new Payment('<>', '186', '<>');
        // Идентификатор проекта и секретный ключ
e.paymentId = '1555943554067';
        // Идентификатор платежа, уникальный в рамках проекта
e.paymentAmount = 1000;
        // Сумма в дробных единицах валюты
e.paymentCurrency = 'RUB';
        // Код валюты в формате ISO-4217 alpha-3
e.paymentDescription = 'Описание платежа';
       // Описание платежа. Необязательный параметр*pp_host*Здесь вместо <> надо указать доменное имя Payment Widget, которое следует узнавать у своего курирующего менеджера или в службе поддержки.ru_pp_parameters.ditaВсе параметры в данном примере, за исключением описания платежа, являются необходимыми для любой оплаты. Дополнительно можно использовать любые другие параметры из числа доступных для работы с Payment Widget. Подробнее о доступных параметрах — в разделе.Создать объект классаи указать значения параметров платежа и значение секретного ключа, полученное от Some Platform. Секретный ключ необходим для автоматического подписывания параметров.
2. const url = e.getUrl();domen_PP/payment?signature=OEKRlLXKStyo
...%3D%3D&amp;payment_id=1555943554067...123Корректный запрос для вызова платежной формы содержит подпись и параметры платежа:Сформировать запрос для вызова платежной формы.
3. Использовать сформированный запрос для вызова платежной формы.

## Обработка оповещений

**HTTPPOSTJSONJSON**Информацию о результатах проведения платежей можно получать в оповещениях, отправляемых со стороны Some Platform на URL, который необходимо сообщить службе технической поддержки Some Platform. Оповещение представляет собойзапрос с данными в формате-строки. Чтобы извлечь информацию о результате проведения платежа из-строки, необходимо:

Далее приведен пример оповещения, которое включает в себя подпись и информацию о результатах проведения платежа. При использовании sdk_js проверка подписи в оповещении выполняется автоматически.

1. `Callback`*secret_key*const callback = new Callback(<>, req.body);Создать объект класса, используя значение секретного ключа, полученного от Some Platform, и данные из оповещения от Some Platform:
2. *secret_key*app.post('/payment/callback', function(req, res) {
  const callback = new Callback(<>, req.body);
  if (callback.isPaymentSuccess()) {
    const paymentCont = callback.payment();    // Получение всей информации о платеже
    const paymentId = callback.getPaymentId(); // Получение идентификатора платежа

    // Здесь размещается исходный код для обработки оповещения проведенного платежа
  }
});Использовать методы и свойства, доступные для работы с оповещениями. В следующем примере используются метод и свойство в исходном коде веб-сервиса, разработанном с использованием Express:

## Дополнительные материалы

ru_pp_callbacks.ditaru_pp_payment_model.ditaДля организации работы с оповещениями также могут быть полезны следующие материалы:
