# Оповещение (callback)

*оповещения*ru_pp_callbacks.ditaДля передачи промежуточной и итоговой информации о результате обработки и выполнения запроса в рамках асинхронного взаимодействия используются(callbacks) — HTTP-запросы, отправляемые методом POST от платежной платформы на согласованные адреса. Общая структура оповещений описана далее, а подробная информация о работе с ними — в разделе.

В каждом оповещении от платформы содержатся следующие элементы в порядке перечисления:

Далее представлен пример оповещения с информацией о результате проведения платежа. Содержимое JSON-строки в этом примере разбито на несколько строк для удобства чтения.

- `POST/notify/successHTTP/1.1`стартовая строка с указанием метода передачи запроса (), URL веб-сервиса для отправки оповещений о результатах (в примере —), протокола и его версии ();
- `Hostwebservice.com`поля заголовка, в том числе полес указанием доменного имени веб-сервиса (в примере —);
- пустая строка — разделитель, отделяющая служебную информацию от тела сообщения;
- тело сообщения, содержащее JSON-строку в кодировке UTF-8 с набором данных и подписью к ним.

## Ответ

*ответа*Payment Widget подтверждает получение запроса отправкой в пользовательский браузер(response) — HTTP-сообщения определенной структуры — в рамках того же сеанса. Передаваемые в ответе данные содержат:

Перечень кодов ответов и пояснительных фраз, используемых в ответах, приведен далее в таблице.

- сведения о приеме запроса, если запрос принят в обработку;
- сведения об ошибке, если запрос не принят в обработку.

| Код с пояснением | Описание |
| --- | --- |
| 200 OK | Запрос успешно принят, ожидайте итоговое оповещение.Пользователю отображается платежная форма |
| 400 Bad Request | Запрос не принят из-за отсутствия в нем обязательного параметра, например идентификатора проекта, или из-за некорректной подписи.Пользователю отображается сообщение об ошибке |
| 404 Not Found | `project_idurlBase`Запрос не принят в обработку из-за некорректных значений параметровили.Пользователю отображается сообщение об ошибке |
| 500 Internal Error | Запрос не принят в обработку из-за сбоя в платежной платформе.Пользователю отображается сообщение об ошибке |

## Схема взаимодействия между пользователем, веб-сервисом и Payment Widget

При использовании Payment Widget выполнение любого действия подразумевает взаимодействие между тремя основными сторонами: пользователем, веб-сервисом и Payment Widget:

Порядок взаимодействия между этими и другими сторонами представлен далее на схеме.

- **Взаимодействие веб-сервиса и Payment Widget**строится на обмене HTTP-сообщениями по принципу «запрос-ответ». В такой схеме клиентская часть веб-сервиса отправляет в Payment Widget запросы, а Payment Widget отправляет синхронные ответы в клиентскую часть веб-сервиса и асинхронные оповещения — в серверную части веб-сервиса. Как правило, Payment Widget отправляет итоговые оповещения о результатах выполнения запроса, но в некоторых случаях могут отправляться и промежуточные оповещения, например если пользователь повторно пытется ввести данные. Кроме того, в веб-сервисе можно реализовать отслеживание интерфейсных событий в Payment Widget с помощью встраиваемой библиотеки.
- **Взаимодействие пользователя и Payment Widget**осуществляется через пользовательский интерфейс платежной формы и технически также основано на обмене HTTP-сообщениями по принципу «запрос-ответ» между пользовательским браузером и Payment Widget.

---

## Доработка веб-сервиса мерчанта

### Общая информация

До начала работы с Payment Widget вам нужно в своем веб-сервисе реализовать:

Информация о настройке веб-сервиса с использованием собственных решений и каждого из специализированных компонентов, предоставляемых Some Platform, представлена далее.

- сбор параметров, необходимых для проведения платежа;
- формирование подписи;
- открытие Payment Widget;
- обработку оповещений;
- обработку оповещений (callbacks) с отправкой ответов об их приеме.

### Использование собственных решений

Для настройки взаимодействия с Payment Widget с использованием собственных решений на стороне веб-сервиса необходимо обеспечить выполнение всех требуемых действий в клиентской и серверной частях.

1. Подготовить решение для сбора данных, необходимых для вызова платежной формы. Минимальный набор таких данных для проведения оплаты содержит идентификаторы проекта, платежа и пользователя, а также сумму и код валюты платежа.`${empty}`Если выбран способ открытия Payment Widget в модальном окне или в элементе iframe HTML-страницы — подключить CSS-библиотеку, добавив ее на страницу веб-сервиса с помощью элемента, который следует разместить внутри заголовка страницы.Это необходимо для корректного отображения платежной формы при указанных способах открытия. Если же выбран способ открытия в отдельной вкладке браузера, подключение этих библиотек не требуется.${empty}
...
<!-- Подключение CSS-библиотеки -->
<link rel="stylesheet" href="domen_PP/shared/merchant.css" />
...
*pp_host*Здесь вместо <> надо указать доменное имя Payment Widget, которое следует узнавать у своего курирующего менеджера или в службе поддержки.подробнее()Если выбран способ открытия Payment Widget в модальном окне и необходимо оформление модального окна в соответствии с дизайном веб-сервиса — определить стили CSS-классов для этого оформления. При использовании способов открытия в элементе iframe и в отдельной вкладке браузера такое оформление не используется.Реализовать отправку собранных данных в серверную часть веб-сервиса для их подписывания и обеспечить прием подписанных данных от серверной части.Реализовать возможности вызова Payment Widget выбранными способами: в отдельной вкладке браузера, в модальном окне и/или в элементе iframe HTML-страницы.Если необходимо фиксировать определенные интерфейсные события — открывать Payment Widget в модальном окне или в элементе iframe HTML-страницы и реализовать функции для обработки таких событий.В клиентской части веб-сервиса:
2. Обеспечить прием данных для подписывания от клиентской части веб-сервиса.Реализовать алгоритм формирования подписи данных.Обеспечить отправку подписанных данных в клиентскую часть веб-сервиса.Обеспечить прием оповещений от платежной платформы, с проверкой подписи и отправкой ответов о приеме, а также обработку информации из этих оповещений.В серверной части веб-сервиса:

### Использование JavaScript-библиотеки

подробнееJavaScript-библиотека Some Platform подключается к клиентской части веб-сервиса и позволяет открывать Payment Widget в модальном окне и в элементе iframe HTML-страницы. Кроме того, использование такой библиотеки позволяет получать информацию об интерфейсных событиях в платежной форме, в том числе о выборе платежного метода, изменении значений полей ввода и других ().

и SDK для веб-сервисовЕсли используется JavaScript-библиотека, на стороне серверной части могут использоваться собственные решения. На стороне клиентской части необходимо:

- Подготовить решение для сбора данных, необходимых для вызова платежной формы. Минимальный набор таких данных для проведения оплаты содержит идентификаторы проекта, платежа и пользователя, а также сумму и код валюты платежа.
- `${empty}${empty}`Подключить CSS- и JavaScript-библиотеки, добавив к странице веб-сервиса с помощью элементовдля CSS-библиотеки идля JavaScript-библиотеки. Эти элементы следует разместить внутри заголовка страницы.${empty}
...
<!-- Подключение CSS-библиотеки -->
<link rel="stylesheet" href="domen_PP/shared/merchant.css" /> 
<!-- Подключение JavaScript-библиотеки -->
${empty}

...
*pp_host*Здесь вместо <> надо указать доменное имя Payment Widget, которое следует узнавать у своего курирующего менеджера или в службе поддержки.
- подробнее()Если выбран способ открытия Payment Widget в модальном окне и необходимо оформление модального окна в соответствии с дизайном веб-сервиса — определить стили CSS-классов для этого оформления. При использовании способов открытия в элементе iframe и в отдельной вкладке браузера такое оформление не используется.
- Реализовать отправку собранных данных в серверную часть веб-сервиса для их подписывания и обеспечить прием подписанных данных от серверной части.
- по щелчку кнопкипо иному событиюРеализовать возможности вызова Payment Widgetилив пользовательском интерфейсе выбранным способом: в модальном окне или в элементе iframe HTML-страницы — с использованием JavaScript-объекта EPayWidget.
- Если необходимо фиксировать определенные интерфейсные события — открывать Payment Widget в модальном окне или в элементе iframe HTML-страницы и реализовать функции для обработки таких событий.

### Использование SDK для веб-сервисов

SDK для веб-сервисов встраиваются в серверную часть веб-сервиса и позволяют подписывать запросы и обрабатывать оповещения. Если используются эти SDK, на стороне клиентской части могут использоваться как собственные решения, так и библиотека JavaScript и SDK для мобильных приложений. А на стороне серверной части необходимо обеспечить:

ru_sdk_overview.ditaПодробная информация о подключении и использовании всех доступных SDK представлена в разделе.

- Прием запросов от клиентской части веб-сервиса.
- Формирование подписи с использованием библиотек из состава SDK.
- Отправку подписанного набора параметров в клиентскую часть веб-сервиса.
- Прием оповещений и отправку ответов о приеме, а также использовать методы, доступные для работы с оповещениями при использовании SDK для веб-сервисов.

---

## Запрос

### Общая информация

*в запросах*При работе с Payment Widget все данные от веб-сервиса должны передаваться(requests) — HTTP-сообщениях заданной структуры. При этом формат данных и параметры адресации таких запросов зависят от выбранного способа открытия платежной формы.

В этом разделе представлена информация о поддерживаемых методах отправки запросов на открытие Payment Widget, форматах данных и параметрах адресации таких запросов.

### Методы отправки

Вне зависимости от выбранного способа открытия Payment Widget для отправки HTTP-запросов поддерживаются методы POST и GET. Выбор метода осуществляется на стороне веб-сервиса и не ограничивается со стороны платежной платформы. По умолчанию, если в HTTP-запросе не указано другое, запросы идентифицируются как отправленные методом GET. В некоторых браузерах максимальная длина URL-адреса ограничена, поэтому при передаче таких сведений, как информация о товарных позициях или «длинная запись», рекомендуется использовать метод POST.

Основные характеристики методов POST и GET представлены далее.

* Ограничение объема передаваемых данных допустимой длиной URL.

|  | POST | GET |
| --- | --- | --- |
| Уровень безопасности | Более высокий за счет передачи параметров в теле запроса | Менее высокий в связи с передачей параметров в адресной строке |
| Возможность передачи всех параметров Payment Widget API |  | * |
| Возможность передавать в виде ссылки |  |  |

::: tip
*pp_host*Здесь вместо <> надо указать доменное имя Payment Widget, которое следует узнавать у своего курирующего менеджера или в службе поддержки.
:::

### Параметры адресации

При формировании запросов на открытие Payment Widget в отдельной вкладке браузера адреса отправки указываются следующим образом:

При формировании запросов на открытие Payment Widget в элементе iframe или в модальном окне, как правило, адреса отправки указывать не требуется.

- `?=&amp;`Если для отправки запросов используется метод GET, в качестве доменного имени запрашиваемого ресурса указывается базовый адрес Payment Widget (например, domen_PP), а затем следуют: URL-путь для выполнения целевых действий через Payment Widget, знак вопросаи строка данных, состоящая из пар «имя-значение» параметра. Имена и значения параметров разделяются знаком «равно» (), а сами пары разделяются знаками «амперсанд» (). Информацию об URL-адресе Payment Widget можно получить у своего курирующего менеджера Some Platform.// URL-путь для выполнения целевых действий через Payment Widget
/payment

// Строка данных
payment_currency=EUR&amp;project_id=42&amp;payment_amount=1000&amp;customer_id=123&amp;payment_id=4438&amp;signature=AE5hmtzdP0Dt7qGTg%3D%3D

// Полный адрес
domen_PP/payment?payment_currency=EUR&amp;project_id=42&amp;payment_amount=1000&amp;customer_id=123&amp;payment_id=4438&amp;signature=AE5hmtzdP0Dt7qGTg%3D%3D*pp_host*Здесь вместо <> надо указать доменное имя Payment Widget, которое следует узнавать у своего курирующего менеджера или в службе поддержки.
- `https://paymentpage.example.com`Если для отправки запросов используется метод POST, в качестве доменного имени указывают базовый адрес Payment Widget (например,), а затем добавляют URL-путь для выполнения нужного действия через Payment Widget. Данные в этом случае передаются в теле запроса. Базовый адрес Payment Widget узнавайте у своего курирующего менеджера Some Platform.
