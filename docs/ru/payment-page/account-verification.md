# Проверка платежной карты

ru_pp_recurring.ditaПроверка действительности платежной карты нужна, чтобы узнать, может ли карта использоваться для проведения платежей. Это может быть актуально перед проведением выплаты пользователю или при регистрации подписки на товары и услуги без списания средств за первый (пробный) период. Подробнее о регистрации повторяемой оплаты см..

В этом разделе рассказывается, как проверить действительность платежной карты с использованием Payment Widget.

`card_verifyaccount verification`Чтобы инициировать проверку карты, в параметрах запроса на вызов платежной формы (пример запроса будет представлен далее) укажите режим работы. После этого пользователь должен подтвердить свое согласие на проверку. Далее ваша система направляет в платежную платформу запрос, для выполнения которого в платформе формируется операция.

Есть два варианта проверки:

условный (нулевой) перевод денежных средств от пользователя на ваш счет;реальная (ненулевая) блокировка средств пользователя с последующей отменой. Сумму блокировки вы можете согласовать со своим курирующим менеджером, а срок отмены блокировки может составлять до 45 дней.

ru_pp_purchase_auth.ditaВ данном разделе мы рассмотрим вариант проверки карты с помощью нулевого платежа. Подробнее о блокировке средств см..

В процессе проверки карты Payment Widget может потребовать от пользователя указать реквизиты карты, сохранить реквизиты карты для последующих платежей и/или подождать уведомления о результате проверки.

## Шаги проверки карт

Для проверки действительности карты через Payment Widget:

Далее рассказывается о структуре запросов на проверку действительности платежных карт и о структуре оповещений с результатами проверки.

1. Сформируйте и отправьте в платежную платформу запрос на открытие Payment Widget.
2. **Аутентификация 3ds**ru_3ds2_about_pp.ditaТакая аутентификация предназначена для обеспечения безопасности оплаты с использованием карт через интернет. Подробнее об этой процедуре см..**Дополнение информации о платеже**ru_pp_clarification.ditaЭта процедура используется, когда по запросу одной из сторон, участвующих в проведении платежа, требуется предоставить дополнительную информацию. Подробная информация о процедуре представлена в разделе.При необходимости выполните вспомогательные процедуры, инициируемые платежной платформой. Это может быть один из вариантов аутентификации пользователя или дополнение информации о платеже:
3. Примите от платежной платформы оповещение (callback) о результате проверки.

## Запрос

В запросе на открытие Payment Widget для проверки действительности карты укажите необходимые параметры:

`EPayWidget`Вот пример параметров из запроса на открытие Payment Widget с использованием виджета:

Пример GET-запроса на открытие Payment Widget:

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе. |  |
| --- | --- |
| Параметр | Описание |
| modetype_string, type_strictly_required | `card_verify`Режим работы Payment Widget. Для проверки карты укажите в этом параметре значение.`card_verify`Пример: |
| project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |
| customer_idtype_string type_strictly_required | `customer_123`Пример:Идентификатор пользователя, уникальный в рамках проекта. |
| customer_first_nametype_string type_strictly_required | `John`Пример:Имя пользователя. |
| customer_last_nametype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |
| customer_emailtype_string type_strictly_required | `johndoe@example.com`Пример:Адрес электронной почты пользователя. |
| payment_currencytype_string type_strictly_required | `references/ru/currencies/USD.dita`Пример:Код валюты оплаты в формате ISO-4217 alpha-3. |
| payment_amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.ditareferences/ru/currencies/USD.dita`10000`Пример: 100,00передается какДля проверки карты сумма платежа должна быть равна нулю.Сумма оплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| signaturetype_string type_strictly_required | ru_pp_authentication.ditaПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |
| Для проверки платежной карты по токену: |  |
| account_tokentype_string, type_strictly_required | `42ab631449a78914502803aed8a0e5a728d558035d29a56f4dcc136c6bfc3021`Пример:ru_pp_token.ditaПодробнее см.Применяется в режимах purchase и Verify_Card.Токен банковской карты пользователя. Токен представляет собой уникальную последовательность из 64 символов, связанную с банковской картой. |
| Для регистрации повторяемой оплаты |  |
| recurringtype_string, type_strictly_required | ``ru_pp_recurring.ditaПодробнее см.Строка, содержащая в закодированном виде параметры регистрации и проведения COF-платежа. Данные собираются в JSON-объект и кодируются в URL-encoded. Структура JSON-объекта приведена в спецификации sts_api в схеме. |
| ru_pp_parameters.ditaПри необходимости добавьте в запрос необязательные параметры из числа доступных для работы с Payment Widget. Подробнее о параметрах запросов в Payment Widget см.. |  |

## Оповещение (callback)

ru_pp_callbacks.ditaРезультат проверки платежной карты платежная платформа возвращает в оповещении. Подробнее о структуре оповещений см..

`112233******7788customer_123`Вот пример тела оповещения с информацией о том, что платежная картапользователядействительна и зарегистрирована для повторяемой оплаты.

Если карта не прошла проверку, платежная платформа отправит вам оповещение с информацией о том, что проведение платежа было отклонено.

## Статусы платежа

В данном разделе представлена информация о возможных статусах платежа и связанных с ним операций.

В процессе проверки карты платежу могут присваиваться следующие статусы.

| Статус платежа | Описание |
| --- | --- |
| `error` | *Конечное состояние. Допускается повторное инициирование этого платежа*При проверке принятого в платежной платформе запроса возникла ошибка, и проведение платежа не было инициировано. |
| `processing` | *Промежуточное состояние*Платеж проводится. |
| `awaiting 3ds result` | `decline`*Промежуточное состояние*Проведение платежа приостановлено. Платежная платформа ожидает от вас запрос с информацией о результате аутентификации 3ds. Если такой запрос не поступит от вас в платежную платформу в течение 30 минут, платеж будет переведен в статус. |
| `awaiting clarification` | `decline`*Промежуточное состояние*Проведение платежа приостановлено. Платежная платформа ожидает от вас запрос с недостающей информацией. Если такой запрос не поступит от вас в платежную платформу в течение 30 минут, платеж будет переведен в статус. |
| `decline` | *Конечное состояние*Платеж отклонен. |
| `success` | *Конечное состояние*Платеж проведен. |

## `Account verification`Статусы операции

`account verification`Следующие статусы могут быть назначены операции.

| Статус | Значение статуса |
| --- | --- |
| `processing` | *Промежуточное состояние*Операция выполняется. |
| `awaiting 3ds result` | `decline`*Промежуточное состояние*Выполнение операции приостановлено. Платежная платформа ожидает от вас запрос с информацией о результате аутентификации 3ds. Если такой запрос не поступит от вас в платежную платформу в течение 30 минут, операция будет переведена в статус. |
| `awaiting clarification` | `decline`*Промежуточное состояние*Выполнение операции приостановлено. Платежная платформа ожидает от вас запрос с недостающей информацией. Если такой запрос не поступит от вас в платежную платформу в течение 30 минут, операция будет переведена в статус. |
| `decline` | *Конечное состояние*Операция отклонена. |
| `success` | *Конечное состояние*Операция выполнена. |

## Ссылки по теме

- ru_pp_interaction_organisation.dita
- ru_pp_callbacks.dita
- ru_pp_payment_model_object.dita
- ru_pp_statuses_and_response_codes.dita

::: tip
Информацию о возможности проверки карт в вашем проекте уточняйте у курирующего менеджера Some Platform.
:::

::: tip
*pp_host*Здесь вместо <> надо указать доменное имя Payment Widget, которое следует узнавать у своего курирующего менеджера или в службе поддержки.
:::

## Схема и статусы проверки платежной карты

### Общая информация

*Проверка карты*— это тип платежа, в рамках которого для проверки возможности использования платежной карты на основании одного исходного запроса осуществляется один условный (нулевой) перевод денежных средств от пользователя к мерчанту или одна реальная (ненулевая) блокировка средств пользователя с последующей отменой. При этом сумма блокировки может согласовываться с мерчантом, а срок отмены блокировки может составлять до 45 дней.

Платежи этого типа могут быть актуальны, например, для регистрации повторяемой оплаты.

::: tip
Информацию о возможности проведения проверки карт необходимо уточнять у курирующего менеджера Some Platform.
:::

### Схема проведения

`card_verifyaccount verification`или платежной системыЧтобы инициировать проверку карты, в параметрах вызова платежной формы укажите режим работы. Тогда после подтверждения пользователем согласия на проверку в платежную платформу направляется запрос, для выполнения которого в платформе формируется операция. И в результате выполняется проверка карты на стороне эмитента, после чего в веб-сервис направляется оповещение с результатом этой проверки.

При проверки карты могут выполняться вспомогательные процедуры: аутентификация пользователя с использованием технологии 3ds и (или) дополнение информации о платеже. Такие процедуры выполняются без участия веб-сервиса мерчанта, но, как правило, требуют участия пользователя.

ru_pp_account_verification.ditaru_pm_intro.dita, а о нюансах проведения такой проверки в зависимости от используемого платежного метода см.Далее в рамках данного раздела представлена информация о возможных статусах проверки карты и связанных с ней операций. Более подробную информацию о проверке карт см. в разделе.

### Статусы платежа

При проверке карт могут использоваться следующие статусы.

| `error` | Проведение платежа не инициировано из-за ошибки, возникшей при проверке принятого в платежной платформе запроса | *Конечное состояние. Допускается повторное инициирование этого платежа* |
| --- | --- | --- |
| `processing` | Платеж проводится | *Промежуточное состояние* |
| `awaiting 3ds result` | `decline`Проведение платежа приостановлено до получения в платежной платформе информации о результате аутентификации 3ds. Если информация о результате не получена в течение 30 минут, то платеж переводится в статус | *Промежуточное состояние* |
| `awaiting clarification` | `decline`Проведение платежа приостановлено до получения дополнительной информации от пользователя. Если такая информация не получена в течение 30 минут, платеж переводится в статус | *Промежуточное состояние* |
| `decline` | Платеж отклонен | *Конечное состояние* |
| `success` | Платеж проведен | *Конечное состояние* |

### Статусы операции account verification

`account verification`При выполнении операциимогут использоваться следующие статусы.

| `processing` | Операция выполняется | *Промежуточное состояние* |
| --- | --- | --- |
| `awaiting 3ds result` | `decline`Выполнение операции приостановлено до получения в платежной платформе информации о результате аутентификации 3ds. Если такой запрос не получен в течение 30 минут, операция переводится в статус | *Промежуточное состояние* |
| `awaiting clarification` | `decline`Выполнение операции приостановлено до получения от пользователя дополнительной информации. Если такая информация не получена в течение 30 минут, операция переводится в статус | *Промежуточное состояние* |
| `decline` | Операция отклонена | *Конечное состояние* |
| `success` | Операция выполнена | *Конечное состояние* |

### Дополнительные материалы

- ru_pp_account_verification.dita— раздел с информацией о проверке платежных карт
- ru_pm_intro.dita— раздел с подробной информацией о выполнении проверки действительности в разных платежных методах.
- ru_pp_callbacks.dita— раздел с информацией об оповещениях и работе с ними.
- ru_pp_statuses_and_response_codes.dita— раздел с информацией о кодах ошибок, используемых в платежной платформе.
