# Проверка доступности платежных методов

В платежной платформе Some Platform вам предлагается множество платежных методов, с помощью которых ваши пользователи могут совершать платежи. Но иногда платежные методы могут оказаться недоступными для проведения платежей, например если сумма платежа не соответствует лимитам платежного метода или же если платежный метод в этот момент не работает.

Вы можете проверять, какие платежные методы доступны для проведения платежа, прежде чем отображать их пользователю. Такая проверка поможет вам избежать потенциального отказа в проведении платежа, если его сумма не соответствует лимитам платежного метода или если платежный метод временно недоступен для проведения платежей.

Чтобы проверить доступность платежных методов, отправьте платежной платформе Some Platform запрос через Gate API. О том, как отправить такой запрос и какую информацию вы получите в ответе на него, рассказывается далее в этой статье.

## Запрос

Ниже представлена информация, необходимая для создания и отправки запроса на проверку доступности платежных методов:

Вот пример основного набора параметров из запроса на проверку доступности платежных методов. В ответе на такой запрос платежная платформа отправит вам информацию о доступных платежных методах по указанному проекту:

В следующем примере представлены параметры из запроса на проверку доступности одного конкретного платежного метода. В ответе на такой запрос платежная платформа отправит вам информацию об этом платежном методе:

В примере ниже представлен запрос на проверку доступности платежных методов с дополнительными параметрами. В ответе на такой запрос платежная платформа отправит вам информацию о доступных платежных методах, а также о платежных методах, которые недоступны из-за расписания работы платежного метода и несоответствия суммы запроса сумме лимита платежа. При этом список отправленных в ответе платежных методов будет варьироваться в зависимости от того, доверенный ли пользователь совершает платеж или нет.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка |  |

| Объект/Параметр | Описание |  |
| --- | --- | --- |
| project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |  |
| directionstype_array type_strictly_required | `payin`— для получения списка платежных методов, доступных для оплаты;`payout`— для получения списка платежных методов, доступных для выплаты.`payin`Пример:Передавайте в этом массиве: |  |
| currencytype_string type_optional | Добавьте этот параметр в запрос, чтобы в ответе получить список платежных методов, доступных для проведения платежа по указанной валюте.`USD`Пример:Код валюты платежа в формате ISO-4217 alpha-3. |  |
| amounttype_integer type_optional | *за исключениемосновных единицах валюты*ru_currency_codes.ditacurrencyПередавайте этот параметр в запросе вместе с параметром, чтобы в ответе получить список платежных методов, доступных для проведения платежа по указанной в запросе сумме и валюте.references/ru/currencies/USD.dita`1000`Пример: 10,00передается какСумма платежа в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |  |
| show_unavailable_methods_by_rulestype_boolean type_optional | *недоступных*`true`Если вы передадите в запросе этот параметр со значением, в ответе вы получите массивы с платежными методами, недоступными из-за:`true`Пример:*расписания работы платежного метода*. В массиве будет содержаться информация о том, когда платежный метод возобновит работу.*несоответствия суммы запроса сумме лимита платежа*currencyamount. В массиве будет содержаться информация о том, какая у платежного метода допустимая минимальная и максимальная сумма платежа. Чтобы получить в ответе массив с этой информацией, передайте также в запросе параметрыи.Параметр, который вы можете добавить в запрос, чтобы в ответе получить списокплатежных методов. |  |
| is_enable_check_by_trusted_pm_user_rulestype_boolean type_optional | *доверенному*`true`Если вы передадите в запросе этот параметр со значением, платежная платформа проверит, доверенный ли пользователь совершает оплату, и на основании результата проверки отправит вам в ответе соответствующий список платежных методов.customer_idПередавайте этот параметр в запросе вместе с параметром, чтобы в ответе получить платежные методы, доступные для конкретного пользователя.`true`Пример:Параметр, который вы можете добавить в запрос, чтобы в ответе получить список платежных методов для отображенияпользователю. |  |
| customertype_object type_optional | idtype_string type_optional | is_enable_check_by_trusted_pm_user_rulesПередавайте этот параметр в запросе вместе с параметром, чтобы в ответе получить платежные методы, доступные для конкретного пользователя.`customer_123`Пример:Идентификатор пользователя, уникальный в рамках проекта. |
| payment_methodtype_string type_optional | methodИспользуйте этот параметр в запросе, если хотите проверить доступность конкретного платежного метода. Код платежного метода указан в параметре, содержащемся в оповещении (callback).`example_method`Пример:Код платежного метода, доступность которого нужно проверить. |  |
| signaturetype_string type_strictly_required | ru_signature_intro.ditaПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |

## Ответ

*синхронно*`200 OK`Ответ на запрос на проверку доступности платежных методов отправляется, то есть в рамках одной HTTP-сессии. Если ваш запрос был составлен правильно, платежная платформа ответит на него кодом состояния HTTPи передаст вам в ответе информацию о доступных платежных методах.

Вот пример данных, которые могут передаваться в ответе на запрос на проверку доступности платежных методов:

Если запрос был составлен неправильно или платежная платформа не смогла обработать ваш запрос, ответ будет содержать:

В таблице ниже перечислены возможные коды, которые вы можете получить в ответе на запрос на проверку доступности платежных методов:

- *отличный*`200 OK`код состояния HTTP,от;
- `error`статус обработки запроса;
- описание причины обнаруженной ошибки.

| Код | Описание |
| --- | --- |
| `200` | Запрос успешен. В ответе содержится информация о платежных методах, доступных для проведения платежа, и их лимитах |
| `400` | Запрос составлен некорректно, отсутствуют обязательные параметры. Скорректируйте запросПлатежный метод не найден. Убедитесь, что в запросе переданы корректные код платежного метода и валютаИспользуется в нескольких случаях: |
| `403` | Невозможно выполнить запрос из-за отсутствия прав, например, если запрос был отправлен с IP-адреса, не включенного в список разрешенных адресов. Свяжитесь со службой технической поддержки |
| `422` | Некорректный JSON. Скорректируйте запрос |
| `500` | Внутренняя ошибка сервера. Повторите запрос |
