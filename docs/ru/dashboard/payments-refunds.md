# Выполнение возврата

## Условия

Dashboard позволяет выполнять возврат на полную и частичную сумму оплат любого типа, в том числе по отдельным списаниям в рамках повторяемых оплат. При этом может использоваться единичная и пакетная отправка запросов и в любом случае должны соблюдаться следующие условия:

Вопросы, касающиеся соблюдения этих условий, можно решать на месте — со специалистами, отвечающими за распределение прав и работу с балансами.

- у используемой учетной записи должно быть право на выполнение возврата;
- оплата (отдельное списание повторяемой оплаты), по которой требуется выполнить возврат, должна быть проведена;
- для платежного метода, с использованием которого была проведена оплата, должны поддерживаться возвраты требуемого типа;
- на балансе, по которому была проведена оплата, должно быть достаточно средств для выполнения возврата.

## Одиночный возврат

Чтобы выполнить одиночный возврат оплаты или отдельного списания в рамках повторяемой оплаты, следует:

Также выполнить одиночный возврат можно напрямую из реестра платежей. Для этого следует:

`decline`Если возврат был отклонен, то статус операции принимает значение, а статус платежа не меняется. Это может быть вызвано разными причинами. И, например, в случае отказа из-за того, что на балансе оказалось недостаточно средств, можно пополнить баланс и повторить попытку возврата.

## Массовые возвраты

Чтобы выполнить группу возвратов с отправкой запросов одним пакетом (массовый возврат), следует:

С любыми вопросами о выполнении возвратов можно обращаться к специалистам технической поддержки Some Platform.

1. далееТребования к таким файлам представлены, вместе с шаблоном и примером заполнения.`general.payment_idgeneral.operation_id`При подготовке файла необходимо учесть, что для выполнения возврата на оплату должен быть указан ее идентификатор в поле, а для возврата на отдельную операцию повторяемой оплаты — еще и идентификатор этой операции в поле.Подготовить файл заданного формата с информацией о целевых возвратах.
2. **Массовые возвраты****Мануальные платежиЗапрос**Для этого следует открыть раздел, щелкнуть кнопкуслева на панели фильтрации и перейти на вкладку массовых возвратов.Перейти на вкладку.
3. **Выберите файл****Отправить запрос**После загрузки файла необходимо убедиться в том, что кнопкастала активной, щелкнуть ее.Для загрузки можно перетащить файл в область загрузки или использовать кнопку.**Отправить запросПредварительный просмотрИнформация о файле)**Если файл некорректен, кнопкане становится активной и отображается сообщение об ошибках. В такой ситуации можно изучить информацию о них (используя переключательи далее кнопкуи скорректировать файл.Загрузить подготовленный файл со списком возвратов и убедиться, что он корректен.
4. **Отправить запрос**Для этого следует щелкнуть кнопку.Отправить пакет запросов на выполнение.
5. `Done`**Массовые запросыМануальные платежиIndicator**При отправке запросов в интерфейсе отображается сообщение об их приеме, после чего можно убедиться в проведении возвратов, проверив статус пакета в реестре массовых запросов — он должен принять значение(для этого следует щелкнуть кнопкуна панели фильтрации в разделеи найти в реестре строку требуемого пакета). В этом реестре дополнительно можно ориентироваться на статусы отдельных возвратов с помощью элементов. Стоит учитывать, что время, необходимое для выполнения возвратов и отображения информации об их статусах, может существенно варьироваться в зависимости от количества запросов в пакете.`refundedreversedpartially refundedsuccessdecline`Для проверки статусов отдельных возвратов можно проверять статусы целевых оплат в реестре платежей (они должны принимать значенияилипри возврате полной суммы ипри возврате частичной суммы), а также проверять статусы возвратов в карточках целевых оплат (эти статусы должны принимать значениеилив зависимости от результата операции).Убедиться в выполнении запросов.
