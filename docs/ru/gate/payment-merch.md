# Аутентификация по инициативе мерчанта

## Общая информация

Аутентификация пользователя со стороны провайдера по инициативе мерчанта предназначена для обеспечения дополнительной безопасности проведения интернет-оплат с использованием платежных карт. Такая аутентификация используется, как правило, в качестве замены аутентификации 3ds1 или для ее дополнения. Это может быть уместно в тех случаях, когда аутентификация 3ds1 недостаточно надежна, например из-за поддержки эмитентами устаревших методов подтверждения пользователем своей личности. При аутентификации по инициативе мерчанта подтверждение личности пользователя осуществляется путем ввода пользователем проверочного кода, полученного в информации о списании. Информацию о списании пользователь получает в SMS-сообщении или банковской выписке.

В платежной платформе аутентификация по инициативе мерчанта поддерживается только для некоторых провайдеров, а для ее подключения необходимо настроить проект мерчанта. Для этого следует обратиться в службу технической поддержки support@example.com

Со стороны веб-сервиса для аутентификации по инициативе мерчанта необходимо:

Более подробные сведения о работе с аутентификацией по инициативе мерчанта представлены далее.

- `awaiting merchant auth`принять оповещение с информацией об изменении статуса платежа на;
- получить согласие пользователя;
- отправить запрос на аутентификацию;
- принять проверочный код от пользователя;
- отправить проверочный код в запросе к платежной платформе.

## Схема работы

*Информирование*`awaiting merchant auth`Аутентификация по инициативе мерчанта может выполняться при проведении одностадийных и двухстадийных оплат с применением платежных карт.о необходимости такой аутентификации осуществляется через оповещения, на которые, как и обычно, необходимо направлять ответы об их приеме. Эти оповещения содержат информацию об изменении статуса платежа на.

*реагированияполучитьотправитьполучитьотправить*Со стороны веб-сервиса дляна такие оповещения необходимо:от пользователя согласие на аутентификацию,в платежную платформу запрос на аутентификацию,от пользователя проверочный код иэтот код в платежную платформу в запросе на продолжение платежа.

`decline`Время ожидания запроса на продолжение проведения платежа с учетом результата аутентификации не ограничено, но общее время проведения платежа не должно превышать максимально допустимое, установленное на стороне провайдера. Если платеж не был проведен или отклонен за это время, он автоматически переводится в статус.

После того как в платежную платформу поступает запрос с проверочным кодом, проведение платежа продолжается дальше.

ru_gate_interaction_organisation.ditaИнформация о формате запросов и оповещений приведена далее; общая информация о работе с API — в разделе.

## Формат оповещения о необходимости аутентификации

ru_gate_callbacks.ditaИнформация о необходимости аутентификации передается в оповещении стандартного формата, описание которого представлено в разделе.

`awaiting merchant auth`В данном случае оповещение свидетельствует о том, что платеж и операция переведены в статусдо получения запроса на продолжение платежа.

## Формат запроса на инициирование аутентификации

`general`Для инициирования аутентификации по инициативе мерчанта необходимо отправить в платежную платформу POST-запрос к конечной точке request-payment-card-merchant-auth. В этом запросе должен использоваться объект, содержащий основные идентификационные сведения и указатель типа запроса:

`start`Таким образом, корректный запрос должен содержать идентификаторы проекта и платежа, указатель типа запроса () и подпись.

- `project_id`— идентификатор проекта, полученный от Some Platform при интеграции;
- `payment_id`— идентификатор платежа, уникальный в рамках проекта;
- `typestart`— указатель типа запроса, необходимо передать значение;
- `signature`ru_gate_authentication.dita— подпись запроса, составленная после указания целевых параметров (подробнее — в разделе).

## Формат запроса на продолжение проведения платежа

Для продолжения проведения платежа с учетом результата аутентификации необходимо отправить в платежную платформу POST-запрос к конечной точке request-payment-card-merchant-auth. В этом запросе должны использоваться следующие объекты и параметры:

`finish`Таким образом, корректный вопрос должен содержать идентификаторы проекта и платежа, указатель типа запроса (), подпись и проверочный код.

1. `general``project_id`— идентификатор проекта, полученный от Some Platform при интеграции;`payment_id`— идентификатор платежа, уникальный в рамках проекта;`typefinish`— указатель типа запроса, необходимо передать значение;`signature`ru_gate_authentication.dita— подпись запроса, составленная после указания целевых параметров (подробнее — в разделе);— объект, содержащий основные идентификационные сведения и указатель типа запроса:
2. `confirmation_code`— проверочный код, полученный от пользователя.
