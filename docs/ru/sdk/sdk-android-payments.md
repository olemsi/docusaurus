# Проведение платежей

## Базовый вариант оплаты

`Sale`По умолчанию в sdk_android настроено проведение разовых одностадийных оплат (тип действия). Для проведения данного типа оплат можно использовать приведенные примеры и дополнительно ничего не настраивать.

## Разовая двухстадийная оплата

`Auth`JavapaymentInfo.setAction(sdk_android_payment_info.ActionType.Auth);KotlinpaymentInfo.setAction(sdk_android_payment_info.ActionType.Auth)Задать значениев типе действия.Подтвердить списание средств через Dashboard или с помощью запроса на request-payment-card-capture.Для проведения разовых двухстадийных оплат необходимо:

ru_pp_purchase_auth.ditaПодробная информация о проведении оплат с предварительной блокировкой представлена в разделе.

## Повторяемая оплата

ru_gate__saved_cards_payments_type.ditasdk_android поддерживает проведение повторяемых оплат. Такие оплаты бывают трех типов: oneclick, автооплаты и регулярные оплаты. Подробнее о повторяемых оплатах — в разделе.

`sdk_android_recurrent_info`Javasdk_android_recurrent_info recurrentInfo = new sdk_android_recurrent_info(
                 "R", // тип повторяемой оплаты
                 "20", // день окончания действия подписки
                 "11", // месяц окончания действия подписки
                 "2020", // год окончания действия подписки
                 "M", // периодичность проведения повторяемых оплат
                 "12:00:00", // время проведения повторяемых оплат
                 "12-02-2019", // дата проведения первой повторяемой оплаты
                 "your_recurrent_id"); // идентификатор повторяемой оплаты в проектеKotlinval recurrentInfo = sdk_android_recurrent_info(
                 "R", // тип повторяемой оплаты
                 "20", // день окончания действия подписки
                 "11", // месяц окончания действия подписки
                 "2020", // год окончания действия подписки
                 "M", // периодичность проведения повторяемых оплат
                 "12:00:00", // время проведения повторяемых оплат
                 "12-02-2019", // дата проведения первой повторяемой оплаты
                 "your_recurrent_id"); // идентификатор повторяемой оплаты в проектеСоздать объект.Дополнительно можно задать:`recurrentInfo`JavarecurrentInfo.setAmount(1000);KotlinrecurrentInfo.setAmount(1000)Сумму списания. По умолчанию сумма списания равна сумме платежа. Задать другую сумму списания можно в объекте:`recurrentInfo`JavarecurrentInfo.setSchedule(new sdk_android_recurrent_info_schedule[]{
               new sdk_android_recurrent_info_schedule("10-10-2020",1200),
               new sdk_android_recurrent_info_schedule("10-11-2020",1000),
               .....
});KotlinrecurrentInfo.setSchedule(arrayOf(sdk_android_recurrent_info_schedule("10-10-2020",1200),
                                  sdk_android_recurrent_info_schedule("10-11-2020",1000),
                                  .....
))Дат и сумм списания. По умолчанию при повторяемых оплатах средства списываются с карты строго фиксированно по времени и сумме, но можно задать собственное расписание в объекте:`sdk_android_recurrent_infosdk_android_payment_info`JavapaymentInfo.setRecurrent(recurrentInfo);KotlinpaymentInfo.setRecurrent(recurrentInfo)Добавить объектв объект.Для проведения повторяемой оплаты необходимо:

## Проверка действительности карты

Проверка действительности платежного инструмента используется в случае необходимости проверки карты пользователя без списания средств, а также для сохранения данных карты с целью дальнейшего использования. При проверке карты выполняется списание с карты нулевой суммы.

`Verify`Для проведения верификации карты необходимо задатьв типе действия:

## Оплата с использованием альтернативных платежных методов

ru_pm_skrill_wallet.ditaBanks of MalaysiaBanks of Thailandru_pm_neteller.ditaru_pm_webmoney.dita,WebMoneyLightWebMoneyClassic:,,,(сценарии оплати)sdk_android поддерживает проведение оплат с использованием альтернативных платежных методов. В большинстве случаев пользователь перенаправляется к сервису платежного метода для завершения оплаты. Описание каждого из этих методов представлено в отдельной статье, а далее также приведено описание особенностей работы споддерживаемыми сценариями оплат метода WebMoney.

Схема взаимодействия (для случая с перенаправлением) представлена далее.

подробнееДля проведения платежей с использованием альтернативных платежных методов требуется связаться со специалистами службы технической поддержки Some Platform для включения данной возможности в рамках проекта. Также можно настроить передачу данных о пользователе ().

**WebMoney**

WebMoneyLightWebMoneyClassicWebMoneyLightWebMoneyWebMoneyClassicWebMoneyПри работе с sdk_android поддерживается проведение оплат с использованием сценариеви. При использованииосуществляется перенаправление пользователя к форме оплатыдля перевода средств на WM-кошелек мерчанта. При использованиипользователь самостоятельно переходит в сервисдля проведения оплаты. Все основные процедуры, такие как вызов платежной формы, прием результатов и обработка оповещений, выполняются так же, как и при работе с другими платежными методами, а при формировании запросов необходимо учитывать следующее:
