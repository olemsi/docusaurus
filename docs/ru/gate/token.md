# Использование токенов

**Токен**— это идентификатор, представляющий собой уникальную строку из 64 символов и связанный с определенной банковской картой и пользователем. Токен не содержит конфиденциальной информации и может храниться в вашей системе, не создавая угрозы нарушения стандартов безопасности по хранению данных банковских карт.

Через Gate вы можете создавать токены (автоматически или по запросу), а также производить по имеющимся токенам оплаты и выплаты.

## Статусы токенов

Статус токена определяет возможность его использования для проведения оплат и выплат.

| Статус | Описание |
| --- | --- |
| `active` | Действительный токен, по которому могут проводиться оплаты и выплаты |
| `revoke` | Токен был отозван, проведение операций по токену невозможно |
| `expiry` | Срок действия токена истек, проведение операций по токену невозможно |

## Создание токена по запросу

Для создания токена банковской карты через Gate:

1. Отправьте запрос с нужными параметрами и подписью на рабочий URL-адрес платежной платформы Some Platform.
2. Примите от платежной платформы оповещение (callback) с информацией о созданном токене и с самим токеном.

## Запрос на создание токена

Далее представлена информация, необходимая для составления и отправки запроса на создание токена платежной карты.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/customer/card/tokenize` |
| Полная спецификация конечной точки | request-customer-card-tokenize |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе. |  |  |
| --- | --- | --- |
| Объекты | Параметры | Описание |
| customertype_object type_strictly_required | idtype_string type_strictly_required | Идентификатор пользователя, уникальный в рамках проекта.`customer_123`Пример: |
| ip_addresstype_string type_strictly_required | IP-адрес устройства пользователя.`198.51.100.47`Пример: |  |
| project_idtype_integer type_strictly_required | Идентификатор проекта, полученный от Some Platform при интеграции.`123`Пример: |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| cardtype_object type_strictly_required | pantype_string type_strictly_required | Номер карты.`1122334455667788`Пример: |
| yeartype_integer type_strictly_required | Год окончания срока действия карты.`2025`Пример: |  |
| monthtype_integer type_strictly_required | Месяц окончания срока действия карты.`8`Пример: |  |
| card_holdertype_string type_strictly_required | Имя держателя карты (как указано на карте).`JOHN DOE`Пример: |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

## Оповещение (callback)

ru_gate_callbacks.ditaru_callback_parms_token.ditaУведомление о создании токена платежная платформа возвращает в оповещении. Подробнее о структуре оповещений см.. Подробнее о параметрах оповещений, касающихся токенов, см..

`123customer_123`Вот пример тела оповещения с информацией о том, что в рамках проектабыл создан токен карты пользователя, а также вновь созданный токен.

## Автоматическое создание токена

Токены могут создаваться автоматически при проведении первой успешной оплаты или выплаты по банковской карте, а также при успешной блокировке средств.

ru_callback_parms_token.ditaПлатежная платформа вернет вам оповещение (callback) с токеном, а также информацией о времени его создания. Подробнее о параметрах оповещений, касающихся токенов, см..

::: tip
Для подключения автоматического создания токенов свяжитесь со службой технической поддержки support@example.com
:::

## Оплата по токену

Gate позволяет пользователям осуществлять быстрые платежи с банковской карты, используя ранее созданный токен.

При проведении оплаты по банковской карте, для которой уже существует токен, новый токен не создается. Если пользователь укажет дату окончания срока действия карты, отличную от указанной при создании токена, то токен не создается заново, а срок действия токена обновляется до указанной даты.

ru_gate_purchase.ditaДополнительные сведения об оплате с использованием токена см. в разделе.

## Выплата по токену

ru_gate_payout_token.ditaGate позволяет осуществлять выплату средств на банковскую карту пользователя, используя ранее созданный токен. Дополнительные сведения о выплатах с использованием токена см. в разделе.

## Получение данных карты по токену

Gate позволяет вам получить данные карты по токену. Для этого вы должны, отправить в платежную платформу соответствующий запрос. Далее представлена информация, необходимая для создания и отправки такого запроса.

В оповещении вы получите данные банковской карты и ее номер в маскированном виде.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/customer/card/bytoken` |
| Полная спецификация конечной точки | request-customer-card-bytoken |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе. |  |  |
| --- | --- | --- |
| Объекты и параметры | Описание |  |
| customertype_object type_strictly_required | idtype_string type_strictly_required | Идентификатор пользователя, уникальный в рамках проекта.`customer_123`Пример: |
| project_idtype_integer type_strictly_required | Идентификатор проекта, полученный от Some Platform при интеграции.`123`Пример: |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| tokentype_string type_strictly_required | Токен банковской карты пользователя. Токен представляет собой уникальную строку из 64 символов, связанную с банковской картой.`f365bb1729f9b72fd9c79f3becc679f29c3e35c91d070d15654`Пример:ru_gate_token.ditaПодробнее см. |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

## Проверка карты по токену

ru_gate_account_verification.ditaGate позволяет проверить действительность платежной карты пользователя, используя ранее созданный токен. Подробнее о проверке карты по ее токену см..

## Отзыв токена

Токен отзывается в следующих случаях:

- у связанной с токеном банковской карты заканчивается срок действия;
- вы отправляете в платежную платформу запрос на отзыв токена.

## Запрос на отзыв токена

Далее представлена информация, необходимая для составления и отправки запроса на отзыв токена платежной карты.

ru_callback_parms_token.ditaПосле окончания обработки запроса вам придет оповещение с результатом отзыва токена. Подробнее о параметрах оповещений, касающихся токенов, см..

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/customer/card/token/revoke` |
| Полная спецификация конечной точки | request-customer-card-token-revoke |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросе. |  |  |
| --- | --- | --- |
| Объекты и параметры | Описание |  |
| customertype_object type_strictly_required | idtype_string type_strictly_required | Идентификатор пользователя, уникальный в рамках проекта.`customer_123`Пример: |
| project_idtype_integer type_strictly_required | Идентификатор проекта, полученный от Some Platform при интеграции.`123`Пример: |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| tokentype_string type_strictly_required | Токен банковской карты пользователя. Токен представляет собой уникальную строку из 64 символов, связанную с банковской картой.`f365bb1729f9b72fd9c79f3becc679f29c3e35c91d070d15654`Пример:ru_gate_token.ditaПодробнее см. |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |
