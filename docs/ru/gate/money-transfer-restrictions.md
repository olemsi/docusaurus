# Ограничения и особенности

При проведении переводов с карты на карту необходимо учитывать следующие ограничения:

`money_transfer/inmoney_transfer/in/saved`При отмене списаний, выполненных в рамках сокращенного варианта перевода (для запросов категориии), следует учитывать следующее:

При проведении переводов с карты на карту необходимо учитывать следующее ограничение: возможность выполнения конвертации на стороне мерчанта необходимо согласовывать с курирующим менеджером.

`money_transfer/inmoney_transfer/in/saved`При отмене списаний, выполненных в рамках сокращенного варианта перевода (для запросов категориии), следует учитывать следующее:
