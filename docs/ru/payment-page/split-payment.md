# Разбиение суммы платежа

На финальной странице оплаты на Payment Widget пользователю отображаются обе суммы — исходная и фактически списанная.

## Общие сведения

При проведении платежа есть вероятность столкнуться с ограничениями, наложенными платежными провайдерами на сумму оплаты. Gate позволяет обойти данные ограничения путем разбиения суммы платежа, превышающей установленный лимит, на несколько операций с меньшими суммами. Для установки максимально допустимого количества операций, создаваемых по платежу с разбиением суммы, обращайтесь к специалистам технической поддержки Some Platform.

сумма платежа делится на сумму лимита, целое число из получившегося результата — это количество операций с суммой, максимально допустимой по лимиту,из суммы платежа вычитается произведение установленного лимита и получившегося целого числа, чтобы узнать остаток суммы.Формула для вычисления сумм и количества всех операций в платеже следующая:

`10 000,00 EUR15 000,00 EUR10 000,00 EUR5 000,00 EUR15000:10000=1.515000-(10000*1)=50001 800,00 EUR5 000,00 EUR1 800,00 EUR1 800,00 EUR1 400,00 EUR5000:1800=2.85000-(1800*2)=1400`Например, если лимит суммы, установленный провайдером, составляет, а сумма платежа от пользователя —, то платеж разбивается на две операции с суммами:(максимально допустимая сумма) и(остаток суммы) по формуле,. Или если лимит суммы —, а сумма платежа —, то платеж разбивается на три операции с суммами:,(максимально допустимые суммы) и(остаток суммы) по формуле:,.

Функциональность поддерживается для проведения разовой оплаты с использованием карт в одну или в две стадии. При проведении платежа пользователю на Payment Widget отображается предупреждение о том, что сумма может быть разбита, и возникнет необходимость пройти аутентификацию 3-D Secure более одного раза.
