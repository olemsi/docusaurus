# Проведение оплаты

## Общая информация

В интерфейсе Dashboard поддерживаются возможности:

- в отдельном разделеРаботать с платежными ссылками — создавать, отправлять и деактивировать их. Схема проведения оплат по платежным ссылкам представлена.
- в отдельном разделеУправлять списаниями по оплатам в две стадии — подтверждать списание заблокированных средств и отменять блокировку средств с использованием единичной и пакетной отправки запросов. Схема проведения оплат в две стадии представлена.

## Условия

Для проведения оплат через интерфейс Dashboard должны соблюдаться следующие условия:

Вопросы, касающиеся распределения прав, можно решать на месте — со специалистами мерчанта.

- у текущей учетной записи должны быть права на выполнение необходимых действий —действия каждого типа, включая подтверждение и отмену списаний, регулируются отдельными правами;
- необходимые действия должны поддерживаться для используемых проекта и платежного метода.

## Оплата по ссылкам

**разовую**Чтобы провестиоплату с использованием платежной ссылки, следует:

**регулярными**Чтобы провести оплату с использованием платежной ссылки с последующимиоплатами (подпиской), следует:

**Деактивация ссылкиСсылки на оплату**Если требуется деактивировать платежную ссылку (до того, как пользователь произвел оплату), следует включить переключательв соответствующей строке реестра в разделе. После этого ссылка становится недействительной и не может применяться для проведения оплаты, даже если она была отправлена пользователю.

::: tip
Первоначальная транзакция по платежной ссылке является транзакцией регистрации подписки. Все последующие транзакции будут оплачиваться в соответствии с графиком выставления счетов.
:::

## Одиночные подтверждения и отмены списаний

Чтобы подтвердить (отменить) списание заблокированных средств для отдельной оплаты в две стадии, следует:

`declineawaiting capture`Если запрос на выполнение операции был отклонен, то статус такой операции принимает значение, а статус платежа не меняется — остается.

1. **Платежи**`authawaiting capture`Для этого можно воспользоваться поиском (например, по идентификатору платежа) или перейти в раздели использовать реестр и фильтры (например, по типу операции —или по статусу платежа —).При необходимости, найти целевую оплату — ту, для которой необходимо подтвердить (отменить) списание.
2. **Платежи**Для этого следует щелкнуть соответствующую строку в реестре раздела.Открыть карточку целевой оплаты.
3. Для этого следует:**СписаниеОтмена**Щелкнуть кнопку, чтобы списать заблокированные средства (, чтобы отменить блокировку средств).Оплата в две стадии(подробнее — в разделе)При изменении суммы заблокированных средств следует учитывать, что такая возможность поддерживается не во всех случаях (в соответствии с региональными и другими особенностями) и с учетом ограничений со стороны платежных систем.Указать в открывшемся окне требуемую сумму, которая может составлять полную или частичную сумму заблокированных средств.**СписаниеОтмена****СписаниеОтмена**Если кнопкиине активны, это может свидетельствовать о том, что одна из этих операций уже выполняется, а если отсутствуют — о том, что для платежа с данным статусом не поддерживается выполнение таких операций.Подтвердить отправку запроса, щелкнув кнопку().Отправить запрос на подтверждение (отмену) списания заблокированных средств.
4. `capturecancelsuccesssuccesscancelled`Для этого можно проверить статус операции в карточке целевой оплаты или статус этой оплаты в реестре. Статус операции (или) должен принять значение, а статус оплаты —в случае списания ив случае отмены.Убедиться, что операция списания (отмены) выполнена.

## Массовые подтверждения и отмены списаний

Чтобы выполнить групповое списание средств или отмену средств с отправкой запросов одним пакетом, следует:

1. далее`capturecancel`Оплата в две стадии(подробнее — в разделе)При подготовке файла необходимо учесть, что в нем должны содержаться операции только одно типа —или, а сумму подтверждения (отмены) может составлять как полная, так и частичная сумма заблокированных средств. При изменении суммы заблокированных средств следует учитывать, что такая возможность поддерживается не во всех случаях (в соответствии с региональными и другими особенностями) и с учетом ограничений со стороны платежных систем.Требования к таким файлам представлены.Подготовить файл заданного формата с информацией о подтверждениях (отменах) списаний по целевым оплатам.
2. **Массовые списанияМассовые отмены****Мануальные платежиЗапрос**Для этого следует открыть раздел, щелкнуть кнопкув левой части панели фильтрации и перейти на вкладку массовых списаний (отмен).Перейти на вкладку().
3. **Выберите файл****Отправить запрос**После загрузки файла необходимо убедиться в том, что кнопкастала активной, щелкнуть ее.Для загрузки можно перетащить файл в область загрузки или использовать кнопку.**Отправить запросПредварительный просмотрИнформация о файле)**Если файл некорректен, кнопкане становится активной и отображается сообщение об ошибках. В такой ситуации можно изучить информацию о них (используя переключательи далее кнопкуи скорректировать файл.Загрузить подготовленный файл со списком операций и убедиться, что он корректен.
4. **Отправить запрос**Для этого следует щелкнуть кнопку.Отправить пакет запросов на выполнение.
5. Для этого можно контролировать статусы и индикаторы выполнения пакетов и статусы отдельных операций.**Массовые запросыМануальные платежи**`Done`Состояние пакета можно контролировать через реестр массовых запросов — для этого следует щелкнуть кнопкуна панели фильтрации в разделе, и найти в реестре строку требуемого пакета и проверить статус и индикатор выполнения этого пакета. Стоит учитывать, что время, необходимое для выполнения операций и отображения информации об их статусах, может существенно варьироваться в зависимости от количества запросов в пакете. По итогам выполнения пакета его статус должен принять значение.`successcancelledsuccessdecline`Для проверки статусов отдельных операций можно проверять статусы целевых оплат в реестре платежей (они должны принимать значенияпри списании средств илипри отмене блокировки средств), а также проверять статусы операций в карточках целевых оплат (эти статусы должны принимать значениеилив зависимости от результата операции).Убедиться в выполнении запросов.
