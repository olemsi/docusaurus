import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Chargebacks","description":"","frontmatter":{},"headers":[],"relativePath":"dashboard/dbl-chargeback.md","filePath":"dashboard/dbl-chargeback.md"}');
const _sfc_main = { name: "dashboard/dbl-chargeback.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="chargebacks" tabindex="-1">Chargebacks <a class="header-anchor" href="#chargebacks" aria-label="Permalink to &quot;Chargebacks&quot;">​</a></h1><p>a merchant of record (MoR)representing the merchant&#39;s interestsan acquirerand interacts with issuers and card networks(details). You can use these callbacks along with other interfaces that provide information about chargebacks (such as the Data API and Dashboard).When working with the Platform payment platform, you can enable and use the functionality of daily callbacks with information about events occurring within the chargeback process. It is relevant when Platform acts</p><p>Similar to standard callbacks sent from the Platform payment platform, chargeback callbacks are HTTP POST requests that contain data in JSON format for processing by the merchant’s web service. However, keep the following special aspects in mind when working with chargeback callbacks:</p><p>Refer to your Platform account manager to integrate chargeback callbacks.</p><ul><li>Data is sent without any form of digital signature.</li><li>Callbacks are sent daily at 12:00 UTC. If for any reason delivery cannot be performed at this time, callbacks are sent at 15:00 UTC instead.</li><li>Callbacks are limited to one URL per merchant project.</li><li><code>200 OK400 Bad Request</code>Callbacks are sent only once. Resending callbacks is not supported even if the callback delivery is not acknowledged with theresponse code or if an error response is returned (for example).</li><li>Callbacks are tied to specific stages within the chargeback process and fall into two types: summary and detailed callbacks (details).</li><li>Callbacks are triggered only if there is updated data. If no changes are detected, no callback is sent.</li></ul><h2 id="setup" tabindex="-1">Setup <a class="header-anchor" href="#setup" aria-label="Permalink to &quot;Setup&quot;">​</a></h2><p>To enable chargeback callbacks:</p><ol><li>Agree with the Platform account manager on adding this feature for specific projects, decide on the callback types you need, and provide the destination URL for the web service. Keep in mind that only one URL can be used per merchant project.</li><li>Receive a notification from the Platform specialists confirming that the feature is enabled.</li><li>If relevant chargeback events have occurred, make sure that you have received the corresponding callbacks.</li></ol><h2 id="callback-types" tabindex="-1">Callback types <a class="header-anchor" href="#callback-types" aria-label="Permalink to &quot;Callback types&quot;">​</a></h2><h3 id="summary-callbacks" tabindex="-1">Summary callbacks <a class="header-anchor" href="#summary-callbacks" aria-label="Permalink to &quot;Summary callbacks&quot;">​</a></h3><p>Summary callbacks communicate the number of chargebacks for which chargeback process-specific events were recorded in the platform during the reporting period. These events are categorised as follows:</p><p>For each of these categories, separate callbacks are generated and contain the following set of parameters:</p><p><code>/chargeback/list/chargeback/get</code><strong>Chargebacks</strong>Having recorded the information received in the summary callbacks, you can review the itemised chargeback data via detailed callbacks, requests to the Data API (such asand; details), or Dashboard (using thesection).</p><h3 id="detailed-callbacks" tabindex="-1">Detailed callbacks <a class="header-anchor" href="#detailed-callbacks" aria-label="Permalink to &quot;Detailed callbacks&quot;">​</a></h3><p>Detailed callbacks contain itemised information about chargebacks for which chargeback process-specific events were recorded in the platform during the reporting period. These events are categorised as follows:</p><p>For each of these categories, separate callbacks are generated and contain the following set of parameters:</p><p>Having recorded and reviewed the information received in these callbacks, you can proceed to taking subsequent steps of the chargeback process if necessary.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p><code>chargebacksChargebackchargeback_finalization_datechb_completed_atchargeback_statusstatus</code>detailsThe parameters of the objects in thearray match those of theobject in the Data API () with the exception of two parameter names:instead ofandinstead of.</p></div><h2 id="use" tabindex="-1">Use <a class="header-anchor" href="#use" aria-label="Permalink to &quot;Use&quot;">​</a></h2><p>The procedure of responding to each callback on the web service side consists of the following steps:</p><ol><li><code>200 OKHTTP 500 Internal Server Error</code>For confirming the receipt of callbacks, synchronous HTTP responses should be sent to the payment platform with the corresponding response codes. If no errors have been detected upon the receipt, the response code should be. In other cases, the response code should reflect the error type: for example,if the callback has been received at an incorrect URL of the web service.Note that resending of callbacks is not supported. Regardless of the response, even if it indicates failure to receive the callback, the information sent in previous callbacks is not included in subsequent ones.Accept and confirm the callback receipt.</li><li>detailsPerform the required actions in accordance with the guidelines for handling chargebacks () and the specifics of the web service.</li></ol><h2 id="useful-links" tabindex="-1">Useful links <a class="header-anchor" href="#useful-links" aria-label="Permalink to &quot;Useful links&quot;">​</a></h2><p>The following sections can be useful for handling chargeback callbacks:</p><ul><li>—a section on handling chargebacks (includes an overview, a description of chargeback process, and answers to frequently asked questions).</li><li>—a section on using the API that allows retrieving operation and balance data (includes an overview, a description of interaction with the interface, and detailed information about each endpoints).</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("dashboard/dbl-chargeback.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const dblChargeback = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  dblChargeback as default
};
