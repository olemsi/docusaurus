# Формат запроса на продолжение платежа

При базовой и расширенной схемах аутентификации через Gate для продолжения проведения платежа с учетом результата аутентификации необходимо отправить в платежную платформу POST-запрос к конечной точке request-payment-card-3ds_result. В этом запросе должны использоваться следующие объекты и параметры:

Таким образом, корректный запрос на продолжение проведения платежа с учетом результата аутентификации 3ds2 должен содержать идентификаторы проекта и платежа, подпись и данные о результате аутентификации.

- `general``project_id`— идентификатор проекта, полученный от Some Platform при интеграции;`payment_id`— идентификатор платежа, уникальный в рамках проекта;`signature`ru_gate_authentication.dita— подпись к данным запроса, составленная после указания целевых параметров (подробнее — в разделе).— объект, содержащий основные идентификационные сведения запроса:
- `pares`При базовой схеме: параметрс данными о результате аутентификации 3ds2, полученными в уведомлении от платежной платформы.
- `cres`При расширенной схеме: параметрс данными о результате аутентификации 3ds2, полученными в уведомлении от сервера управления доступом.

---

## Формат запроса на начало аутентификации

`3ds_notification_url`Если в запросе на проведение платежа передан параметр, при расширенной схеме аутентификации через Gate для инициирования аутентификации необходимо отправить в платежную платформу POST-запрос к конечной точке request-payment-card-3ds_check_iframe, если нет — инициирование аутентификации выполняется на стороне платформы. В запросе к этой конечной точке должны использоваться следующие объекты и параметры:

Таким образом, корректный запрос на инициирование аутентификации 3ds2 должен содержать идентификаторы проекта и платежа и подпись.

- `general``project_id`— идентификатор проекта, полученный от Some Platform при интеграции;`payment_id`— идентификатор платежа, уникальный в рамках проекта;`signature`ru_signature_intro.dita— подпись к данным запроса, составленная после указания целевых параметров (подробнее — в разделе).— объект, содержащий основные идентификационные сведения запроса:
- `threeds_completion_indicatortruefalse`— указатель получения уведомления о приеме данных в течение десяти секунд после открытия объекта iframe. Необходимо передать значениепри получении уведомления и значениепри его отсутствии в заданный период.
