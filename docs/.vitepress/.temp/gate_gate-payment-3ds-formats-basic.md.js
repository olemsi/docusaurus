import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Basic workflow messages","description":"","frontmatter":{},"headers":[],"relativePath":"gate/gate-payment-3ds-formats-basic.md","filePath":"gate/gate-payment-3ds-formats-basic.md"}');
const _sfc_main = { name: "gate/gate-payment-3ds-formats-basic.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="basic-workflow-messages" tabindex="-1">Basic workflow messages <a class="header-anchor" href="#basic-workflow-messages" aria-label="Permalink to &quot;Basic workflow messages&quot;">​</a></h1><h2 id="callback-format-when-authentication-is-needed" tabindex="-1">Callback format when authentication is needed <a class="header-anchor" href="#callback-format-when-authentication-is-needed" aria-label="Permalink to &quot;Callback format when authentication is needed&quot;">​</a></h2><p><code>acsredirectData</code>The redirection data is passed in one of the two objects:(if the provider is not involved in the authentication process) or(if the provider is involved).When the basic authentication flow is used, the callback containing the data for redirecting the customer is sent from the payment platform to the web service. These callbacks are arranged in a standard format described in greater detail in the following article.</p><p><code>acs</code>The redirection data is passed in theobject.PAReqCReq<code>acspa_reqacs_urlmd</code>Theobject of the callback contains the following data: themessage (in theparameter) to send the authentication initiation request to the issuer, the URL of the authentication page (), and the merchant data kept on file by the card scheme ().</p><p><code>redirectDatabodymethodurl</code>Theobject of the callback contains the following data: the body of the request (), the HTTP method of the request (), and the URL of the provider ().</p><h2 id="format-of-the-redirection-request" tabindex="-1">Format of the redirection request <a class="header-anchor" href="#format-of-the-redirection-request" aria-label="Permalink to &quot;Format of the redirection request&quot;">​</a></h2><p>Use a POST method and include the following objects and parametersDepending on what is included in the redirection data, use a POST or GET method and include the following objects and parameters:To redirect the customer to the issuer&#39;s ACS page or the provider&#39;s page, send the request to the URL received in the callback that signals the need for authentication.</p><p>HTML fileFor your convenience, here is a ready-madethat you can use by replacing the sample code with the actual payment parameters.</p><h2 id="format-of-the-message-with-the-authentication-result" tabindex="-1">Format of the message with the authentication result <a class="header-anchor" href="#format-of-the-message-with-the-authentication-result" aria-label="Permalink to &quot;Format of the message with the authentication result&quot;">​</a></h2><p><code>pares</code>The message with the authentication result is sent to the web service in theparameter.</p><h2 id="format-of-the-payment-completion-request" tabindex="-1">Format of the payment completion request <a class="header-anchor" href="#format-of-the-payment-completion-request" aria-label="Permalink to &quot;Format of the payment completion request&quot;">​</a></h2><p>To resume processing of the payment once the authentication procedure has been completed, send a request to theendpoint using the POST HTTP method. The request must contain the following objects and parameters:</p><p>Thus, a correct request for payment completion following the 3‑D Secure authentication contains project and payment identifiers, the signature, and the authentication result.</p><ul><li><code>general\`\`project_id</code>—the project identifier obtained from Platform.<code>payment_id</code>—the payment identifier unique within the merchant project.<code>signature</code>—the request signature generated after all required parameters have been specified. For more information about signature generation, see .—the object containing the general identification information of the request:</li><li><code>pares</code>—the parameter containing the customer&#39;s 3‑D Secure authentication result sourced from the PARes message.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("gate/gate-payment-3ds-formats-basic.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const gatePayment3dsFormatsBasic = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  gatePayment3dsFormatsBasic as default
};
