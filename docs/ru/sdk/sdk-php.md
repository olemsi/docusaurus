# sdk_php

sdk_php — это набор средств разработки для взаимодействия веб-сервисов, разработанных на PHP, с платежной платформой Some Platform при проведении оплат через Payment Widget. В этом разделе представлена информация о работе с sdk_php с примерами кода на языке программирования PHP.

sdk_php совместим с PHP до версии 7.2 включительно и доступен для загрузки по следующим ссылкам:

- GitHub:
- Packagist:

## Возможности

подписывать набор параметров платежа и формировать запрос для вызова Payment Widget,проверять подлинность оповещений от Some Platform и получать из них информацию о платеже.sdk_php позволяет:

## Состав

sdk_php содержит библиотеки и служебные файлы. Для работы могут использоваться:

- **src**— библиотеки для разработки,
- **tests**— библиотеки для автоматизированного тестирования,
- **composer.json**— файл, в котором описаны библиотеки.

## Порядок работы

Для использования sdk_php необходимо:

При возникновении вопросов о работе с sdk_php следует обращаться в службу технической поддержки Some Platform по адресу support@example.com

1. Если у компании нет идентификатора проекта и ключа для взаимодействия с Some Platform — отправить заявку на подключение.Если у компании идентификатор и ключ для взаимодействия с Some Platform есть — сообщить специалистам технической поддержки о намерении интеграции с использованием sdk_php и согласовать с ними порядок тестирования.Решить организационные вопросы, касающиеся взаимодействия с Some Platform:
2. Установить библиотеки, входящие в состав sdk_php, в каталог с исходным кодом веб-сервиса и подключить их в коде, а также доработать код для использования необходимой функциональности.
3. **tests**Для тестирования следует использовать тестовый идентификатор проекта, тестовые значения параметров платежа и пример оповещения из библиотеки.project_idДля перевода в рабочий режим необходимо заменить тестовое значениена рабочее, полученное от Some Platform.Протестировать и запустить в работу обновленный исходный код веб-сервиса.

## Установка и подключение библиотек

PackagistУстанавливать библиотеки, входящие в состав sdk_php, в проект с исходным кодом веб-сервиса можно вручную или автоматически с помощью менеджера зависимостей. Менеджер зависимостей Composer обеспечивает загрузку необходимых библиотек из репозиторияи формирование скрипта для подключения загруженных библиотек.

Чтобы установить библиотеки с помощью Composer и подключить их в исходном коде веб-сервиса, необходимо:

1. [https://getcomposer.org/](https://getcomposer.org/)Если не установлен Composer — загрузить, установить и настроить (подробнее).
2. composer require sdk_php_lib/paymentpage-sdk:dev-main**vendorautoload.php**В командной строке операционной системы перейти в каталог с исходным кодом веб-сервиса и выполнить следующую команду:Эта команда служит для размещения загруженных библиотек в каталогеи создания скриптадля одновременного подключения всех библиотек в исходном коде веб-сервиса.
3. **autoload.php**require` __DIR__.'../../vendor.autoload.php';Подключить скриптв исходном коде веб-сервиса:

## Вызов платежной формы

Запрос для вызова Payment Widget формируется из набора параметров, подписываемых для обеспечения защиты данных при передаче запроса в платежную платформу Some Platform. sdk_php позволяет автоматически подписывать параметры и формировать из них запрос. Для вызова Payment Widget с использованием sdk_php следует:

Далее приведен пример формирования запроса для вызова платежной формы Payment Widget с открытием на английском языке. На странице с выбором платежных методов обеспечивается отображение информации о платеже: идентификатора, валюты, суммы и описания платежа. На странице с вводом реквизитов, необходимых для проведения платежа, обеспечивается отображение таймера с обратным отсчетом времени.

1. `Payment`$payment = new sdk_php_lib\Payment('186', '1555943554067');
    // Идентификатор проекта и идентификатор платежа, уникальный в рамках проекта
$payment->setPaymentAmount(1000)->setPaymentCurrency('RUB');
    // Сумма (в дробных единицах валюты) и код валюты (в формате ISO-4217 alpha-3)
$payment->setPaymentDescription('Тестовый платеж');
    // Описание платежа. Необязательный параметрru_pp_parameters.ditaВсе параметры в данном примере, за исключением описания платежа, являются необходимыми для любой оплаты. Дополнительно можно использовать любые другие параметры из числа доступных для работы с Payment Widget. Подробнее о доступных параметрах — в разделе.Создать объект классаи указать значения параметров платежа.
2. `Gate`*secret_key*$gate = new sdk_php_lib\Gate('<>');
    // Секретный ключ проекта, полученный при интеграции от Some PlatformСоздать объект классаи указать значение секретного ключа, полученное от Some Platform. Секретный ключ необходим для автоматического подписывания параметров.
3. $url = $gate->getPurchasePaymentPageUrl(${empty}, $payment);domen_PP/payment?signature=OEKRlLXKStyo
...%3D%3D&amp;payment_id=1555943554067...123*pp_host*Здесь вместо <> надо указать доменное имя Payment Widget, которое следует узнавать у своего курирующего менеджера или в службе поддержки.Корректный запрос для вызова платежной формы содержит подпись и параметры платежа:Сформировать запрос для вызова платежной формы.
4. Использовать сформированный запрос для вызова платежной формы.

## Обработка оповещений

**HTTPPOSTJSONJSON**Информацию о результатах проведения платежей можно получать в оповещениях, отправляемых со стороны Some Platform на URL, который необходимо сообщить службе технической поддержки Some Platform. Оповещение представляет собойзапрос с данными в формате-строки. Чтобы извлечь информацию о результате проведения платежа из-строки, необходимо:

Далее приведен пример данных из оповещения, которое включает в себя подпись и информацию о результатах проведения платежа. При использовании sdk_php проверка подписи в оповещении выполняется автоматически.

1. `Gate`*secret_key*$gate = new sdk_php_lib\Gate('<>');Если ранее, при формировании запроса для вызова Payment Widget, не был создан объект класса, создать его и указать значение секретного ключа, полученного от Some Platform.
2. `Callback`**JSON**$callback = $gate->handleCallback($data);Создать объект класса, используя-строку с информацией о платеже из оповещения от Some Platform:
3. Callback::getPaymentId();            // Получение идентификатора платежа
Callback::getPaymentStatus();        // Получение текущего статуса платежа
Callback::getPayment();              // Получение всей информации о платежеИспользовать методы, доступные для работы с оповещениями. Можно получить полную информацию о платеже или информацию только об отдельных параметрах платежа:

## Дополнительные материалы

ru_pp_callbacks.ditaru_pp_payment_model.ditaДля организации работы с оповещениями также могут быть полезны следующие материалы:
