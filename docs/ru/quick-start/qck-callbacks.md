# Оповещение (callback)

## Общая информация

Для передачи промежуточной и итоговой информации о результате обработки и выполнения запроса используются оповещения. Оповещения отправляются в случае завершении обработки операции на стороне платежной платформы.

В каждом оповещении от платформы содержатся следующие элементы в порядке перечисления:

Дополнительные сведения о подписи данных см. в.ru_signature_intro.dita

Справа представлен пример оповещения с информацией о результате проведения платежа. Содержимое JSON-строки в этом примере разбито на несколько строк для удобства чтения.

- стартовая строка с указанием метода передачи запроса (), URL вашей системы для отправки оповещений о результатах (в примере —), протокола и его версии ();`POST/notify/successHTTP/1.1`
- поля заголовка, в том числе полес указанием доменного имени вашей системы (в примере —);`Hostwebservice.com`
- пустая строка — разделитель, отделяющая служебную информацию от тела оповещения;
- тело оповещения в формате JSON, содержащее набор данных оповещения и подпись. В теле оповещения используются только символы Unicode (UTF-8).

## Доставка оповещений

Оповещения отправляются сразу же после проведения операции.

Факт доставки оповещения в вашу систему фиксируется на стороне платежной платформы в ответе от вашей системы. Ответ должен содержать HTTP заголовок с кодом ответа.**200 ОК**

В случае если необходимо отложить отправку оповещения, в запросе на проведение платежа следует передать параметрв объектес необходимым значением в секундах. Максимальное значение задержки отправки составляет 600 секунд.

Если оповещение не было доставлено, со стороны платежной платформы выполняются повторные попытки отправки оповещения по следующему расписанию:

Максимально платежная платформа выполняет 120 попыток доставки оповещения в течение 11 суток. Расписание отправки может незначительно изменяться в зависимости от загруженности системы.

- 6 попыток с интервалом в 10 сек.;
- далее 62 попытки в течение четырех часов с увеличивающимся интервалом (интервал увеличивается в соответствии с формулой, где— порядковый номер попытки отправки);*n*``
- потом каждые 4 часа до достижения максимального количества попыток.

## Адреса для приема оповещений

Платежная платформа Some Platform отправляет оповещения на URL-адрес, который вы предоставляете сотрудникам Some Platform в процессе интеграции. Если у вас есть несколько проектов, вы можете предоставить для каждого проекта отдельный URL-адрес, на который платежная платформа будет отправлять оповещения.

Вы можете предоставить сотрудникам Some Platform отдельные URL-адреса, чтобы получать оповещения с определенной информацией по платежу. Например, чтобы оповещения об успешном завершении платежа платежная платформа отправляла на один адрес, а об отклонении — на другой. Вы также можете предоставить отдельный URL-адрес для получения оповещений по платежам определенного типа.или платежам, проведенным с использованием конкретного платежного метода

Вы можете самостоятельно указать дополнительные адреса для приема оповещений в Dashboard. Для этого в Dashboard перейдите в раздели выберите вкладку. Инструменты в Dashboard позволят вам определить, в каких случаях и на какие адреса Some Platform будет отправлять вашей системе оповещения.**ПроектыОповещения**

Обратите внимание, что ваша система должна принимать оповещения только с IP-адреса, полученного от команды интеграции Some Platform. Чтобы уточнить IP-адрес, с которого Some Platform отправляет оповещения, обратитесь в службу поддержки Some Platform.

**Адреса для приема оповещений по конкретному платежу**

Вы можете указать отдельный URL-адрес для приема оповещений по конкретному платежу. Для этого:

После этого все оповещения по этому платежу (и только по нему) будут отправляться на указанный в этом параметре URL-адрес независимо от того, какой URL-адрес для приема оповещений задан для вашего проекта.

- в запрос на открытие страницы оплаты добавьте параметри задайте в качестве его значения URL-адрес, на который Some Platform будет отправлять оповещения;
- в запрос на выплату добавьте в объектпараметри задайте в качестве его значения URL-адрес, на который Some Platform будет отправлять оповещения.

## Обработка оповещений

При обработке оповещений ваша система должна следовать следующим правилам:

1. Принимать оповещения только с IP адреса, полученного от службы поддержки Some Platform.
2. На успешно принятые оповещения ответ вашей системы должен быть.`HTTP 200 OK`
3. На оповещения, которые уже ранее были приняты вашей системой, ответ должен быть также.`HTTP 200 OK`
4. На оповещения, принятые с ошибкой, ответ вашей системы должен быть(например, если вам не удалось преобразовать строку параметров в массив) или(например, если оповещение пришло на неправильный URL-адрес).`HTTP 400 Bad RequestHTTP 500 Internal Server Error`
5. Повторные оповещения, которые ранее не были приняты или были приняты с ошибкой, ваша система должна попытаться принять еще раз.
6. Ваша система не должна ограничивать время на доставку оповещений, доставка оповещений может происходить с задержкой.
