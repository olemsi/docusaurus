# Выплата

Выплата — это тип платежа, в котором осуществляется перевод денежных средств пользователю с вашего счета.

Платежная платформа поддерживает следующие варианты проведения выплаты:

В этом разделе рассказывается о выплате по номеру карты и токену. О массовых выплатах говорится в разделе о Dashboard.

ru_gate_payout_statuses.ditaСхема и статусы выплаты представлены в разделе.

- ru_gate_payout_card.dita
- ru_gate_payout_token.dita
- Массовая выплата через Dashboard

## Схема и статусы выплаты

на счета, ассоциированные с платежными картами,ru_pm_intro.dita, а о проведении выплат на альтернативные платежные инструменты — в разделеru_gate_payout.ditaДалее в рамках данного раздела представлена информация о возможных статусах выплаты и связанных с ней операций. Более подробную информацию о проведении выплатможно найти в разделе.

### Общая информация

*Выплата*— это тип платежа, в рамках которого на основании одного исходного запроса осуществляется один перевод денежных средств от мерчанта к пользователю.

ru_db_payments.ditaВ рамках платежной платформы поддерживается один вариант для работы с выплатами через запросы (разовые единичные выплаты), но дополнительно обеспечивается возможность проведения массовых выплат через Dashboard (с автоматическим формированием требуемого количества платежей; подробнее — в разделе).

### Схема проведения

`payoutpayout`подробнееЧтобы инициировать выплату, следует отправить в платформу запрос категории. Для выполнения такого запроса в платформе формируется операция, результатом выполнения которой является перечисление средств на счет пользователя. При выполнении повторных попыток проведения выплаты в рамках одного платежа или разбиении суммы платежа формируются несколько таких операций ().

*уточнение информации*`clarification`В настоящее время эта процедура не используется для работы альтернативными платежными методами.Для проведения выплаты может требоваться отправка дополнительного запроса, если необходимодля какой-либо из сторон, участвующих в проведении платежа (например, предоставление в платежную систему адреса держателя карты, не переданного в исходном запросе). В этом случае от платформы в веб-сервис отправляется оповещение с названиями параметров для уточнения и проведение платежа в платформе приостанавливается до получения от веб-сервиса запроса с необходимой информацией —.

### Статусы платежа

При проведении выплаты могут использоваться следующие статусы.

| `error` | Проведение платежа не инициировано из-за ошибки, возникшей при проверке принятого запроса | *Конечное состояние. Допускается повторная отправка запроса с тем же идентификатором платежа и повторная попытка проведения этого платежа* |
| --- | --- | --- |
| `processing` | Платеж проводится | *Промежуточное состояние* |
| `awaiting clarification` | `clarificationdecline`Проведение платежа приостановлено до получения запроса на продолжение с учетом требуемой дополнительной информации —. Если такой запрос не получен в течение 30 минут, платеж переводится в статус | *Промежуточное состояние* |
| `awaiting retry` | ru_gate_payout_retry.ditaОжидаются повторные попытки выполнения выплаты в рамках проведения платежа (подробнее — в разделе) | *Промежуточное состояние* |
| `partially_paid_out` | ru_gate_split_payment.ditaПлатеж проведен частично, с разбиением суммы (подробнее — в разделе) | *Конечное состояние* |
| `decline` | Платеж отклонен | *Конечное состояние* |
| `success` | Платеж проведен | *Конечное состояние* |
| `reversed` | Платеж отменен | *Конечное состояние* |

### Статусы операции payout

`payout`При выполнении операциимогут использоваться следующие статусы.

| `processing` | Операция выполняется | *Промежуточное состояние* |
| --- | --- | --- |
| `awaiting clarification` | `clarificationdecline`Выполнение операции приостановлено до получения запроса на продолжение с учетом требуемой дополнительной информации —. Если такой запрос не получен в течение 30 минут, операция переводится в статус | *Промежуточное состояние* |
| `decline` | Операция отклонена | *Конечное состояние* |
| `success` | Операция выполнена | *Конечное состояние* |

### Дополнительные материалы

При работе с выплатами могут быть полезны следующие материалы:

- ru_gate_interaction_organisation.dita— раздел с общей информацией о взаимодействии с платежной платформой через Gate.
- ru_gate_payout.dita— раздел с подробной информацией о проведении выплат на счета, ассоциированные с платежными картами.
- ru_gate_callbacks.dita— раздел с информацией об оповещениях и работе с ними.
- ru_gate_statuses_and_response_codes.dita— раздел с информацией о кодах ошибок, используемых в платежной платформе.

## Схема и статусы выплаты

В данном разделе представлена информация о возможных статусах выплаты и связанных с ней операций.

### Статусы платежа

В процессе выплаты платежу могут присваиваться следующие статусы.

| Статус платежа | Описание |
| --- | --- |
| `error` | При проверке запроса возникла ошибка, поэтому платежная платформа не приняла запрос и не начала обработку платежа.*Конечное состояние. Вы можете отправить запрос на проведение этого платежа повторно с тем же идентификатором платежа* |
| `processing` | Платеж в процессе выполнения.*Промежуточное состояние* |
| `awaiting clarification` | `decline`Проведение платежа приостановлено. Платежная платформа ожидает от вас запрос с недостающей информацией. Если такой запрос не поступит от вас в платежную платформу в течение 30 минут, платеж будет отклонен и переведен в статус.*Промежуточное состояние* |
| `awaiting retry` | ru_gate_payout_retry.ditaОжидаются повторные попытки выплаты (подробнее — в разделе).*Промежуточное состояние* |
| `partially_paid_out` | ru_gate_split_payment.ditaПлатеж проведен частично, с разбиением суммы (подробнее — в разделе).*Конечное состояние* |
| `decline` | *Конечное состояние*Платеж отклонен. |
| `success` | *Конечное состояние*Платеж проведен. |
| `reversed` | *Конечное состояние*Платеж отменен. |

### Статусы операции payout

`payout`Следующие статусы могут быть назначены операции.

| Статус операции | Описание |
| --- | --- |
| `processing` | *Промежуточное состояние*Операция выполняется. |
| `awaiting clarification` | `decline`*Промежуточное состояние*Выполнение операции приостановлено. Платежная платформа ожидает от вас запрос с недостающей информацией. Если такой запрос не поступит от вас в платежную платформу в течение 30 минут, операция будет отклонена и переведена в статус. |
| `decline` | *Конечное состояние*Операция отклонена. |
| `success` | *Конечное состояние*Операция выполнена. |

### Ссылки по теме

- ru_gate_interaction_organisation.dita
- ru_gate_callbacks.dita
- ru_gate_statuses_and_response_codes.dita

---

## Выплата по номеру карты

### Общая информация

В данном разделе описана выплата через Gate по номеру карты. Реквизиты банковской карты, как правило, передаются в явном виде, и в таких случаях необходимо соблюдать требования отраслевого стандарта безопасности данных платежных карт — PCI DSS (Payment Card Industry Security Standards Council).

### Шаги проведения выплаты

Чтобы выполнить выплату через Gate по номеру карты:

Далее в данном разделе представлена схема проведения выплаты в базовом случае, то есть без дополнения информации о платеже.

Далее рассказывается о структуре запроса на выплату по номеру карты, а также о структуре оповещений с результатами выплаты.

1. Отправьте запрос с нужными параметрами и подписью на рабочий URL-адрес платежной платформы Some Platform.
2. ru_gate_clarification.ditaПодробнее о предоставлении дополнительной информации, см..При необходимости предоставьте дополнительную информацию о платеже.
3. Примите от платежной платформы оповещение (callback) о результате проведения выплаты.

### Запрос

Далее представлена информация, необходимая для составления и отправки запроса на выплату.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/card/payout` |
| Полная спецификация конечной точки | request-payment-card-payout |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросеtype_optional — параметр необязателен, но в общем случае его наличие в запросе помогает облегчить проведение платежа, например упрощает процесс платежа для пользователя или повышает процент успешных платежей.type_conditional — обязательность параметра зависит от ситуации. Подробнее узнавайте в описании параметра. |  |  |
| --- | --- | --- |
| Объект/массив | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| customertype_object type_strictly_required | idtype_string type_strictly_required | `customer_123`Пример:Идентификатор пользователя, уникальный в рамках проекта. |
| first_nametype_string type_strictly_required | `John`Пример:Имя пользователя. |  |
| middle_nametype_string type_optional | `Paul`Пример:Отчество или среднее имя пользователя. |  |
| last_nametype_string type_strictly_required | `Doe`Пример:Фамилия пользователя. |  |
| ip_addresstype_string type_strictly_required | `198.51.100.47`Пример:IP-адрес устройства пользователя. |  |
| ziptype_string type_conditional | Обязательность данного параметра уточняйте у курирующего менеджера Some Platform.`6000`Пример:Почтовый индекс пользователя. |  |
| countrytype_string type_conditional | Обязательность данного параметра уточняйте у курирующего менеджера Some Platform.`AU`Пример:Страна пользователя в формате ISO 3166-1 alpha-2. |  |
| statetype_string type_conditional | Обязательность данного параметра уточняйте у курирующего менеджера Some Platform.`WA`Пример:Штат, провинция или регион пользователя. Передавайте этот параметр только для следующих стран: country_AU, country_CA или country_US. |  |
| citytype_string type_conditional | Обязательность данного параметра уточняйте у курирующего менеджера Some Platform.`Perth`Пример:Город пользователя. |  |
| addresstype_string type_conditional | Обязательность данного параметра уточняйте у курирующего менеджера Some Platform.`89 St Georges Terrace, 6000, Perth, AUSTRALIA`Пример:Адрес пользователя. |  |
| paymenttype_object type_strictly_required | amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.ditareferences/ru/currencies/USD.dita`10000`Пример: 100,00передается какСумма выплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| currencytype_string type_strictly_required | `references/ru/currencies/USD.dita`Пример:Код валюты выплаты в формате ISO-4217 alpha-3. |  |
| cardtype_object type_strictly_required | pantype_string type_strictly_required | `1122334455667788`Пример:Номер карты. |
| yeartype_integer type_conditional | Обязательность данного параметра уточняйте у курирующего менеджера Some Platform.`2025`Пример:Год окончания срока действия карты. |  |
| monthtype_integer type_conditional | Обязательность данного параметра уточняйте у курирующего менеджера Some Platform.`8`Пример:Месяц окончания срока действия карты. |  |
| card_holdertype_string | `JOHN DOE`Пример:Имя держателя карты (в соответствии с указанным на карте). |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

::: tip
Для карт других платежных систем эти сведения должны передаваться базовой латиницей.Имя, отчество (или среднее имя) и фамилию получателя необходимо передавать для всех карт, за исключением выпущенных в Российской Федерации. Для последних передача этих параметров не обязательна.
:::

### Оповещение (callback)

ru_gate_callbacks.ditaУведомление о проведении выплаты платежная платформа возвращает в оповещении. Подробнее о структуре оповещений см..

`123100,00 USD553691******0802customer_123`Вот пример тела оповещения с информацией о том, что в рамках проектапроведена выплата в размерена картупользователя.

Далее представлен пример данных из оповещения с информацией об отказе в проведении выплаты. Платеж отклонен из-за превышения максимально разрешенного размера выплаты.

### Ссылки по теме

- ru_gate_payout_statuses.dita
- ru_gate_payout_retry.dita

---

## P2P-выплата

### Общая информация

P2P-выплата (person-to-person) — это тип платежа, в котором осуществляется перевод денежных средств от одного пользователя другому.

### Шаги проведения выплаты

Чтобы выполнить P2P-выплату через Gate:

Далее в данном разделе представлена схема проведения выплаты в базовом случае, то есть без дополнения информации о платеже.

Далее рассказывается о структуре запроса на P2P-выплату, а также о структуре оповещений с результатами выплаты.

1. Отправьте запрос с нужными параметрами и подписью на рабочий URL-адрес платежной платформы Some Platform.
2. В настоящее время эта процедура не используется для работы с альтернативными платежными методами.ru_gate_clarification.ditaПодробнее о предоставлении дополнительной информации, см..При необходимости предоставьте дополнительную информацию о платеже.
3. Примите от платежной платформы оповещение (callback) о результате проведения выплаты.

### Запрос

Далее представлена информация, необходимая для составления и отправки запроса на выплату.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/individual/payout` |
| Полная спецификация конечной точки | request-payment-individual-payout |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросеtype_optional — параметр необязателен, но в общем случае его наличие в запросе помогает облегчить проведение платежа, например упрощает процесс платежа для пользователя или повышает процент успешных платежей.type_conditional — обязательность параметра зависит от ситуации. Подробнее узнавайте в описании параметра. |  |  |
| --- | --- | --- |
| Объект/массив | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| customertype_object type_strictly_required | idtype_string type_strictly_required | Идентификатор пользователя, уникальный в рамках проекта.`customer_123`Пример: |
| first_nametype_string type_strictly_required | Имя пользователя.`John`Пример: |  |
| middle_nametype_string type_strictly_required | Отчество или среднее имя пользователя.`Paul`Пример: |  |
| last_nametype_string type_strictly_required | Фамилия пользователя.`Doe`Пример: |  |
| ip_addresstype_string type_strictly_required | IP-адрес устройства пользователя.`198.51.100.47`Пример: |  |
| paymenttype_object type_strictly_required | amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.ditareferences/ru/currencies/USD.dita`10000`Пример: 100,00передается какСумма выплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| currencytype_string type_strictly_required | `references/ru/currencies/USD.dita`Пример:Код валюты выплаты в формате ISO-4217 alpha-3. |  |
| cardtype_object type_strictly_required | pantype_string type_strictly_required | Номер карты.`1122334455667788`Пример: |
| yeartype_integer type_conditional | Год окончания срока действия карты.Обязательность данного параметра уточняйте у курирующего менеджера Some Platform.`2025`Пример: |  |
| monthtype_integer type_conditional | Месяц окончания срока действия карты.Обязательность данного параметра уточняйте у курирующего менеджера Some Platform.`8`Пример: |  |
| card_holdertype_string type_strictly_required | Имя держателя карты (в соответствии с указанным на карте).`JOHN DOE`Пример: |  |
| sendertype_object type_optional | first_nametype_string type_optional | Имя отправителя.`Иван`Пример: |
| last_nametype_string type_optional | Фамилия отправителя.`Иванов`Пример: |  |
| citizenshiptype_string type_optional | Гражданство отправителя.`LV`Пример: |  |
| residencetype_string type_optional | Страна, резидентом которой является отправитель.`BL`Пример: |  |
| birthplacetype_string type_optional | Место рождения отправителя.`Москва`Пример: |  |
| billingtype_object type_optional | Объект с информацией о платежном адресе отправителя. |  |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

::: tip
Для карт других платежных систем эти сведения должны передаваться базовой латиницей.Имя, отчество (или среднее имя) и фамилию получателя необходимо передавать для всех карт, за исключением выпущенных в Российской Федерации. Для последних передача этих параметров не обязательна.
:::

### Оповещение (callback)

ru_gate_callbacks.ditaУведомление о проведении P2P-выплаты платежная платформа возвращает в оповещении. Подробнее о структуре оповещений см..

`123100,00 USD553691******0802customer_123`Вот пример тела оповещения с информацией о том, что в рамках проектапроведена выплата в размерена картупользователя.

Далее представлен пример данных из оповещения с информацией об отказе в проведении P2P-выплаты. Платеж отклонен из-за превышения максимально разрешенного размера выплаты.

### Ссылки по теме

- ru_gate_payout_statuses.dita
- ru_gate_payout_retry.dita

---

## Выплата по токену

### Общая информация

В данном разделе описана выплата через Gate по токену платежной карты.

ru_gate_token.ditaТокен — это идентификатор, представляющий собой уникальную строку из 64 символов и связанный с определенной банковской картой и пользователем. Токен не содержит конфиденциальной информации и может храниться в вашей системе, не создавая угрозы нарушения стандартов безопасности по хранению данных банковских карт. Более подробную информацию о токенах см. в разделе.

### Шаги проведения выплаты

Чтобы выполнить выплату через Gate по токену:

Далее в данном разделе представлена схема проведения выплаты в базовом случае, то есть без дополнения информации о платеже.

Далее рассказывается о структуре запроса на выплату по токену карты, а также о структуре оповещений с результатами выплаты.

1. Отправьте запрос с нужными параметрами и подписью на рабочий URL-адрес платежной платформы Some Platform.
2. В настоящее время эта процедура не используется для работы с альтернативными платежными методами.ru_gate_clarification.ditaПодробнее о предоставлении дополнительной информации, см..При необходимости предоставьте дополнительную информацию о платеже.
3. Примите от платежной платформы оповещение (callback) о результате проведения выплаты.

### Запрос

Далее представлена информация, необходимая для составления и отправки запроса на выплату.

| HTTP-метод запроса | POST |
| --- | --- |
| Формат тела запроса | JSON |
| Конечная точка | `/v2/payment/card/payout/token` |
| Полная спецификация конечной точки | request-payment-card-payout-token |

| type_strictly_required — параметр обязательно должен присутствовать в начальном запросеtype_optional — параметр необязателен, но в общем случае его наличие в запросе помогает облегчить проведение платежа, например упрощает процесс платежа для пользователя или повышает процент успешных платежей.type_conditional — обязательность параметра зависит от ситуации. Подробнее узнавайте в описании параметра. |  |  |
| --- | --- | --- |
| Объект/массив | Параметр | Описание |
| generaltype_object type_strictly_required | project_idtype_integer type_strictly_required | `123`Пример:Идентификатор проекта, полученный от Some Platform при интеграции. |
| payment_idtype_string type_strictly_required | `payment_47`Пример:Идентификатор платежа, уникальный в рамках проекта. |  |
| signaturetype_string type_strictly_required | Использование подписи к даннымПодпись запроса, составленная после определения всех параметров запроса. Подробнее о составлении подписи см.. |  |
| customertype_object type_strictly_required | idtype_string type_strictly_required | Идентификатор пользователя, уникальный в рамках проекта.`customer_123`Пример: |
| first_nametype_string type_strictly_required | Имя пользователя.`John`Пример: |  |
| middle_nametype_string type_strictly_required | Отчество или среднее имя пользователя.`Paul`Пример: |  |
| last_nametype_string type_strictly_required | Фамилия пользователя.`Doe`Пример: |  |
| ip_addresstype_string type_strictly_required | IP-адрес устройства пользователя.`198.51.100.47`Пример: |  |
| paymenttype_object type_strictly_required | amounttype_integer type_strictly_required | *за исключениемосновных единицах валюты*ru_currency_codes.ditareferences/ru/currencies/USD.dita`10000`Пример: 100,00передается какСумма выплаты в дробных единицах валюты без десятичной точки и пробеловслучаев, когда у валюты нет дробной части. Если у валюты нет дробных единиц (то есть количество разрядов дробных единиц равно нулю), то в этом параметре нужно указывать сумму в. Подробнее о разрядах дробных единиц у валют см.. |
| currencytype_string type_strictly_required | `references/ru/currencies/USD.dita`Пример:Код валюты выплаты в формате ISO-4217 alpha-3. |  |
| tokentype_string type_strictly_required |  | `f365bb1729f9b72fd9c79f3becc679f29c3e35c91d070d15654`Пример:ru_gate_token.ditaПодробнее см.Токен банковской карты пользователя. Токен представляет собой уникальную строку из 64 символов, связанную с банковской картой. |
| cardtype_object type_conditional | card_holdertype_string type_conditional | Имя держателя карты (в соответствии с указанным на карте).Этот параметр обязателен, если выплата выполняется по токену, при создании которого имя держателя карты не использовалось.`JOHN DOE`Пример: |
| API ReferenceПри необходимости добавьте в запрос необязательные параметры, указанные в спецификации Gate:. |  |  |

::: tip
Для карт других платежных систем эти сведения должны передаваться базовой латиницей.Имя, отчество (или среднее имя) и фамилию получателя необходимо передавать для всех карт, за исключением выпущенных в Российской Федерации. Для последних передача этих параметров не обязательна.
:::

### Оповещение (callback)

ru_gate_callbacks.ditaУведомление о проведении выплаты платежная платформа возвращает в оповещении. Подробнее о структуре оповещений см..

`123100,00 USD553691******0802customer_123`Вот пример тела оповещения с информацией о том, что в рамках проектапроведена выплата в размерена картупользователя.

Далее представлен пример данных из оповещения с информацией об отказе в проведении выплаты. Платеж отклонен из-за превышения максимально разрешенного размера выплаты.

### Ссылки по теме

- ru_gate_payout_statuses.dita
- ru_gate_payout_retry.dita
- ru_gate_token.dita
